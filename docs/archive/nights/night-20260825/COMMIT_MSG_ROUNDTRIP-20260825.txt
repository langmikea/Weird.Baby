every field the museum draws now survives the round trip, or is refused by name

THE REPAIR THAT STANDS BETWEEN THE DAY EDITOR AND MIKE TYPING INTO IT. Round:
C:\AI\_night-20260825\ROUND_TRIP_REPAIR_LOG-20260825.md.

THE FULL SET IS SEVEN AND OPS KNEW OF THREE. Established by measurement rather
than by reading the lists: a fixture entry carrying every field
DRAWN_ENTRY_FIELDS names, in every shape the renderer accepts, run through the
real reader and the real emitter, with each "drawn" claim then confirmed at its
render site. stamp (record-model.js:100, Exhibit.jsx:5239 and 5475). note
(RecordEntry.jsx:749). wire (record-model.js:180 and 221-226, Exhibit.jsx:5509).
plates on the entry (record-model.js:181 and 231-242). Section doors. The body
{pre} marker. And the one nobody had named: EVERY SUB-FIELD OF AN ATTACHMENT.
Plus evidence, an eighth of a different kind - declared, deliberately undrawn
since R5, not carried.

AND THE DISTINCTION MATTERS MORE THAN THE COUNT. SIX OF THE SEVEN WERE ALREADY
LOUD: the reader files them in `unreadable`, and recordShapeFaults() - the list
reveal:check and npm run record read - returns them by record number and field
name, measured on the fixture. The reader half has been honest since 2026-08-11.
NUMBER SEVEN WAS THE ONLY SILENT ONE. draftEntries was taught source, pages and
plates on 2026-08-19 PRECISELY so they would travel, and the emitter then wrote
them away with nothing printed anywhere.

PROVED BY LOSING SOMETHING FIRST, ON REAL DATA. Record 003 read out of the tree
with four attachments carrying source, pages and six plates with their captions.
Through the real record:land it came back as four bare titles - four sources,
four page counts and SIX PUBLISHED PHOTOGRAPHS WITH THEIR CAPTIONS destroyed -
while the tool's own proof printed, in the same run, "ALL 51 STRINGS ROUND-TRIP:
his characters are unchanged." On the fixture the emitted entry carried no
stamp, no wire, no plates, no note, and Record 004's folder tree came back an
ordinary paragraph.

AFTER: the fixture's field set in and out is IDENTICAL and the values are deep
equal ignoring key order - key order inside an object literal differs because
pages is emitted after the string fields, and key order is not data, which is
stated rather than glossed. The listing returns as { pre: "..." }. And the real
Record, end to end - source, reader, real emitter, parsed back, compared field by
field across all five entries - reads WHOLE: every field of every entry survived,
0 reported on the way in and 0 on the way back. record:proof's strongest check
still passes: a no-op landing is BYTE-IDENTICAL, sha 20f4c8aa.

CARRY OR REFUSE, PER FIELD. CARRIED: stamp and note (plain strings); wire (a list
of strings, one li per line on the glass); plates on the entry - the same
{img,label} shape the attachment half already read, so it is ONE platesOf() for
both places a plate hangs, because two readers for one shape is how halves
drift; the body {pre} MARKER and not merely its text, since folding it to a
string kept every character and threw away the one bit that says they are
COLUMNS; and every attachment sub-field. REFUSED, LOUDLY AND BY NAME: section
doors, because doors are Ops' wiring rather than his writing and their shape
varies by kind - a reader that half-understood one could emit a broken door,
which is worse than refusing; and evidence, because nothing draws it, no entry
carries one, and its shape is undeclared, so teaching a reader for it would be
guessing at a shape.

THE ATTACHMENT COMMENT WAS RIGHT ABOUT AUTHORSHIP AND WRONG ABOUT WRITING. It
said only title is emitted "because only title is what he wrote", and that
filling in source, date, scan or plates would be inventing provenance. The first
half is still true - nothing here authors a doc field, and a doc with a title
and no image stays a designed state that prints "not here yet". The second half
confused AUTHORING with CARRYING. A field that arrived is written back: that is
provenance preserved, and it is the U-round rule this file already lives under -
a generator may remove a field, never the answer that was in it.

AND THE EMITTER REFUSES WHAT IT CANNOT WRITE. EMITTED_ENTRY_FIELDS and
EMITTED_DOC_FIELDS are the mirror of the reader's sets - a list of what is
emitted, not of what is allowed, the same construction as DRAWN_ENTRY_FIELDS - and
a draft carrying anything else stops the run by name before a byte reaches disk.
That is the half that was missing: the reader has refused since 2026-08-11 and
the end that WRITES THE TREE never did.

THE TRAP THIS ROUND HAD TO AVOID IS NAMED IN THE FILE IT EDITS: the 2026-08-19
note warns that widening READ_ENTRY_FIELDS ALONE silences a true warning while
the tool downstream still drops the field. The set, the reader and the emitter
were widened together, and the before/after is what proves it rather than the
list.

WHAT ELSE HAD TO MOVE. textOf() is exported from record-entries.mjs as the one
place anything asks a body item for its words, so a third shape is one function
to teach rather than six sites to find. record-edit.mjs matches a paragraph
through it - PROVED not to change behaviour where it matters, since Records 001
and 003, the two that print misplaced-note warnings, carry only string bodies
and for an all-string body indexOf and findIndex(textOf) are the same test;
those warnings are pre-existing. day.mjs draws stamp now, because a field the
reader carries and the editor hides is the same hole one surface further on, and
its `locked` flag CAME OFF - it said wire, plates and note cannot survive the
draft, which was true when written and is false as of today. record.html is
regenerated and its ONLY difference is Record 004's folder tree seeding as
{"pre":"..."} instead of a bare string. day.html did not change at all, which is
the expected reading: no entry carries stamp, wire, plates or note today.

THE GUARD THAT SHOULD HAVE CAUGHT IT - REPORTED, NOT BUILT, AS ASKED. Nothing
compares the three lists and they live in three files: DRAWN_ENTRY_FIELDS in
tools/reveal-ledger.mjs asks does anything RENDER this; READ_ENTRY_FIELDS in
reveal/record-entries.mjs asks can the surface HOLD it; EMITTED_ENTRY_FIELDS,
new today, asks can the tree be WRITTEN with it. Each guards its own edge and no
one asserts the relation, which is why this bit twice - 013 lost four paragraphs
at the reader and the attachments lost six photographs at the writer, for the
same structural reason. WHAT WOULD CATCH IT: a check asserting DRAWN is a subset
of READ is a subset of EMITTED, with an explicit exemption list carrying a
written ruling per exemption - the construction DRAWN_ENTRY_FIELDS already uses
for evidence - covering the sub-levels too (READ_SECTION_FIELDS, READ_DOC_FIELDS,
EMITTED_DOC_FIELDS, and the body-item shapes). It does not MERGE the lists; the
three questions are genuinely different and the files say so at length.
WHERE IT BELONGS: reveal:check, not its own tool. It already owns
DRAWN_ENTRY_FIELDS, so the check sits beside its own data; it already runs on
every packet and already refuses one, where a separate tool is a sixth command
in section 9's list and section 0 records that every gate here except the deploy
guard is human discipline; and exporting the other two sets is a line each and
makes the coupling visible. A SECOND FINDING BELONGS WITH IT: record:land
--verify prints "ALL 51 STRINGS ROUND-TRIP: his characters are unchanged", which
is TRUE, is read as "the entry round-tripped", and printed green over six
photographs being destroyed. Whatever the guard becomes, that line should say
what it proved and what it did not.

FLAGGED, NOT FIXED - npm run record:proof FAILS 3 OF ITS CHECKS AND DOES SO AT
HEAD: "a CHANGE to a commented entry is REFUSED", "a change to an UNcommented
entry (005) lands and loses no comments", and "guard 6: it may not eat the
reasoning". Verified pre-existing rather than assumed: the four changed files
were snapshotted with sha256, restored to HEAD with git checkout --, the proof
re-run to identical failures, and the work restored and proved byte-identical
against the snapshot. Not absorbed into this round. Also carried: STAGE_PREFIX
is imported by day.mjs and used by nothing.

GATES, REAL EXIT CODES PER GATE: lint exit 1, which IS the baseline - 17
problems, 9 errors / 8 warnings, zero new - build 0 - provenance 0 -
reveal:check 0 - instory 0 - parity 0 - arc:check 0 - ops:size 0 -
docs:numbers:gate 0 - npm run record exit 0, 16 sections read, 1 written as a
list+pre - record:land --verify exit 0 - reveal:day "Nothing to move. The tree
and the Record agree." - lap:clean, nothing left in public/. Nothing in src/
changed, so the museum lap does not apply. NOTHING WAS LANDED: every round trip
ran against a draft in a scratch directory, and record:proof reports the source
file identical to where it started.
