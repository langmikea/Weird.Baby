the hover hints are one short line, and the ceiling is in the function

MIKE, 2026-08-26, after driving the day editor: "Kill the giant hover hint
boxes - way too much to bother reading any of it."

Measured before anything was cut: 484 hints, 98,378 characters, every one of
them two lines, median 161 and the longest 894 - on the box he was typing into.
A hint nobody reads is worse than no hint, because it looks like help.

After: 386 hints, 8,154 characters, median 21, longest 60, none of them two
lines. Ops ruled cut them down and not out, so the test applied per mark was
whether the thing can already speak for itself.

A GLYPH OR A ONE-LETTER MARK cannot, so it keeps a hint of a few words plus
what it reads - "ready · press for not ready", "EXECUTIVE SUMMARY · ready",
"files · 2 behind the door". ANYTHING ALREADY LABELLED IN WORDS ON THE GLASS
lost its hint outright: a box titled EXECUTIVE SUMMARY, a filename printed under
its tile, a plate's own caption, SECTIONS · 5, the panel summaries, the shelf
section labels, the dateline, the "3 of 5". Each was a sentence introducing a
string an inch away, and together they were most of the 98,378 characters.
A LIVE NUMBER survives as numbers only, in the budget mark's own grammar:
"9 lines · longest 45/68 · 4 spaces off".

The worst one is worth naming. The lines box carried 894 characters explaining
what a section's lines are and where 68ch comes from, on a box he is looking at
and typing into. The box is the measure - that was always the argument for
having a box - so a paragraph restating it is the page not trusting its own
design.

The second line went entirely. The scheme was MEANING then READING and the
meaning was the same sentence every time that mark was drawn on any day. It
earned its place on the first hover and was furniture on every one after.

AND THE CEILING IS ENFORCED IN hint() RATHER THAN WRITTEN DOWN. Doctrine 25
records that this exact thing grows back - week1.html was split for this
complaint on 2026-08-07 and the worksheet's masthead was seven paragraphs three
rounds later. So hint() throws: one line, 60 characters, and a longer one stops
the build by name. It is not decoration. It caught four hints during this
round's own cut, after I had already been through the file by hand. The client
half cuts instead of throwing, because nothing is being built while he types.

One hint now beats the thing it sits on rather than restating it: a shelf tile
is 52px and shows no caption, so its hint is its label.

MEASURED SERVED at 127.0.0.1:8899 - 379 hints, 7,987 characters, median 21, max
60, zero two-line and zero over the ceiling after clicking and typing. The state
button repaints its own hint through the cycle and back to ready. Nothing else
moved: box content 429.94px = 68ch, indent 14px one level, 0 sideways overflow
at 1280 and 375, 7 marks on all five calendar rows, 0 console errors. At 1280
window.scrollTo(0,900) leaves html and window at 0 and the record scrolls 412px
while the calendar moves 0.

One reading looked like a regression and was not: a first measurement showed the
page scrolling 1,382px, and the browser pane was 800px wide - inside the
max-width:820px fallback where the page is designed to scroll. Re-measured at a
forced 1280 the desktop ruling holds exactly. Suspect the probe; here the probe
was the viewport nobody had checked. NOT PHOTOGRAPHED: the pane would not
composite, the same refusal as pass five.

Nothing in src/ changed, so the museum lap does not apply, and the Record, the
draft and the marks are byte-identical to where they started.

Gates: lint 1 (baseline 9/8, zero new) · build 0 · provenance:gate 0 ·
reveal:check 0 · instory:gate 0 · parity:gate 0 · arc:check 0 · ops:size 0 ·
docs:numbers:gate 0 · day:proof 0 (29/29, 5 shown losing first) · reveal:day
nothing to move · lap:clean nothing left.

The round log is at C:\AI\_night-20260826\HINTS_CUT_LOG-20260826.md and nothing
was deployed.
