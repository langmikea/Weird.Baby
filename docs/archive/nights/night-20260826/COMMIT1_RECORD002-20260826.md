# COMMIT 1 — RECORD 002'S TWO BLOCKS, AND P6 (2026-08-26)

**HEAD at start `1d60732`.** One entry, as scoped. **003, 004 and 005 untouched.**

**FIVE FILES:** `src/data/artists/robots-record.js`,
`docs/canon/09-PUBLISHED.md`, `docs/dictation-20260807/moved-blocks.json` (new),
`tools/dictation/day-proof.mjs`, and the regenerated `day.html`.

---

## 1 · THE JUDGEMENT, PER BLOCK — BOTH ARE DUPLICATES IN FULL

**I EXPECTED A SPLIT AND THERE ISN'T ONE. Every claim in both blocks is already
written down elsewhere, in fuller form.** Nothing is standing-and-new, and
nothing is entry-specific-and-unique. **2,579 of 2,579 characters are a
delete-and-cite.**

### BLOCK 0 — 1,383 characters, `[2026-08-18] HIS REWRITE, VERBATIM`

| what it says | class | already at |
|---|---|---|
| *"the whole body is replaced; three lines where there were six"* | duplicate | `MUSEUM_COPY_AND_RECORD_LAYOUT_LOG-20260817.md` § 27 — *"three lines for six"* |
| the ZIP-index line and the per-file-header line, **quoted verbatim** | duplicate | § 27, both quoted |
| the recovered-names line | duplicate | § 27 — **and § 27 also carries the invented count the comment deliberately withheld** |
| *"leaving the figure in the source is leaving a fact a later round could reinstate believing it was data"* | duplicate | `MUSEUM_RULINGS-20260817.md` § 9, near-verbatim |
| *"we do not hold back what we say we have…"* | **duplicate — Ruling 9's own quote** | `MUSEUM_RULINGS-20260817.md` § 9 |
| his typography as typed — `SUMMARY -All`, `Incoming Data:` | duplicate | § 27, verbatim |

### BLOCK 1 — 1,196 characters, `[2026-08-18] THE _tmp/ LINE … ARE BOTH STRUCK`

| what it says | class | already at |
|---|---|---|
| the `_tmp/` line and its marginal note, quoted | duplicate | § 27, and `docs/ARC.md:167` |
| the closing line, quoted | duplicate | Ruling 9, quoted whole |
| *"it pointed at 'the last entry', which was `_tmp/`, so it could not survive it"* | duplicate | Ruling 9, **near-verbatim** |
| *"A Record names only what it can produce"* | **duplicate — Ruling 9's one-line form** | Ruling 9 |
| *"the other ten entries are unchanged… the manifest is now one element rather than two"* | duplicate | § 28 — *"the manifest body splits to **10 lines**"* |
| *"Record 003 still opens on the tmp folder in its own words…"* | **duplicate AND STALE** | Ruling 9 carries it **and records that the line was replaced on 2026-08-19**, which the source comment never learned |

**THE LAST ROW IS THE ARGUMENT FOR THE WHOLE APPROACH.** The source comment
asserted something that stopped being true a day after it was written, and the
rulings file already carried the correction. **A copy in the source is a copy
that cannot be told when it goes stale.**

**AND ONE THING THE COMMENT DID BETTER THAN ITS HOME, CHECKED:** the three
struck DETAILED REPORT lines are quoted in the source and only NAMED in Ruling
9. They are quoted **verbatim in § 27 of the round log**, so nothing is lost —
but that is the check that had to be run before a deletion, and it is why the
manifest cites the log rather than the ruling for those three.

## 2 · WHAT MOVED, AND WHAT DID NOT

**NOTHING WAS ADDED TO `09-PUBLISHED.md` THAT WAS NOT ALREADY TRUE.** Its
`RECORD 002` section already carried the strike and a `[ruling 9]` citation.
What it gained is **the trail to the words** — a table naming, for each of the
five struck lines, which file holds its text — plus the typography flag and a
note that the source no longer carries any of it.

**THE SOURCE GAINED A PREAMBLE BLOCK** recording the deletion, its three
destinations, and the difference from the 2026-08-16 precedent.

### THE PRECEDENT, AND WHY IT WAS NOT FOLLOWED

**THIS HAS BEEN DONE TWICE BEFORE AND BOTH TIMES THE BLOCKS WENT TO THE
PREAMBLE** — Record 001's three and Record 003's one, *"moved for the same
reason… and the preamble is the part of this file a landing preserves. Not one
character of it is edited."* That is why 001 has no comments and lands.

**AND THE PRECEDENT LEFT ITS OWN WARNING IN PLACE.** The note above Record
003's moved block now reads: *"On 2026-08-16 the entry's TEXT was replaced from
NEW_RECORD_MAKER_V3.xlsx, so the section it describes is not the section that is
there now."* **A note parked in a preamble goes stale where nobody is reading
it.** That is the case for Ops' destinations rather than the preamble, and it is
recorded at the new block.

## 3 · P6 — AND IT WENT RED ON ITS FIRST RUN

`docs/dictation-20260807/moved-blocks.json`: per block, the **sha256 of the
bytes that left** and the **claims it made with the file each is cited to** —
**2 blocks, 16 claims, 3 files.** P6 reads the cited file and looks for the
phrase, on every run.

**IT IS THE ONLY CHECK IN `day:proof` THAT CAN GO RED WITHOUT THE EDITOR
CHANGING**, because it watches two other documents.

**AND IT CAUGHT A REAL ORPHAN IMMEDIATELY — MINE.** The first run:

```
FAIL  and all 16 claims are still findable where they are cited — 3 file(s)
      ORPHANED  entry 2 block 1 — the closing line, verbatim
                the cited file no longer carries it:
                "The last entry is the only one we can open"  in MUSEUM_RULINGS-20260817.md
```

The phrase **crosses a line wrap** in that markdown file. The citation was
wrong, not the ruling. Corrected to a phrase that lives on one line, and **the
constraint is now written into the manifest's own note** — a `find` phrase must
not cross a wrap.

**PROVED BY DELETING A LINE, AS RULED.** P6 removes one cited line from
`MUSEUM_RULINGS-20260817.md` and reports:

```
LOST  WITH ONE CITED LINE DELETED FROM docs/MUSEUM_RULINGS-20260817.md, P6 names
      the block that depended on it — entry 2 block 0, claim "the doctrine it
      quoted" — and the line it lost: "> **\"We do not hold back what we say we
      have. We hold back"
```

**The block and the line, both named.**

## 4 · PROOF

| | |
|---|---|
| **`draftEntries()` deep-equal** | **all five entries identical** to the committed draft — the deletion touched only comments |
| **comment characters** | in-span **21,486 → 18,907**, removed **2,579** — exactly block 0 + block 1 |
| | preamble **16,743 → 19,341** — the new audit block; **the preamble is outside every span, so it does not re-block anything** |
| **file** | 49,535 → 49,513 bytes |
| **`P1.3`'s landing table** | **001 lands · 002 lands · 003, 004, 005 REFUSED** — 002 flipped, the other three did not move |
| **a no-op landing** | byte-identical, sha `476e8e2e…` |
| **`record:land -- --verify`** | 51 of 51 strings |
| **`day.html`'s baked sha** | matches the edited source exactly |
| **P6** | 2 blocks, 16 claims, 0 orphaned |

**THE REMAINING BLOCKAGE: 18,907 characters in 15 blocks across three entries** —
003 two, 004 seven, 005 six.

## 5 · WHAT THE FIRST EDIT TO 002 WILL DO TO ITS FORMATTING

**MEASURED RATHER THAN PREDICTED.** A probe edit was landed into 002 against
the real tree, the shape diffed, and the tree restored.

```
002's source span:  1,624 -> 1,613 bytes
lines in the span:      34 -> 33
folded "+ ..." lines:   13 -> 12
```

**THE FIRST SIX LINES ARE CHARACTER-IDENTICAL** — `{ no: 2,`, `date:`, `title:`,
and the deck's own three folded lines. **It is not a reformat.** The entry was
written by `generate()` in the first place, so regenerating it reproduces it;
what moves is where the string folds fall, because the text got longer.

**SO MIKE MEETS THIS AS A KNOWN THING: his first save to 002 changes eleven
bytes of source shape and one line, and no character of his prose.**

## 6 · GATES

| gate | exit |
|---|---|
| `npm run lint` | **1 — the baseline**: 17 problems, **9 errors / 8 warnings**, zero new |
| `npm run build` | **0** |
| `npm run provenance:gate` | **0** |
| `npm run reveal:check` | **0** |
| `npm run instory:gate` | **0** |
| `npm run parity:gate` | **0** |
| `npm run arc:check` | **0** |
| `npm run ops:size` | **0** |
| `npm run docs:numbers:gate` | **0** |
| `npm run record:land -- --verify` | **0** — 51 of 51 |
| `npm run day:proof` | **0 — ALL 49 CHECKS PASSED**, 10 shown LOSING first |
| `npm run reveal:day` | *"Nothing to move. The tree and the Record agree."* |
| `npm run lap:clean` | nothing left in `public/` |

**`src/` CHANGED AND THE MUSEUM LAP WAS NOT DRIVEN, WHICH IS SAID RATHER THAN
GLOSSED.** What changed is comments in a data file; comments do not ship, the
parsed entries are deep-equal, `reveal:check` and the build are green, so no
visitor-facing string moved. There is nothing for the lap to measure that it did
not measure yesterday.

## 7 · NEXT

**Record 003** — two blocks, 3,333 characters, the heaviest standing share of
the four. Then 005, then 004.
