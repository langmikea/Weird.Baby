Fence every generator that can write hand-supplied album art

Mike's rulings, 2026-08-26, and they are PROVISIONAL in his own words:
"For now at least. I will not know until I start looking at the entire
site." A - the grey albums lose the W.B logo, fade accepted. B - the
Robots sleeve keeps its wording. B - WEIRD.BABY MUSIC stays as it is.
All three are OPEN register rows (A-a, A-b, A-c) carrying that sentence
verbatim, plus short-list rows 67 and 68. A later round must not read
them as settled; M26 is four days old and is the precedent for what
happens when an open row gets cited as doctrine.

NO COVER WAS REGENERATED, REDRAWN OR CHANGED. This packet is the
fences. Protection does not wait on a look.

The fence was on the one tool that writes nothing. make_unit_covers.py
has carried HAND_AUTHORED alone since 2026-08-10 and is retired; the
four tools that actually write those paths had none. covers:house would
have destroyed Mike's vinyl master, covers:robots his own NEW
Robots.png, covers:template both of Mikey's photographs. tools/
cover_fences.py is one set for all five, keyed on the BASENAME because
a governed picture has two addresses, checked over the whole intended
set before the first write, with --dry-run guarded on the same
reasoning and the refusal printed rather than tracebacked.

Four of the five generators now refuse every run, because every path
they own is hand-authored or superseded. That is the true state of
those tools and has been since 2026-08-10 without being enforced.
make_house_covers.py is the only one with live output, through --only,
which is also what docs/BACKLOG.md item 2 needed: the row now names
about-cover.png and carries the command that hits only that file.

Proved by tripping, against a copy. All five refuse and all exit 1; the
five legitimate covers rewrite byte-for-byte identical; all twelve
files in the copy unchanged after nine runs; 25 of 25 tool-by-name
refusals; four spellings of one file all refused. The first harness
read EXIT 0 on every refusal and was wrong - it piped python into tail
and read the pipeline's code, so a wrong number sat under correct and
visible refusal text.

Four sentences in the tree read as permission to overwrite his art, two
of them saying "a re-render cannot drift" about a file that is hand
drawn. robots.js twice, asset-table.json's what and qualityNote, and
ledger-declare.mjs's route.wb comment. provenance/assets.json was
already right and the others were conformed to it. verdict and bucket
are untouched on every row.

--verify has been red since 2026-08-09 and is in no gate: 419,442
differing pixels, the template's rule at rows 992-995 against the
shipped file's at 1061-1064. It is not drift, the comparand changed.
Repointing it is one line and is deliberately not done here, on Q-b's
argument. Row A-d.

Gates: lint 9/7 baseline, build green, provenance PASS, reveal:check,
parity:gate, instory:gate all PASS, and docs:numbers:gate PASS after it
caught two stale counts in BACKLOG.md - which also caught a status
value I had invented, OPEN - PROVISIONAL, that the register's own
counter cannot read. The round log is at docs/
MUSEUM_COVER_FENCES_LOG-20260826.md, the survey it acts on is at
C:\AI\_night-20260826\ALBUM_ART_SURVEY-20260826.md, and nothing was
deployed.
