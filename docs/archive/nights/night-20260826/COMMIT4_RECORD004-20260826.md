# COMMIT 4 — RECORD 004, AND `C-day2` CLOSES (2026-08-26)

**HEAD at start `c4a7b69`.** The last entry. **Every Record now accepts an edit
and Mike's editing pass is unblocked.**

**TWELVE FILES.** Seven carry the work; five are the register, the links it
orphaned, and the two pages the carried findings went to.

---

## 1 · ALL SEVEN CLASSIFIED

**FOUR ARE DUPLICATES OF ONE DOCUMENT. THREE FINDINGS IN A SINGLE BLOCK WERE
WRITTEN DOWN NOWHERE ELSE — and all three are the shape the last three rounds
kept turning up.**

| # | ch | what it is | verdict |
|---|---:|---|---|
| **0** | 134 | deck line 2 was `> /Robots data analysis` | **duplicate** — applied log § 004 |
| **1** | 1,005 | the password `355113`, Zu Chongzhi's ratio, the published folder names | **duplicate** — 09-PUBLISHED, 06-PORTAL |
| **2** | 957 | his rewritten listing; what `listingRows()` had to learn | **duplicate** — 09-PUBLISHED; the mechanism at `RecordEntry.jsx:251` |
| **3** | 1,024 | *"not meant to seen"* flagged; the canon-load-bearing sentence | **duplicate** — 09-PUBLISHED; 06-PORTAL records the rewrite taking the museum's half |
| **4** | 1,326 | `OTHER` and `ADDENDUM 01 - Bench Description` struck | **duplicate** — applied log § 004, nearly verbatim |
| **5** | 3,300 | the two attachment rows struck | **THREE FINDINGS CARRIED** |
| **6** | 3,846 | the docs field returns — QC_101 | **duplicate** — Rulings 18 & 19, the QC101 log, 06-PORTAL |

### THE THREE THAT WERE CARRIED, ALL FROM BLOCK 5

**1 · `Manual ref to Portal` IS `SCAN 11 - VID-LINK`, WHICH RECORD 003 HAD
ALREADY DELIVERED.** A plate-less row resolves to `held` and draws **not here
yet** — so Thursday's Record would have said the museum does not have a thing it
showed on Wednesday. **That is Ruling 10 failing in a direction its own examples
do not cover:** not a number overstating the wall, but a row DENYING something
already on it. **Carried to Ruling 10**, with the test that catches it — *before
a Record says it does not have a thing, ask whether an earlier Record has
already delivered it.*

**2 · `held` IS A FALL-THROUGH THAT CLAIMS A PROVENANCE.** Its declared meaning
is *its provenance IS recorded*; anything not `imaged` and not `quoted` lands
there. Both struck rows recorded none, so `attachmentsOf()` built a meta line
reading `not here yet` **and nothing else** — empty-and-unsourced, not
empty-and-honest, and the reader cannot tell the two apart. **Carried to
`src/lib/record-model.js`, beside `docState`.**

**3 · THE STRIKE COST NO PROSE, AND THAT WAS MEASURED.** Six search terms —
`attach`, `contents`, `enclos`, `appendix`, `below`, `see ` — **zero hits**; the
one backward reference lands on 003's delivered scan and still did after Mike
rewrote the sentence around it. **Carried to `09-PUBLISHED.md`, Record 004.**

## 2 · STALENESS — EVERY CLAIM RE-MEASURED, NONE STALE

| claim | measured |
|---|---|
| deck line 2 is now `> /Robots ZIP File Cracked` | **true** |
| the password `355113` is in the DETAILED REPORT | **true** |
| 002's manifest still names `PORTAL/CH3-STANDARD/` and `CH4-DETAIL/` | **true, both** |
| the listing carries `ROOT`, `PORTAL_2v16.CFG`, `QC_101.TIF` | **true** |
| *"not meant to seen"* is still his, as typed | **true** |
| no `OTHER` and no `ADDENDUM 01` in 004 | **true** — two sections only |
| one attachment: QC_101, source `ABEAL FORM QC-101`, 1 page, 1 plate | **true** |

**AND NOTHING RESTED ON A REASON THAT EXPIRES.** 004 is the entry that gained an
attachment after publication on *"we have had no visitors"* — a licence that ends
when an audience arrives. **Ruling 18 records the licence AND its expiry in those
words**, so deleting the comment loses neither half. The expiry is not this
round's to manage; it is recorded where it will be read.

**AND 004 HOLDS THE LAST `{pre}`** — the case 005's carried paragraph names.
Nothing in these seven touches the folder tree's content; block 2 describes the
listing and the mechanism, both of which have homes.

## 3 · P6 — A FIFTH PHRASE CAUGHT, AGAIN MY OWN PROSE

Eighteen candidates run through the present-AND-on-one-line check before the
manifest was written:

```
BAD  in the harder direction   MUSEUM_RULINGS-20260817.md   ← my own new Ruling 10 text
```

Replaced with `a row that DENIES something already`. **Five catches across four
rounds, three of them in prose I had just written.** The manifest is now
**17 blocks, 64 claims, 10 files**, green on all 64.

## 4 · PROOF

| | |
|---|---|
| **`draftEntries()` deep-equal** | **all five entries identical** to the committed draft |
| **comment characters** | in-span **11,592 → 0** |
| | preamble **24,204 → 27,365** — the audit block, outside every span |
| **file** | 46,933 → 38,382 bytes |
| **`P1.3`** | **001, 002, 003, 004, 005 — ALL FIVE LAND** |
| **a no-op landing** | byte-identical, sha `1c541a01…` |
| **P6** | 17 blocks, 64 claims, 0 orphaned |
| **`record:land -- --verify`** | 51 of 51 strings |

**IN-SPAN COMMENT CHARACTERS ACROSS THE FOUR COMMITS: 21,486 → 0.**

## 5 · ONE OF MY OWN CHECKS WENT RED, CORRECTLY

`P1.3` asserted `CAN_LAND.length < ENTRIES.length` — *"the wall is REPORTED
rather than discovered"*. **True while `C-day2` was open and red the moment the
last entry was emptied.** A check that outlives its subject.

Rewritten to assert the **finished** state, and **the table is what is kept**: an
entry that gains a comment block again falls out of it on the next run, which is
the only warning anybody gets.

## 6 · THE REGISTER, AND THE LINKS IT ORPHANED

**`C-day2` CLOSED and moved to `OPEN_ACTIONS_CLOSED.md`** (Doctrine 24 — a closed
row leaves his view). **`C-day3` raised** for the two lander defects.

**AND CLOSING IT ORPHANED SIX LINKS**, which is §9's prune hazard firing exactly
as written. All six repointed at the closed register: `MUSEUM_RULINGS`,
`09-PUBLISHED` (×2), `BELL-103`, `BACKLOG` (×2), `OPEN_ACTIONS_CLOSED`'s own
`C-day1` row. **Dead intra-doc links to `#c-day2`: 0.**

**One stale sentence went with them.** `day.mjs`'s header said `C-day2` *"is the
one thing still between this page and an ordinary writing day"* — true this
morning, false now. Corrected.

## 7 · WHAT IS LEFT

**`C-day3` — two defects in `record:land`'s refusal path, flagged and unfixed:**

1. **`emit-record-entries.mjs:747` compares body items by REFERENCE** —
   `ab[j] !== bb[j]`. Right for a string, **always false for a `{pre}` object**,
   so guard 8's diff reports 004's listing as changed on every stale-draft
   refusal with both sides byte-identical. **The exact pattern the round-trip
   repair fixed once** in `record-edit.mjs`; that repair taught one file and this
   line was missed. Fix: `textOf(ab[j]) !== textOf(bb[j])`.
2. **The same refusal says *"Open the editor (`npm run record`)"*** — the surface
   Mike ruled dies when Piece 4 ships. It should say `npm run day:serve`.

**Neither causes a wrong refusal.** The lander is a `[MIKE]` command behind eight
guards and deserves its own round and its own proof.

**Also still carried:** `day.html` is not reproducible because `SOURCE_STATE`
bakes the source's mtime and `day:proof` moves it (cosmetic — nothing compares
mtime); `provenance/assets.json`'s `scan-31-a` row names a master path that does
not exist; `STAGE_PREFIX` is imported by `day.mjs` and used by nothing.

## 8 · WHAT THE FOUR ENTRIES TAUGHT

**002 was duplicate throughout. 003 kept one sentence. 005 kept one paragraph.
004 kept three findings. Every single survivor was a fact about a MECHANISM'S
REASON TO EXIST** — why there are two renders, how many cases a shape has left,
what a fall-through state actually claims, what a strike cost.

Rulings had a rulings file. Strikes had round logs. His words had canon pages.
**What had no home was why a thing works the way it does** — and that is the one
class worth reading a comment block for before deleting it.

## 9 · GATES

| gate | exit |
|---|---|
| `npm run lint` | **1 — the baseline**: 17 problems, **9 errors / 8 warnings**, zero new |
| `npm run build` | **0** |
| `npm run provenance:gate` | **0** |
| `npm run reveal:check` | **0** |
| `npm run instory:gate` | **0** |
| `npm run parity:gate` | **0** |
| `npm run arc:check` | **0** |
| `npm run ops:size` | **0** |
| `npm run docs:numbers:gate` | **0** |
| `npm run record:land -- --verify` | **0** — 51 of 51 |
| `npm run day:proof` | **0 — ALL 49 CHECKS PASSED**, 10 shown LOSING first |
| `npm run reveal:day` | *"Nothing to move. The tree and the Record agree."* |
| `npm run lap:clean` | nothing left in `public/` |

**`src/` CHANGED IN TWO FILES AND THE MUSEUM LAP WAS NOT DRIVEN.** Both changes
are comments — one in a data file, one above `docState` in `record-model.js`.
Comments do not ship, the parsed entries are deep-equal, `reveal:check` and the
build are green, and no visitor-facing string moved.
