# COMMIT 3 — RECORD 005'S SIX BLOCKS (2026-08-26)

**HEAD at start `5bccaeb`.** One entry. **004 untouched — it is the last.**

**FOUR FILES:** `src/data/artists/robots-record.js`,
`src/routes/exhibit/RecordEntry.jsx`, `docs/dictation-20260807/moved-blocks.json`,
and the regenerated `day.html`.

---

## 1 · THE CLASSIFICATION, ALL SIX

**FIVE ARE DUPLICATES IN FULL. ONE PARAGRAPH IN THE SIXTH WAS WRITTEN DOWN
NOWHERE ELSE, AND IT IS THE SAME SHAPE AS 003's — a fact about why a mechanism
exists, which no register field holds.**

| # | ch | what it is | verdict |
|---|---:|---|---|
| **0** | 627 | 005 has its own headline: `PORTAL CONNECTION ONLINE`, why it is spent here | **duplicate** |
| **1** | 137 | the deck reordered — Portal leads, ZIP follows | **duplicate** |
| **2** | 129 | the EXECUTIVE SUMMARY swapped the same way | **duplicate** |
| **3** | 2,012 | the DETAILED REPORT cut to one line, then rewritten to three | **duplicate** |
| **4** | 209 | `OTHER` — the requisition is APPROVED here and raised nowhere | **duplicate** |
| **5** | 868 | `ADDENDUM 01 - The Four Settings` struck entire | **one paragraph carried** |

### WHERE THE FIVE ALREADY LIVE

**`docs/MUSEUM_RULINGS_APPLIED_LOG-20260820.md` § 005 CARRIES FOUR OF THEM, AND
BLOCKS 1 AND 2 IN A SINGLE LINE:**

> *"Deck and EXECUTIVE SUMMARY **reordered** — Portal first, ZIP second, same
> sentences. DETAILED REPORT replaced by its first line. **`ADDENDUM 01 - The
> Four Settings, as printed` struck entire**, both closing lines with it."*

It also carries the four-settings paragraph **verbatim**, the `{Mike to rewrite}`
braces and `wb-ops-braces`, and — at § 240 — *"The requisition lives in 005
only"*, which is block 4.

**`docs/canon/09-PUBLISHED.md`** carries the headline ruling with its reasoning
(*the week's payoff*, *the cheapest signal a Record has*), the two first
appearances filed as canon, and **both Doctrine 21 flags verbatim** — his
`Portal` against his `The Portal`, and `etc.` with the period doing double duty.
`docs/MUSEUM_RECORD_005_LOG-20260821.md` carries the same plus the rewrite that
closed the note.

### THE ONE THAT WAS CARRIED

Block 5's middle paragraph:

> *"IT TAKES THE `{ pre }` CASE RULED THE SAME DAY WITH IT, AND THE MECHANISM
> STAYS — Record 004's folder tree is the remaining case and the reason the
> shape exists."*

**NO REGISTER FIELD HOLDS A COUNT OF HOW MANY BODY ITEMS USE A SHAPE**, so
nothing else could have said it. Swept: `RecordEntry.jsx`, `Exhibit.css`, the
rulings and the canon pages carry the Listing's *why* — the Arial column spread
— but **not that it is down to one case.** The one near-miss,
`RecordEntry.jsx:732`'s *"the only entry carrying one"*, is about **013's
plate**, not the `{pre}`.

**AND IT IS THE READING UNDER WHICH SOMEBODY DELETES THE LISTING.** A mechanism
serving a single body item looks like dead weight to a cleanup round; the
paragraph is the argument that it is not. Carried into `RecordEntry.jsx` beside
the Listing's own argument, **with the count dated and marked re-measurable**
— it is a count, and counts move.

**THIS IS THE SECOND TIME IN THREE ENTRIES THAT THE ONLY UNIQUE THING WAS OF
THAT KIND.** 003's was *why there are two renders*; 005's is *how many cases the
shape has left*. Both are facts about a mechanism's reason to exist, and neither
has a field.

## 2 · STALENESS — ALL SIX RE-MEASURED, NONE STALE

| claim | measured against the tree |
|---|---|
| 005's title is `PORTAL CONNECTION ONLINE`, 24 characters | **true** |
| 002 and 004 still share `GENERAL STATUS UPDATE` | **true** — both 21 characters |
| the deck leads with the Portal line, ZIP second | **true** |
| the EXECUTIVE SUMMARY leads the same way | **true** |
| the DETAILED REPORT is the three lines he wrote | **true** — one body item, three lines, UNIX-6x and the COMM payload in it |
| `Portal` in his first line, `The Portal` in his third | **true** |
| no `RAISED` section anywhere in the volume | **true** — none |
| exactly one entry still carries a `{ pre }` | **true — 004** |

**All eight hold.** 002's closing sentence had gone stale; 003's had not; none
of 005's has.

## 3 · P6 — TWO MORE PHRASES CAUGHT BEFORE THE MANIFEST WAS WRITTEN

Fourteen candidates run through the present-AND-on-one-line check first:

```
BAD  the cheapest signal            09-PUBLISHED.md      ← crosses a wrap
BAD  the reason the shape exists    RecordEntry.jsx      ← crosses a wrap — my own new comment
```

**The second is the sharper one: it was a phrase in prose I had just written.**
Both replaced with wrap-safe ones (`week's payoff`, `only one left`). **Three
rounds, three catches, and every one of them before it could rot.**

The manifest is now **10 blocks, 44 claims, 7 files**, green on all 44. Its
breakage still names the block and the line.

## 4 · PROOF

| | |
|---|---|
| **`draftEntries()` deep-equal** | **all five entries identical** to the committed draft |
| **comment characters** | in-span **15,574 → 11,592**, removed **3,982** — the six blocks exactly |
| | preamble **21,802 → 24,204** — the audit block, outside every span |
| **file** | 48,610 → 46,933 bytes |
| **`P1.3`** | **001, 002, 003, 005 land · 004 REFUSED** — 005 flipped, 004 did not move |
| **`day.html`'s baked sha** | matches the edited source |
| **P6** | 10 blocks, 44 claims, 0 orphaned |
| **`record:land -- --verify`** | 51 of 51 strings |

**REMAINING: 11,592 characters in 7 blocks, all in Record 004** — 86% of that
entry's span, and the last of the four.

## 5 · GATES

| gate | exit |
|---|---|
| `npm run lint` | **1 — the baseline**: 17 problems, **9 errors / 8 warnings**, zero new |
| `npm run build` | **0** |
| `npm run provenance:gate` | **0** |
| `npm run reveal:check` | **0** |
| `npm run instory:gate` | **0** |
| `npm run parity:gate` | **0** |
| `npm run arc:check` | **0** |
| `npm run ops:size` | **0** |
| `npm run docs:numbers:gate` | **0** |
| `npm run record:land -- --verify` | **0** — 51 of 51 |
| `npm run day:proof` | **0 — ALL 49 CHECKS PASSED**, 10 shown LOSING first |
| `npm run reveal:day` | *"Nothing to move. The tree and the Record agree."* |
| `npm run lap:clean` | nothing left in `public/` |

**`src/` CHANGED IN TWO FILES AND THE MUSEUM LAP WAS NOT DRIVEN.** The Record
change is comments in a data file; the `RecordEntry.jsx` change is a **comment
inside a JSX expression container**, not a rendered element. Comments do not
ship, the parsed entries are deep-equal, `reveal:check` and the build are green,
and no visitor-facing string moved.

**The two lander defects flagged at commit 2 are carried unchanged** —
`emit-record-entries.mjs:747`'s reference comparison on a body item, and the
refusal pointing at the retired `npm run record`.

## 6 · NEXT — AND IT IS THE LAST

**Record 004** — seven blocks, 11,592 characters, **86% of its own span**. Every
class is in it: standing, entry-specific, code mechanism, canon, and the two
attachment rows struck and then reversed.
