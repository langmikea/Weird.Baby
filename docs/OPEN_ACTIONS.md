# OPEN ACTIONS — the standing register

**Built 2026-08-04 (v49/A1) on Mike's instruction: *"the round's spine — Mike has
no way to see what is already reported."* It is the ONE place he looks.**

Every open item across **both repos** — defects, backlog, ledgered ideas,
`[PAPA]` gaps, flagged judgment calls — harvested from `STATE.md` in the museum
and the robots repo, every round log, every audit doc and every ledger.

> ## THE MAINTENANCE RULE (doctrine, recorded 2026-08-04)
>
> **Every future round updates this file in the same commit it seals.** A round
> that closes an item flips its status here; a round that exposes one adds a row
> here. A finding that lives only in a round log is a finding nobody will read
> again — that is the failure this register exists to end. Round logs stay the
> narrative; **this is the ledger.**
>
> Canonical statement: `docs/canonical/OPERATIONS.md` §7, Doctrine 14.

**Status** — `OPEN` (nobody has started) · `IN PROGRESS` · `RULED-AWAITING-BUILD`
(decided, not built) · `DONE`.
**Owner** — `Mike` (his call, or his camera) · `Ops` (scoping, verification,
docs) · `Code` (a build).

**What this register is not:** it is not a priority order and it does not say
what to do next. Everything below is open; the sequencing is Mike's.

---

## 0. THE SHORT LIST — what is waiting on Mike, and exactly what he must supply

Pulled out of the tables below, not duplicated by them.

**[D8 2026-08-06] EVERY ROW NOW CARRIES WHAT HE HAS TO HAND OVER**, in one line,
because "waiting on Mike" was a status and not an instruction — a reader could see
that a row was his without seeing what closing it costs him. Most are one word;
several are one photograph; two are somebody else's consent and are not his to
give alone.

**NOTHING ON THIS LIST IS PENDING ON OPS.** Everything mechanical, unambiguous and
already ruled has been landed. Three items were looked at and deliberately not
built, each named with its reason at the foot of this section.

| # | The question | What he must supply | Row |
|---|---|---|---|
| 1 | **Three plates on the MGK-VIIIp wall show something their captions do not describe.** Two are compositing assets. | Which of the three come off the wall, or three replacement captions. The tombstone's "Nine" moves with the answer. | [M7](#m7) |
| 2 | **The one plate showing the machine saying something shows it backwards** — the whole photograph is flipped. | One word: flip it. It is one horizontal flip of the file and Ops can do it on his say-so. | [M2](#m2) |
| 3 | **The portrait introducing Hunter Root is a selfie in a vehicle, wearing another band's name.** | A different photograph, or a ruling that this one stays. Cropping does not fix the second half. | [M3](#m3) |
| 4 | ~~The Manual's plate is a render.~~ **CLOSED P2 by deletion.** | — | [M4](#m4) |
| 5 | **Two dead files carry 154 strings, three self-flagged unverified about a real musician.** | One word: delete, or keep and a sentence saying why. | [M5](#m5) |
| 6 | **Fifteen files in `public/` are referenced by nothing** — one a corrupt PNG. | One word per file, or one word for the lot: keep or delete. Most are real photographs the museum owns. | [M9](#m9) |
| 7 | **44 shipped assets have never had your verdict.** | An hour with `npm run assets:checklist -- --room <slug>` and a pass or a fail per asset. | [M22](#m22) |
| 8 | **One wall says its nine plates are "before power" and one of them is captioned as the firmware running.** | Which sentence moves: the tombstone's claim, or the lit plate's place on that wall. | [M25](#m25) |
| 9 | ~~The MGK-NIAC has no photograph of itself whole.~~ **CLOSED v56/R4.** | — | [M30](#m30) |
| 10 | ~~The FAQ face ships with no picture.~~ **CLOSED v56/R3.** | — | [M29](#m29) |
| 11 | **The reveal ledger carries no schedule.** | Answers to any of the 49 cards `npm run reveal:cards` prints — one blank each, any order, at his pace. | [M32](#m32) |
| 12 | **The Portal's drum has six engraved feeds and five will not arm.** | Which feed arms first, and what is on it. Each is a boolean and a feed. | [M33](#m33) |
| 13 | **Twelve games run in the twin and none runs on the machine.** | A ruling on whether the firmware ever gets a GAMES handler. Nothing promises it today. | [M34](#m34) |
| 14 | **Is there a fifth reveal class, and is it `SEALED`?** | One word — yes and call it SEALED, or no. A `no` is equally usable: it means HELD covers both rows. | [M35](#m35) |
| 15a | **Eleven held photographs are waiting for one word, and you can now see them in one click.** | Keep or delete — one word per photograph or one for the lot; and better, which of them should come out from behind the door. **`C:\AI\Projects\_review\HELD-PHOTOGRAPHS-20260806\index.html`** | [C-a](#c-a) · [C-c](#c-c) |
| 15b | **Both machines' Technical Specifications faces lost their real-build register and nothing replaced it.** | The units' own particulars — what an ABEAL spec sheet for these engines says. It is drafted in the robots repo; porting it is authoring, which is yours. | [N-g](#n-g) · [N-h](#n-h) |
| 15c | **The in-story manual's specification section is sourced from the real Arduino firmware, and in one row the firmware overrules the manual.** | One ruling: which source wins when the manual and the firmware disagree about a 1965 machine. | [N-i](#n-i) |
| 15d | **`/hr`'s player bar has no room for the track name on a phone.** | A phone layout call for the bar and the variant chips. It is a design pass, not a fix. | [N-j](#n-j) |
| 15e | **The twin prints 76 operator slots on its own glass and only the stage door stands between them and a reader.** | Whether the twin's placeholders get the NOT-UX treatment, or the door is accepted as the whole answer. | [N-k](#n-k) |
| 15f | **The week-one outline opens on an entry that is not about the transmissions.** The Record holds exactly one entry, day 1 says so and is right &mdash; but it is 013, and it is about a sealed bag, a USB-C adapter and a unit on charge. | One of three: day 1 opens with the entry that is there, or 013 is re-dated, or day 1 adds one. It is one sentence and it decides the week. | [W-1](#w-1) |
| 15g | **The week-one outline is written and none of it is yours.** Ops structured what you said aloud on 2026-08-02; every gold slot on the page is empty. | Your own headline for the week and for each of the five days &mdash; dictated into the gold slots. A weighting too, if you want one; the topics are lists and nothing ranks them. | [K-b](#k-b) |
| 15 | **The giving research asks fourteen questions.** | Answers, one at a time. Not a museum item and it has no row below. | [GIVING_RESEARCH-20260805.md §9](GIVING_RESEARCH-20260805.md#9-the-decisions-he-faces) |
| 16 | ~~One exhibit ships seventeen Facebook iframes to a visitor who has clicked nothing.~~ **RULED R6 — A, DISCLOSE. Not deferred, not click-to-load. [C34](#c34) closes with it.** | — | [M37](#m37) |
| 17 | **Two real households are named in your invoice supply and neither reached the page.** | One word per household: have they agreed to be named? It is their consent, not his alone. | [M38](#m38) |
| 18 | **You marked two links and gave no addresses.** | Two URLs, or a ruling that the slots come out until the channels exist. | [M39](#m39) |
| 19 | **The Mike-and-Mo-have-enough half of "Why give all the money away?" is still to come.** | The words. The slot is marked and nothing plausible was written into it. | [M40](#m40) |
| 20 | **Does the billionaires answer belong in the Foundation at all?** | A room. `/booth` is the obvious alternative and it is not a recommendation. | [M41](#m41) |
| 21 | **The manual is a supply line: which page does the story reach for first?** | A Record entry that names a page. It comes out of his voice sessions, not out of a build. | [M44](#m44) |
| 22 | ~~The manual's still says TEXT NOT SUPPLIED.~~ **CLOSED P1–P5.** | — | [M45](#m45) |
| 23 | **How often should a SHORT come out?** | A number and an owner. The museum can count what it has not shown; it cannot see a cut. | [M46](#m46) |
| 24 | ~~Three NIAC/VIIIp menu differences are holdings gaps.~~ **REVERSED, P1 — parity is absolute.** | — | [M47](#m47) |
| 25 | **The Manual's face now has no picture.** | Either a photograph of a page actually written, or a ruling that it stays imageless until P2 exists. | [M48](#m48) |
| 26 | **The Foundation's name differs between the board and the door**, and D7 moved the door. | One name for the bar, one for the board, or a ruling that the two may differ. | [M26](#m26) |
| 27 | **The bill's four panels lost their only written "press me".** | A press cue, or a ruling that a photograph with a name under it reads as pressable on a phone. | [M51](#m51) |
| 28 | ~~The poster's blurb says "none of them is ours" above the panel that says the museum holds his records.~~ **CLOSED R4 — the panel's sentence is deleted, so the blurb has nothing left to contradict.** | — | [M52](#m52) |
| 29 | ~~A `[PAPA]` note's second sentence prints on the poster.~~ **CLOSED D3a — and the same defect was found on a second face.** | — | [M53](#m53) |
| 30 | ~~Mikey Mike's verified domain is wired in the shop and not on his card.~~ **CLOSED P11.** | — | [M54](#m54) |
| 31 | **BEHIND THE CURTAIN is ledgered and cannot be built first.** | The first thing he actually wants to say in it. The room comes after the entry, by his own rule. | [M55](#m55) |
| 32 | **WHEN THINGS GET SLOW is ledgered and the folder was not opened.** | His selection of frames, and whether they are captioned. The folder names two other people. | [M56](#m56) |
| 33 | ~~The manual cannot go online.~~ **RULED D6 — it stays OFFLINE until real pages exist. The viewer stays built.** | — | [M61](#m61) |
| 34 | ~~The Foundation's albums and tracklist are not built.~~ **DONE D7 — option A, all three mechanisms intact.** | — | [M62](#m62) |
| 35 | **"Follow us on social media" is on the glass and this museum holds no handle for any platform.** | The accounts. A handle is not something Ops may invent. | [M60](#m60) |
| 36 | **What IS Weird.Baby Robots?** | The answer. Seed: *"Purveyors of the Weird.Baby"*. The slot prints nothing until it arrives. | [M57](#m57) |
| 37 | **The robots FAQ lost the only line in the wing that said which door is which.** | A ruling: one door is enough for a first read, or the other three get named. | [M58](#m58) |
| 38 | **THE NAME is deleted and the two-names reconciliation is stated nowhere.** | Whether any of the three struck facts comes back, and on which face. | [M59](#m59) |
| 39 | **The WB logo is missing a thin black outline.** | The art, when it matters. One file fixes all three surfaces. | [M63](#m63) |
| 40 | **"About the Songs" for Weird.Baby Music — wanted someday, not today.** | The songs' own labels. It is not scaffolded, on the no-coming-soon credo. | [M64](#m64) |
| 41 | **The robots wing opens on THE RECORD, not the FAQ.** | One line: the order stands, or the FAQ leads and the Record sits under it. | [M65](#m65) |
| 42 | ~~The robots front desk answers the contact question with the bare address.~~ **RULED R1 — folded in. Both rooms read `CONTACT`, no follow-on sentences.** | — | [M66](#m66) |
| 43 | ~~The two machines' FAQ blurbs differ in wording, not only in noun.~~ **RULED R2 — BOTH DELETED. Nothing left to diverge.** | — | [M67](#m67) |
| 44 | **The museum's share card and its Twitter card are two different descriptions of one museum — and the third, the search description, names two exhibits where the lobby board lists three.** | One word: which description is the museum's. **All three are printed verbatim in the R6 report and in [M68](#m68) below**, because you could not read them from a phone. | [M68](#m68) |
| 45 | ~~Hunter Root's holdings sentence has two wordings.~~ **RULED R4 — DELETED ENTIRELY, everywhere. Neither wording survives.** | — | [M69](#m69) |
| 46 | ~~The Foundation's FAQ is an entry list now; its accordion is gone.~~ **RULED R7 — the booth's format, in every wing. Four faces conformed.** | — | [M70](#m70) |
| 47 | **Forty-seven of Hunter Root's ninety-three tracks are reachable through nothing** — and since H1 only you and Ops can see it. Downgraded, not closed. | A ruling on what `/hr` should LOOK like without them, whenever you next work from it. No visitor meets this any more. | [M71](#m71) |
| 48 | **Hunter Root's artist card now has no biography at all.** R4 struck both its paragraphs because both were the holdings announcement. | Three paragraphs of him, the way the other three have. Ops will not draft them. One sourced life fact is in hand and named in the row. | [M72](#m72) |
| 49 | **The image question is still unruled, and H1 moved it rather than answering it.** All 107 vault image URLs are now behind the password; `/wal` still serves two of his images publicly. | One word: the same rule for images, or images are different and why. It now bites on `/wal`, not on `/hr`. | [M73](#m73) · [M82](#m82) |
| 50 | **`/hr/archive`'s header still announces the holding** — *"7 records, plus an EP and a set of singles and rarities · 93 tracks on file"*, verified again this round. Now behind the password. | One word: it goes too, or a catalogue page may say what it is a catalogue of. | [M75](#m75) |
| 51 | **The R7 research is delivered and it puts one number to you: the knob is worth thirteen tracks, not ninety-three.** | Whether to build Bandcamp embeds at all, given that. The map is `docs/AD_FREE_PLAYBACK_RESEARCH-20260806.md`. | [M76](#m76) |
| 52 | **`HR_KEY` HAS NO DEFAULT AND THE WING IS SHUT UNTIL YOU SET IT — INCLUDING TO YOU.** H1 fails closed on purpose. | One host-side command, once per environment: `npx wrangler secret put HR_KEY`. Until then `/admin` says so in as many words. | [M85](#m85) |
| 53 | **His post text is copied into the museum's own data — 49 artifact titles and descriptions, verbatim.** An embed licence covers the iframe, not a second copy of the words. Never put to you before. | A ruling: the text goes the way the audio went, or it stays and why. | [M80](#m80) |
| 54 | **All 97 vault facts about him are PUBLIC on `/wal` and `/hr` going private changed none of it.** | A ruling on whether the whole vault fact set belongs in a room that shows two of his songs. | [M81](#m81) |
| 55 | **The share cards describe an open museum and the front door says it is not open.** Three redrafted descriptions are printed verbatim in the round log. | Your correction to the three drafts, and one line on whether a share card should say the place is not open yet. | [M83](#m83) · [M68](#m68) |
| 56 | **The Gift Shop title bar is in the Weird.Baby face as a test.** Reverting is one block deleted. | Keep or revert. | [M83](#m83) |
| 57 | **A `/CB` wing, and an easter egg in the screen of the unit inside album art.** Both ledgered, neither built. | Whether either is wanted, and when. | [M77](#m77) · [M78](#m78) |

| 58 | **Nine of the archive's fourteen frame "dates" are not dates, and the five that are say MAR 2021 — years before the machine arrived.** | Which moves: the dates, the story, or the field. Both halves are the N7 flag. | [M93](#m93) |
| 59 | **The manual's six attested sections are struck, and one carried a `[PAPA]` slot of yours** — MARGINS, *"which hands, and what they wrote"*. | Whether any of it comes back, and where. It is in git and in this register and nowhere else. | [M89](#m89) |
| 60 | **The viewer has no image descriptions now and the wall still has them.** | One line: the wall's captions go too, or a description is worth printing where a visitor is choosing and not once they are looking. | [M86](#m86) |
| 61 | **The archive tombstones are gone and took two claims with them** — that the withholding is authored, and that the photographs are ours. | Whether either sentence comes back, and where. | [M90](#m90) |
| 62 | **The nameplate has a hand-stamped serial field and no serial.** | The number. Ops may not mint one. | [M94](#m94) |
| 63 | **The two Technical Specifications faces are now in two different forms** — the mainframe's is a one sheet, the portable's is not. No gate can see this. | One line: the portable conforms, or the two are allowed to differ. | [M92](#m92) |
| 64 | **The wing has two image archives and the groupings cannot cross them** — your own example named *"all NIAC, all VIIIp, everything"*. | Whether the two archives become one room. | [M91](#m91) |
| 65 | **MGK-NIAC's Technical Specifications face now has no picture** — the third in this wing, deliberately. | A ruling, or nothing. Three is a pattern rather than three exceptions. | [M88](#m88) |
| 66 | **The builder's own power-draw line has no home** — a real quotation, and a spec sheet has no row for one. | Whether it comes back, and on which face. | [M87](#m87) |

**WHAT OPS LOOKED AT AND DELIBERATELY DID NOT BUILD [D8]**, because "already
ruled" is a test and these three fail it:

- **[C34](#c34)** click-to-load embeds — it is Ops' recommendation, and
  [M37](#m37) asks him for a letter. Building B while A and C are on the table is
  Ops answering the question it put to him.
- **[C36](#c36)** `/wal`'s title bar at 390px — the row offers three ways out
  (shorten, wrap, drop the brand) and picking one is a design call.
- **C17** the four pre-existing lint errors — its own row says each needs semantic
  review rather than a mechanical fix.

**[v54/F8] Rows 17–20 arrived in one round, and that is what it looks like when
the operator answers a room's questions himself:** the gaps stop being things Ops
noticed and start being things he named. None of them blocks anything — every one
is held honestly on the page today.

---

## 1. RULINGS AND CALLS — Mike's

<a id="m1"></a>

| ID | What it is | Where it came from | Status | Owner | Raised |
|---|---|---|---|---|---|
| **M1** | ~~The unit count, 31½ against 31.4.~~ **CLOSED BY DELETION.** Mike struck the 31½ logo — *"it speaks out loud about something not meant to be spoken out loud"* — and the sweep he asked for found the same count in three places, all three now gone: the tally card, its caption, and the FAQ's *"How many are there?"*. **Nothing on this museum's glass prints a count**, so there is nothing here to reconcile; the robots repo still says 31.4 in its own words draft and that is that repo's to keep. Those three rows were also the museum's ENTIRE `INVENTION` holding pen, so the provenance register's invention count is now 0 and its ceiling is ratcheted to 0 with it. | R1 · v48; ruled A5 · v51 | DONE | Mike | 2026-08-04 |
| <a id="m2"></a>**M2** | **`front_screen.png` is mirror-reversed** — captioned *"The front glass, lit"*, the one plate showing the machine saying something. **New this round: the WHOLE PHOTOGRAPH is flipped**, not the screen — the lettering behind the unit reads backwards too, so the fix is a horizontal flip of the file rather than a re-shoot. | R2 · v48; whole-image diagnosis v49/A7 | OPEN | Mike | 2026-08-04 |
| <a id="m3"></a>**M3** | **The WAL portrait of Hunter Root.** The shirt reads CHET VINCENT AND THE MUSIC INDUSTRY across the chest. **New this round: it is also a phone selfie taken in a vehicle** — steering wheel, mirror and a parking lot are in frame — doing the job of an artist's introduction portrait. Cropping the shirt out does not fix the second half. | R3 · v48; framing v49/A7 | OPEN | Mike | 2026-08-04 |
| <a id="m4"></a>**M4** | ~~The Manual's plate is a render.~~ **CLOSED P2 BY DELETION — there is no plate left to be a render.** Mike ruled [M45](#m45) too thin, the still and its caption are struck, and the face carries no image. **This row is closed by the ruling and not by a photograph**, so B8's requirement is untouched and unmet: the day a page is printed and photographed, the plate that goes back is a photograph or it does not go back. Original text: | R4 · v48; file swapped G1 · v56; closed P2 | DONE | Mike | 2026-08-04 |
| <a id="m4-orig"></a>**M4 (orig)** | **The Manual's plate is a render.** B8's own ruling for that face says the printed page is the source and the *photograph of the print* is the plate. **[v56/G1] THE FILE CHANGED AND THE ROW SURVIVES IT.** It was clean digital type on white, 21 KB — and it was a page of the RETIRED manual, copied before the robots repo rebuilt the document, which nothing noticed because a copied file cannot see its source deleted upstream. It is now page 1 of the live STRUCTURE ISSUE: a photocopy of a TYPED page, 98 KB, with paper texture, ribbon variation and a copier ink field. **That is a long step toward what B8 asks for and it is still a render**, so this row is unchanged in substance. Ops' quality read moved placeholder → weak. | R4 · v48; file swapped G1 · v56 | OPEN | Mike | 2026-08-04 |
| <a id="m5"></a>**M5** | **Two unreachable files still carry words.** `hr_facts.js` (124 strings, three self-flagged UNVERIFIED about a real musician) and `hr_journal_prompts.js` (30). One `import` from the glass. Delete, or keep and declare why. | R5 · v48 | OPEN | Mike | 2026-08-04 |
| <a id="m6"></a>**M6** | ~~THE MORGUE or IMAGE ARCHIVE.~~ **CLOSED — Mike struck THE MORGUE.** Both walls now read IMAGE ARCHIVE and the subtitle carries the unit alone. The individual photographs are still called plates. | Mike's A3 brief · v49; ruled N1 · v50 | DONE | Mike | 2026-08-04 |
| <a id="m7"></a>**M7** | **Three of the nine MGK-VIIIp plates do not show what their captions say.** `MGK-TWIN_MONITOR_SCREEN_BEZEL.png` (*"The bezel around the glass"*) is a CRT frame graphic with a blank white screen area. `monitor_base.png` (*"The base it stands on"*) is the family-shot composite dropped inside that frame — no base in it. `unit_new_base.png` (*"The unit on its new base"*) is the same pair on white with both screens masked out — a compositing intermediate. The wall's tombstone says *"Nine, all held by this museum"*. | v49/A7 | OPEN | Mike | 2026-08-04 |
| <a id="m8"></a>**M8** | **`mgk-viii-cover.jpg` cannot be straightened by rotation.** Measured: the machine's own aperture is −1.75° on its top edge and +2.48° on its bottom; the grille bars measure −2.69° to +5.40° depending on sampling height. It is keystoned, not tilted, and it has a mains adapter and a wooden floor in frame. It wants a photograph. | Mike's A8 brief · v49 | OPEN | Mike | 2026-08-04 |
| <a id="m9"></a>**M9** | **[P2 2026-08-05] FOURTEEN BECOMES FIFTEEN.** `/robots/manual/structure-issue-p1.png` (98 KB) is orphaned by the ruling that struck the Manual's plate — **shown yesterday, struck today, and not deleted**, which is the same call `parts_drawer.jpg` and the seven robot plates got. It is page 1 of a document the robots repo still builds, so the museum holding a copy of it costs nothing and deleting it would be the museum throwing away a print of a live document. **It is also the second file this year to reach the shelf by SUBTRACTION rather than by being built ahead — see [M49](#m49), which is about the count rather than about the file.** Previously: | v49/A5 · v50/N1 · v51/A1 · v56/R4 · P2 | OPEN | Mike | 2026-08-04 |
| <a id="m9-v56"></a>**M9 (v56)** | **[v56/R4] FIVE BECOMES FOURTEEN, AND NINE OF THEM ARE THIS ROUND'S OWN DOING — DELIBERATELY.** Mike's canon made the MGK-NIAC album show the MAINFRAME only, so seven robot plates came off the wall (`head_lens`, `head_oblique`, `chest_grille`, `limbs_lower`, `torso_unfinished`, `feet_plinth`, `slot_mockup`), `bench_power` went with them (the robot's feet are in it) and `matrix_lit` was orphaned by the Technical Specifications swap. **NONE was deleted — they are real photographs this museum owns, which is the call N1 made on `parts_drawer.jpg`** — and every one of them is the held egg's material (`egg.niac.operator`), so "delete" is the wrong verb for most of this list now. `npm run surfacing` counts them every run. **The original five are unchanged and still want a ruling:** | v49/A5 · v50/N1 · v51/A1 · v56/R4 | OPEN | Mike | 2026-08-04 |
| <a id="m9-k1"></a>**M9 (K1, 2026-08-07)** | **NINE OF THE FOURTEEN ARE GONE AND THE OTHER FIVE STILL WANT A RULING.** Mike ruled the held photographs killed &mdash; *"none are very good, and if that view is ever needed it gets reshot"* &mdash; so `head_lens`, `head_oblique`, `chest_grille`, `limbs_lower`, `torso_unfinished`, `feet_plinth`, `slot_mockup`, `bench_power` and `matrix_lit` are off disk, and `parts_drawer` with them. **The row above said "delete is the wrong verb for most of this list now" and the reason it gave was that they were the held egg's material; the egg outlives them** and its material is three plates upstream, so the reason held and the answer went the other way. `npm run surfacing` counts nine fewer idle files from this round. **The original five that predate v56 are untouched and are still the open question.** | v49/A5 &middot; v56/R4 &middot; K1 | OPEN | Mike | 2026-08-07 |
| <a id="m9-orig"></a>**M9 (orig)** | **FIVE files sat in `public/` referenced by nothing** — `/images/wb-merch/hunter-root.png` (1.7 MB, orphaned when F7b removed his banner), `/images/wb/vol1_cover_v0.png` (superseded; also a JPEG named `.png`), `/WeirdBaby_PhotoID_backup.png` (**truncated — a corrupt PNG at a public URL**), `/robots/reference/mgk-viii/parts_drawer.jpg` (317 KB, orphaned when N1 removed The Parts), and `/robots/art/mgk-viii-cover.jpg` (272 KB, orphaned when A1 replaced the cover — the same call parts_drawer got: a real photograph this museum owns is not deleted by a cover change, and it is not re-homed onto a wall whose tombstone counts its plates). Delete, or keep and say why. | v49/A5 · v50/N1 · v51/A1 | OPEN | Mike | 2026-08-04 |
| **M10** | **`/foundation` Q7's narrowing.** The one edit E1 made to copy Mike had already approved. One string to revert if he wants the flat *No* back. **[v54/F6] THE QUESTION IS NOW CALLED "Can I donate?"** — Mike's own title, replacing *"Can I send money?"*. The narrowed ANSWER is untouched, so this row is unchanged in substance; it is amended only so that a search for the old title does not come up empty. | v43/E5 | OPEN | Mike | 2026-08-03 |
| **M11** | **The four unpublished invoice figures** — domain, robot supplies, manufacturing, the extras. The invoice is built to take them. **[v54/F5] A SECOND, DIFFERENT GAP NOW SITS ON THE SAME OBJECT AND MUST NOT BE CONFLATED WITH THIS ONE:** this row is about AMOUNTS Mike has not published; [M38](#m38) is about NAMES two other households have not consented to. Answering either does nothing for the other. | v43/E1 | OPEN | Mike | 2026-08-03 |
| **M12** | **`THE_CHARTER.md` is still DRAFT v0.3, "not published."** | v43/E5 | OPEN | Mike | 2026-08-03 |
| <a id="m13"></a>**M13** | **The Billionaire's Credo is unwritten.** Q10 carried a `[PAPA]` and the scrubber dropped that one sentence. **[v54/F3] THE WHOLE ANSWER IS NOW HELD, NOT JUST ITS LAST LINE, AND MIKE'S RULING CAME IN THREE PARTS: the raw ideas are good, the VOICE is wrong, and the PLACEMENT is wrong.** So every sentence carries the marker, `visitorProse` empties the answer, `kept` drops the question, and **`/foundation` no longer asks about billionaires at all.** It is the first answer in the building to take that path to the end. **The three ideas are preserved verbatim in the data** for him to write from — the *Illionaires* coinage, *being remembered for the size of the pile is a strange thing to spend a life earning*, and *we would rather make more pie than fight over the last piece*. One Ops sentence was NOT carried forward: *"Why keep grabbing for more?"* had no idea of his under it, and putting Ops rhetoric into the pile he writes from would defeat the point of holding it. **The PLACEMENT half is a separate question and is [M41](#m41)** — he asked whether this belongs in this room at all, and nothing was moved, because choosing the room would be Ops answering the question he asked. | v43/E5; held whole F3 · v54 | OPEN | Mike | 2026-08-03 |
| **M14** | **`--wb-gold-mute` and `--wb-gold-lo` are half a stop apart.** F0's finding; a palette call, and palette calls are Mike's. | v40/F0 · v43/E5 | OPEN | Mike | 2026-08-03 |
| **M15** | ~~`/robots` Welcome still says "three cartons".~~ **CLOSED BY DELETION, which is the only way Doctrine 12 allows.** The Welcome blurb now reads "a delivery of them arrived on a dock" — no count, and no substitute count, because nobody supplied one. | v47 exposure #9; closed N4 · v50 | DONE | Ops | 2026-08-04 |
| **M16** | **Two songs have almost no song-tier facts.** Jesse Welles' *"That Can't Be Right"* has **zero** (all three it had were our research narrative); *"There's A Hole"* has one, and the file's own note says one fact reads as a stuck scroller. Nothing was invented to fill either. | v46/C4 | OPEN | Mike | 2026-08-04 |
| **M17** | **The `/hr` journal is dead machinery.** The tab is unreachable and `submitEntry` writes to component state only — an entry would not survive a reload. Build it or retire it. | v46/C4 | OPEN | Mike | 2026-08-04 |
| **M18** | **Twenty-seven questions on Record 013**, one gap apiece, none blocking. Highest value is D1, *why wasn't it hacked*. | `docs/RECORD_013_QUESTIONS-20260804.md` · v47/H6 | OPEN | Mike | 2026-08-04 |
| **M19** | **What a record NUMBER means.** Are the numbers the full 436-record numbering, or a numbering of this volume? A volume where some entries have numbers and some do not is the visible symptom of the undecided question, and it is on the page. | v45/R5 #1 | OPEN | Mike | 2026-08-04 |
| **M20** | **The three Printful sticker variants still carry the old low-res master.** The in-shop image is fine; this is the print file. Blocks nothing. | STATE gift-shop pipeline | OPEN | Mike | 2026-06-23 |
| <a id="m21"></a>**M21** | **Thirty-six `[PAPA]` markers sit in content files awaiting his words** — robots 17, Worth A Listen 6, `/foundation` 7, `/booth` 6. Net +1 this round: DOC CONTROL added two (the manual's wording, and whether an original is ever published) and Contact's deleted REACH row took one away. All are scrubbed at the render seam, so none is visible; each is a sentence he has not written. **[v52/Q3] Net −1: the MGK-NIAC name face's OPEN entry carried `[PAPA] — whether the carousel reads MGK-NIAC or MGK-VIII`, and Mike answered it, so the marker is gone rather than moved. That reads 35. A RE-DERIVATION THIS ROUND DISAGREES AND IS RECORDED RATHER THAN RECONCILED:** a comment-stripping parser counting `[PAPA]` inside string literals reads robots 13 · WAL 6 · foundation 6 · booth 5 = **30**, not 35. The gap is five and it is a COUNTING METHOD, not a finding — nobody should act on either number until it is agreed whether the unit is a marker, a literal or a `papa:` field. **[v53/B3] Net −1 on whichever number is right: the booth's gift-shop answer was deleted whole and it carried *"what the house does with anything its own shelf earns"*. It was deliberately NOT re-homed** — that is a question about the Foundation rather than about the shop, and the Foundation's own page is where it belongs if it is to be asked at all. Booth 6 → 5 by the first method, 5 → 4 by the second. **[v54/F1–F6] `/foundation` MOVES BY +4 ON A GROSS COUNT AND THE GROSS COUNT IS THE MISLEADING ONE, which is this row's standing point made again.** Added: three markers on the held billionaires answer ([M13](#m13)), one on the Mike-and-Mo slot ([M40](#m40)), one on the consent gate ([M38](#m38)), two on the unsupplied link addresses ([M39](#m39)). Removed: one, the old billionaires credo marker, absorbed when the whole answer was marked. **But the three billionaires markers are ONE gap, not three** — they are one answer, held once, split across sentences only because the scrubber cuts by sentence. A count of markers and a count of things Mike owes are different numbers, and this round is the clearest demonstration yet: gross +4, real gaps +4 (consent · two addresses · the Mike-and-Mo content), and the billionaires gap was already counted here before. | standing, `src/lib/visitor-prose.js` | OPEN | Mike | ongoing |
| <a id="m22"></a>**M22** | **44 shipped assets carry no verdict from Mike** (43 last round; the two new machine covers joined the shipped set and the old MGK-VIII cover left it). `npm run assets:checklist -- --room <slug>` prints the inspection list for one room; `npm run assets:gate` fails while any presented asset is unpassed. | v49/A5+A6 | OPEN | Mike | 2026-08-04 |
| <a id="m23"></a>**M23** | ~~Two pairs of live alternatives waiting to be halved.~~ **CLOSED, AND HE STRUCK MORE THAN HALF.** The booth: **BOTH** candidates deleted, and no replacement visual — *"THE TITLE IS THE GRAB"* — along with the `?hook=` switch and ~180 lines of stylesheet. The guest book: the scrolling version wins, the static list and `?book=` are gone, and its behaviour changed with the ruling — **three rows visible, a stepped page-advance with a bounce and a 5s rest** instead of a continuous drift. | Mike's N5 + N6 brief · v50; ruled M23a/M23b · v51 | DONE | Mike | 2026-08-04 |
| <a id="m24"></a>**M24** | **"The Firmware" was renamed on BOTH faces that carried it**, not one — MGK-VIII's (a board, an envelope, four output chains) and MGK-VIIIp's artifact slot (two source trees). Leaving either would have kept the retired name on the glass, so both now read TECHNICAL SPECIFICATIONS and their subtitles do the telling-apart. If only one was meant, it is one string back. | v50/N1 | OPEN | Mike | 2026-08-04 |
| <a id="m25"></a>**M25** | ~~One wall says its nine plates are “before power” and one of them is captioned as the firmware running.~~ **CLOSED N4 BY SUBTRACTION.** The claim lived in the archive tombstone’s `State` row, and Mike struck both walls’ tombstones — so there is no sentence left for the lit plate to contradict. Nothing about the plate or its caption moved. What the strike DID cost is [M90](#m90). | v50/N8 | DONE | Mike | 2026-08-04 |
| <a id="m26"></a>**M26** | **The Foundation's name now differs between the board and the door** — the directory reads *Weird.Baby Foundation* (N1 dropped the article there) and the room's own title bar and heading still read *The Foundation* / *The Weird.Baby Foundation*. That is defensible (a board is a list, a door carries the full name) and it was NOT extended beyond the instruction, which named the directory. One word either way. | v50/N1 | OPEN | Mike | 2026-08-04 |
| <a id="m27"></a>**M27** | **The DONATED BY column is built and every cell is an absence, which is the true state and not a gap.** Three rows read *Nobody yet* (nothing has come in through a channel that is NOT BUILT), two read *Not a gift* (the shop and the music are earned, not given). **ANONYMOUS is declared, styled and documented and appears nowhere** — writing it into a cell to show the column works would have invented a contribution. The first real gift is one field. | Mike's N7 brief · v50 | OPEN | Mike | 2026-08-04 |
| <a id="m28"></a>**M28** | **The reveal arc is populated on 6 assets of 253 and unset on the rest, by instruction.** `arrived` on the four MGK-VIIIp plates the wall's own tombstone attests as received; `online` on the two that show the machine running. Every other row is `null`, which the table's legend states is UNSET and not a stage. Extending it is a judgement per asset, and the MGK-VIII set needs M25 answered first. | Mike's N8 ruling · v50 | OPEN | Mike + Ops | 2026-08-04 |
| <a id="m29"></a>**M29** | ~~THE `/robots` FAQ FACE SHIPS WITH NO PICTURE.~~ **CLOSED v56/R3, BY INHERITANCE RATHER THAN BY A NEW OBJECT.** The FAQ absorbed WELCOME, DOC CONTROL and CONTACT, which made it the wing's LANDING — `/robots` opens on this album and this track. The exception Mike granted (*a page whose own words are the hook needs no image*) was granted to a face in the MIDDLE of a wing; F1's argument is the one that applies to a landing, and it was that the wing whose whole subject is a physical object should not introduce itself in prose. **The face inherited Welcome's family shot along with Welcome's job.** Nothing new was sourced, and no object was invented for the slot — which is the half of A5's ruling that still binds. Original text: | Mike's A5 ruling · v51; closed R3 · v56 | DONE | Mike | 2026-08-04 |
| <a id="m29-orig"></a>**M29 (orig)** | **THE `/robots` FAQ FACE SHIPPED WITH NO PICTURE.** Striking the 31½ card took the face's only object, and nothing replaced it — inventing a second typographic card to fill the slot is the thing the ruling was against. That is a live conflict with the **VISUAL HOOK LAW**, resolved the way Mike resolved the same conflict on `/booth` in the same round: **a page whose own words are the hook needs no image**, and the exception is recorded rather than argued away. If he wants an object back there it is a data block, and every word on it would have to be one the face already says. | Mike's A5 ruling · v51 | OPEN | Mike | 2026-08-04 |
| <a id="m30"></a>**M30** | ~~THE MGK-NIAC'S COVER WEARS A DETAIL, AND IT IS THE SAME PLATE THE FACE BELOW IT SHOWS.~~ **CLOSED v56/R4, and by a ruling that made the question different rather than by picking one of the two ways out.** Mike's canon: **NIAC is the MAINFRAME** and the camera-body robot is an easter egg that stays out of frame. The cover badge was `head_lens.jpg` — the robot's face — and so was the still on THE NAME one press below. The cover is now `core_helical.jpg` (the coil through the cage bars) and the face is `column_lit.jpg` (the lit column and the cabinet's edge): **different plates, and neither is the robot.** Confirmed on the glass at desktop. The museum still holds no photograph of the CABINET whole — that is `phys.niac.whole` and P4, whose subject this ruling also changed. Original text: | Mike's A1 brief · v51; closed R4 · v56 | DONE | Mike | 2026-08-04 |
| <a id="m30-orig"></a>**M30 (orig)** | **THE MGK-NIAC'S COVER WORE A DETAIL, AND IT WAS THE SAME PLATE THE FACE BELOW IT SHOWED.** *(Renamed with the album at v52/Q3; the cover file is now `mgk-niac-cover.png` and the lettering in it reads MGK-NIAC. Nothing else about this row changed.)* A1 asked for *an image of the unit* in the badge; this museum holds no photograph of the MGK-NIAC whole — its own archive is titled DETAILS ONLY and its tombstone says the frame is withheld — so the badge is `head_lens.jpg`, the machine's face. That is also the still on the MGK-NIAC face one press away, so the deck and the panel carry the same photograph at once. `head_oblique.jpg` was rendered as the alternative and is the weaker cover by a distance. Two ways out and both are Mike's: **a photograph of the unit whole** (which is P4), or **swapping the FACE's still** to break the repeat. Ops did neither. **[v53/B1] CONFIRMED ON THE GLASS, which it had never been** — this row was written off the asset table and off the data. At 390px the cover badge and the face's still are in one screenshot without scrolling, and they are visibly the same photograph. | Mike's A1 brief · v51 | OPEN | Mike | 2026-08-04 |
| <a id="m31"></a>**M31** | **PAPA EMAIL MANAGEMENT.** Ops triages everything arriving at `papa@weird.baby` and surfaces only what actually needs Mike. Backlogged on his instruction, not built, and no mechanism for it exists today. Low urgency. | Mike's A6 brief · v51 | OPEN | Mike + Ops | 2026-08-04 |
| <a id="m32"></a>**M32** | **THE REVEAL LEDGER HAS NO SCHEDULE IN IT.** `reveal/ledger.json` holds 152 rows across both repos with build state, reachability, dependencies and the reveal arc — and **every single row reads `when: null`**, because nobody has supplied a story day or week and Doctrine 12 forbids inventing one. The consequence is that the audit's *"where do assets cluster on one day"* section is structurally empty, and that a HELD row with no date cannot be told apart from a HELD row that is simply forgotten. **[v55/R2] THE INSTRUMENT IS NOW BUILT, so what is left is one word from you and nothing else.** `npm run reveal:cards` prints **49 cards** — one per HELD row with no date, each carrying what it is, where it stands, what has to happen first, and one blank. Answer any, in any order, at your pace; an answer becomes a `when:` value. Narrow it with `--spendable` (the 12 on the back shelf — built and reachable by nobody, the only rows that cost nothing to release), `--cls egg`, `--record`. **The deck is 49 and not 143 deliberately:** 93 undated rows are already REVEALED, and asking what day something came out when nobody wrote the day down would be an invitation to invent one. **Nothing about this blocks anything** — it is a pile of cards, not a gate. | v52/R2+R6; instrument built v55/R2 | OPEN | Mike | 2026-08-05 |
| <a id="m33"></a>**M33** | **FIVE OF THE PORTAL'S SIX DRUM POSITIONS ARE PROMISES.** IDLING/UPD, BOOT PLAYBK, OFF · 1ST BOOT, LAST STATE and TEST BENCH are engraved where a visitor reads them and none of them arms; only STANDARD does. That is the single largest concentration of unbuilt-but-visible surface in the museum — and, read the other way, **the single largest concentration of reveal levers**, because each one is a boolean and a feed. It is deliberate (built as held, Mike's metered-revelation instrument), so this is a row rather than a defect: five arrivals are sitting there whenever the arc wants them. | v52/R4 | OPEN | Mike | 2026-08-05 |
| <a id="m34"></a>**M34** | **TWELVE GAMES RUN IN THE TWIN AND NONE OF THEM RUNS ON THE UNIT.** The dispatcher marks it: the firmware has no GAMES handler, so clicking a game on the actual machine does nothing today. Nothing promises otherwise anywhere, so nothing is broken — it is simply the largest thing the simulator does that the object it simulates does not. | v52/R1 | OPEN | Mike | 2026-08-05 |
| <a id="m35"></a>**M35** | **THE FIFTH REVEAL CLASS — ONE QUESTION, PUT AS ONE QUESTION [v55/R5].** It has been carried forward three rounds as a description; it is set down here in Doctrine 12's own three-part format instead, because a thing described is a thing nobody has to answer.<br>**WHAT IS KNOWN.** You have four reveal classes and you named them: ANNOUNCED (the house says it exists) · HINTED (noticed before it is explained) · DISCOVERED (reachable with no announcement) · HELD (built and deliberately not reachable **yet**). You also gave the case that does not fit: *"Maybe the nickels are hidden inside of the thing so they're not even photographed. There's no photograph of the nickels, so nobody knows anybody's getting nickels."*<br>**WHAT IS MISSING.** One word: **is there a fifth class, and is it called `SEALED`?** — for a thing that is canon, physically present, deliberately unphotographed, and **not scheduled for reveal at all.**<br>**WHY IT MATTERS.** HELD means *not yet*; a sealed thing may be reachable **never**, and the table cannot currently tell those apart. Two rows are already sitting in the gap. `phys.nickels` carries a dependency that literally reads *"a reveal class that does not exist yet"* — the most honest row the table can hold until you answer. And **[v55/R2] building the cue cards found the second one, which is not an artifact at all:** `route.hr` is HELD PERMANENTLY BY YOUR OWN RULING — reachable, never listed — so its card asks what day it comes out, and you have already said *never*. **The same missing word covers both**, which is the argument that this is a class and not a one-off note about nickels. A `no` is equally usable: it means HELD is the answer for both and the cards stop asking. Ops adopted nothing — naming a class is yours. | robots ASSET_REVEAL_CHECKLIST · carried v52 · put as one question v55/R5 | OPEN | Mike | 2026-08-04 |
| <a id="m37"></a>**M37** | **THE MUSEUM TOUCHES THREE THIRD PARTIES, TWO OF THEM UNPROMPTED, AND ONLY ONE HAD EVER BEEN DISCLOSED.** Measured in a browser with `performance.getEntriesByType('resource')`, nine-second settle, **nothing clicked**: `/`, `/booth`, `/shop`, `/foundation` and `/hr/archive` request Google Fonts and nothing else; `/robots`, `/wal` and `/wb` add **`www.youtube.com` ×3** (`Exhibit.jsx:333` injects `iframe_api` on load, not on play); `/hr` adds that **and `www.facebook.com` ×16 across seventeen iframes**. The booth's answer now names all three and says the last two arrive before a visitor presses anything — that is the honest floor, not the decision. **THE DECISION IS YOURS AND IT IS A REAL ONE:** the credo on the front of that same page reads *"No tickets, no tiers, no ads"*, and one room of this museum hands seventeen Facebook plugin frames to somebody who only opened it. Three ways out and none is Ops': leave it and let the answer carry it, put the embeds behind a click ([C34](#c34)), or take them off the wing. **[F8 2026-08-05] THE THREE WAYS OUT ARE NOW SET DOWN AS A RULING TO MAKE, not as a paragraph to re-read, because that is the difference between a finding and a decision — and this row had been reported twice without ever being put as a choice.** Pick one:<br>**A · DISCLOSE AS-IS.** Change no code. The booth's answer already names Google, YouTube and Facebook and says the last two arrive unprompted; that answer becomes the whole of the remedy. Costs nothing, builds nothing, and leaves a visitor who reads no FAQ handing sixteen requests to Facebook for opening one room.<br>**B · CLICK-TO-LOAD.** A still and a play button in place of each embed; the real player and the real plugin frame mount on the first press. Outbound traffic for a reader falls to zero, a player costs one extra click, and the wing looks the same. This is [C34](#c34) and it does NOT need this ruling first — it is strictly less outbound traffic for the same behaviour, and it is Ops' recommendation of the three.<br>**C · DROP THE EMBEDS.** Take the seventeen Facebook cards off `/hr` entirely. The only option that makes the credo *"No tickets, no tiers, no ads"* true without a footnote, and the only one that loses content — seventeen cards of a real artist's own posts. It would also close [C12](#c12) by removing its subject.<br>**They are not exclusive:** B can ship now and A or C can follow. What Ops will not do is choose — the credo on that page is yours, and so is the artist's material. **[R6 2026-08-06] RULED — A. MIKE: "the FAQ states plainly that YouTube (and any other embed host) is contacted when those pages load. Disclosed, not deferred, not click-to-load."** The booth's answer becomes the whole of the remedy, which makes its accuracy the whole of the remedy — so it was RE-MEASURED on the built bundle this round and **the Facebook half was wrong again, in the museum's favour.** `/hr` on arrival, nine seconds, nothing clicked and nothing scrolled: Google Fonts, `www.youtube.com` ×3, **and Facebook ZERO.** The sixteen plugin frames carry `loading="lazy"` (`HrExhibitFlow.jsx:2240`) and sit below the fold, so they arrive when a visitor SCROLLS — still nothing pressed, but not "the same way" as the player, which is what the answer said. Scrolled to the bottom: `www.facebook.com` ×16, 17 iframes, exactly as v53 recorded (v53's reading was taken with the deck already open, which is why it read as arrival). **The clause now says WHEN.** Three of its four versions have understated what leaves the building. **[C34](#c34) closes with this row** — B was ruled against. | v53/B1+B2; options set F8 · v54; ruled R6 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-05 |
| <a id="m38"></a>**M38** | **TWO REAL HOUSEHOLDS ARE NAMED IN THE INVOICE'S SUPPLY AND NONE OF THEM REACHED THE PAGE — THIS IS A CONSENT QUESTION, AND IT IS THE ONE ITEM IN THIS ROUND OPS REFUSED TO SHIP.** Mike supplied two zero-cost invoice lines with real people against real costs: one household against the DOMAIN, another against the STICKERS. They are the strongest rows that object could carry — a named person carrying a named cost is the room's whole argument drawn instead of asserted, and the invoice was built for exactly this. **They are held because publishing a private individual's name on a public page is not the museum's call to make on their behalf, and this house has already taken a name off the glass once for less** (CS 2026-08-04 removed the operator's OWN name from a sentence that did not need it — these are other people's, which is a higher bar, not a lower one). **What shipped instead:** the RULE (*money given for a cost is spent promptly on that cost, or it goes back; nothing is pooled and there is no slush fund*) and the MECHANISM (*a cost carried by somebody other than the keeper takes its own line here, in the name of whoever carried it*) — both of which are true with no name attached. **What is needed is one word per household: have they agreed to be named?** A yes is two line items. A no is honest and the mechanism sentence already covers it. The names are in the round log's question list and deliberately nowhere in `src/`. | Mike's F5 supply · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m39"></a>**M39** | **TWO LINKS ARE MARKED ON THE GLASS AND NEITHER HAS AN ADDRESS.** Mike marked a link on *"Can I donate?"* and one on *"Can I send you something?"* and supplied neither URL. Built as instructed and **left visibly unresolved**: each renders as the door's NAME in the register face with the ledger's own `NOT BUILT` stamp beside it, and **no `<a>` element at all** — nothing to click, because a dead anchor is the dead control Doctrine 11's corollary says to remove. The state is read from `reveal/ledger.json` (`channel.qr`, `channel.supplies`), **so on the day either channel is built the stamp flips without this file being edited** — which is the point of wiring them rather than hard-coding a chip. **What is needed: the two addresses, or a ruling that the slots come out until the channels exist.** | Mike's F6 supply · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m40"></a>**M40** | **THE ANSWER TO "Why give all the money away?" IS MISSING THE HALF MIKE SAID IS COMING.** He ruled that the answer carries Motto #3 — *Take all you need, not grab all you can*, which now LEADS it — and that it also needs the content about him and his household already having enough, **which he flagged as still to come.** The slot is a marker, not a paraphrase: nothing plausible was written into it, because a stand-in on the one page whose entire subject is honesty about money is the exact failure Doctrine 12 exists for. **One note on the motto, so it is not re-litigated:** he calls it Motto #3 and the WORDS ship while the NUMBER does not — this museum has never published a numbered list of mottos, so printing *"motto number three"* would put an internal designation on the glass, which is Doctrine 11's own example of a failing line. If a numbered set of mottos is ever published, that decision changes. | Mike's F1 supply · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m41"></a>**M41** | **DOES THE BILLIONAIRES ANSWER BELONG IN THE FOUNDATION AT ALL?** Mike questioned the FIT, separately from questioning the voice ([M13](#m13) holds the voice half). Nothing was moved: the room is his to choose and Ops picking one would be Ops answering the question. **The case for keeping it here:** it was one of the three subjects he named when he commissioned this room. **The case against:** every other question on the page is about what THIS house does with money, and this one is about what other people do with theirs — it is the only outward-facing opinion in a room of inward-facing commitments. **`/booth` is the obvious alternative address** and it is not a recommendation. Today it is held in place and prints nothing, so there is no cost to leaving this open. | Mike's F3 ruling · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m42"></a>**M42** | **DELETING "Who pays you?" TOOK THE ONLY LIST OF THE FOUR GIFTS OF SERVICE OFF THE PAGE, AND THE COST IS RECORDED RATHER THAN ABSORBED.** Mike ruled the question deleted [F2] and it is deleted. What went with it: the charter's clause 3 enumeration — **the design, the code, the shelf, the legal work** — and the only sentence stating the job pays *nothing: not salary, not expenses, not credit taken.* **The SUBSTANCE survives on the page twice** (the invoice IS costs billed at nothing; the posture signed under it says who carries them and for how long); the ENUMERATION does not. Worth flagging because this exact clause has now moved twice: v41/C3 removed the legal-work item, v42/R3 restored it on a careful argument, and v54/F2 removed the whole list. **That argument is not being re-run — his ruling stands.** The row exists so that if the four gifts should stand somewhere else on this page, it is a decision he makes rather than a thing that quietly vanished. | Mike's F2 ruling · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m43"></a>**M43** | **THE ROUND'S ONE JUDGEMENT CALL ON MIKE'S OWN COPY: "Where does it actually go?" IS A POINTER THAT KEPT ONE SENTENCE.** His instruction was *the ledger, not prose*. Honoured literally it would have read *"Read the ledger"* and pointed at the wrong half — **the register on that page is the INCOMING side**, five channels money and goods arrive through, and it does not contain where anything goes. So the charter's own beneficiary clause stayed as the pointer's destination in words (*"To established charitable organisations, and to people caught doing conspicuous good"*), the rest of the paragraph went, and the answer now names the ledger and declares **the outgoing half NOT BUILT** in the room's own grammar. **What was deleted is not lost:** the application-and-grant-form argument went, and he re-supplied that himself as the Pro-Tip on the next question, in better words. **The alternative is one string:** a bare *"Read the ledger."* if he wants the instruction taken at face value and the charter clause gone with it. | Mike's F1 instruction · v54 | OPEN | Mike | 2026-08-05 |
| <a id="m44"></a>**M44** | **THE MANUAL'S PAGES ARE NOW A SUPPLY LINE AND NOTHING HAS BEEN CALLED FOR YET.** Your ruling: the manual **arrived in pieces**, so the museum needs only the specific pages the story reaches for — printed, marked, photographed, one at a time, as Record entries call for them. [v55/R3] built the vessel for exactly that: `doc.manual.page.NN` carries its own production stage (**needed · printed · photographed · placed**) and names the Record entry that asked for it, so one page can be finished on its own. **`doc.manual.plates` — the set-of-24 row — cannot express this and stays as it is:** it reads NOT_BUILT whether twenty-three pages are done or none, and it is the row that carries the one promise the museum makes on the glass. **Nothing is populated, on instruction:** the story has not asked for a page, and choosing which page it reaches for would be Ops writing the story. **This starts moving when a Record entry names a page** — which is your voice sessions, not a build. One page is then one line in `reveal/ledger-declare.mjs`. It does not block P2 either: P2 is still *print and photograph*, and this only says which page and for whom. | Mike's R3 ruling · v55 | OPEN | Mike | 2026-08-05 |
| <a id="m45"></a>**M45** | ~~THE MANUAL'S STILL SAYS "TEXT NOT SUPPLIED".~~ **CLOSED P2. MIKE RULED IT TOO THIN, AND HIS REASON IS SHARPER THAN THE QUESTION WAS: it was the museum admitting it had not written the manual, WEARING A FICTION AS COVER — Doctrine 11 hiding inside a picture.** The rule he gave with it is wider than this face: **either the plate shows a page actually written, or there is NO PLATE until one exists. Empty beats a placeholder in fiction's clothing.** Not a better caption — and the tell is that the caption was already doing the one piece of work the swap needed; a caption that has to argue a picture out of its own lettering is a caption losing an argument with a photograph. `still` and `stillCaption` are struck from `face.viiip.manual`. **`reel.plates` is unchanged and still `[]`** — the head plate was never a frame in the reader, so P2 in the art register is untouched. Closes [M4](#m4) with it. Exposes [M48](#m48); `structure-issue-p1.png` joins [M9](#m9)'s shelf (14 → 15). Original text: | v56/G1; ruled P2 · 2026-08-05 | DONE | Mike | 2026-08-05 |
| <a id="m45-orig"></a>**M45 (orig)** | **THE MANUAL'S STILL NOW SAYS "TEXT NOT SUPPLIED", AND THAT IS A ONE-WORD RULING.** *Known:* G1 replaced the Manual's plate, because the old one was a page of the **retired** 24-page render and the museum was showing the title page of a document that no longer exists. The new plate is page 1 of the live structure issue and it is better art — a photocopy of a typed page rather than clean digital type. *Missing:* whether its own printed words survive a visitor reading them. The page reads **STRUCTURE ISSUE · STRUCTURE AND ARRANGEMENT ONLY · TEXT NOT SUPPLIED.** *Why it matters:* in the fiction that is an early issue circulated for arrangement before the text was set — a fifth copy-state beside your four (preliminary, final, marked-up, APPROVED), and the FAQ's own came-in-pieces answer already says the manual was assembled out of copies caught at different stages. **Read the other way it is the museum admitting it has not written the manual, which is Doctrine 11 printed INSIDE A PICTURE where no string sweep can ever see it.** The caption is written to hold the first reading (*"An early issue of the working copy — arrangement first, before the text was set."*) and at thumbnail size the line is not legible; it is legible in the reader. **A `no` costs one string and the face goes back to having no picture.** | v56/G1 | OPEN | Mike | 2026-08-05 |
| <a id="m46"></a>**M46** | **HOW OFTEN SHOULD A SHORT COME OUT?** *Known:* you asked for a rhythm of surfacing **plus periodic SHORTS**, and R7 built the first half — `npm run surfacing` counts what is held and never shown, by wing, and logs it so the number becomes a trend. *Missing:* the shorts cadence. *Why it matters:* **the museum cannot count a short and the report refuses to print a zero for one**, because a short is a CUT — the storyboard doctrine, the obfuscation law and the cutting all live in the robots repo, and a zero here would read as *none were made* when it means *this tool cannot see*. The material exists: two signature cuts are storyboarded and roughed (both 1 s shorter as of v56/R4). What is missing is a number and an owner. | Mike's R7 gap · v56 | OPEN | Mike | 2026-08-05 |
| <a id="m47"></a>**M47** | ~~THE PARITY CHECK SAYS THE SAME THING THREE WAYS.~~ **RULED P1, AND THE RULING IS THAT THE MENU IS RIGHT.** Mike: the three divergences are **HOLDINGS GAPS, NOT DESIGN** — the mainframe has no manual, no portal feed and no questions of its own — and forcing parity would print rows leading nowhere, which THE STUB LAW forbids on exactly those grounds. **So: parity is the default, a divergence is a yellow flag, and a flag justified in writing by a holdings gap is RESOLVED rather than overridden. NIAC's menu shows what NIAC has.** Built with it, because R2 shipped four written reasons and no way to tell *answered* from *excused*: a justification now carries a `kind` — **HOLDINGS** and **PROPERTY** resolve, **DESIGN** stays a standing flag and nothing uses it yet (it is declared so the first divergence that IS a preference has somewhere honest to go). **And "it clears itself" is a mechanism now:** a holdings gap must name the LEDGER ROW that would exist and be built if the museum held the material, and `parity:gate` reads `reveal/ledger.json` and **faults the day it does** — `doc.manual.niac` (not a row at all), `portal.feed.niac.1`/`.2` (rows, NOT_BUILT), `face.niac.faq`. Four new refusals, each broken on purpose and each fired. **Today: 2 shared · 4 divergences — 4 resolved · 0 standing flags · 0 undeclared.** The gaps still close from the same direction — more NIAC material — which is what [R5](#m32) says is coming. Original text: | v56/R2; ruled P1 · 2026-08-05 | DONE | Mike | 2026-08-05 |
| <a id="m47-orig"></a>**M47 (orig)** | **THE PARITY CHECK SAYS THE SAME THING THREE WAYS: THE MUSEUM KNOWS FAR LESS ABOUT NIAC THAN ABOUT THE VIIIp.** `npm run parity` reports 2 shared menu items and **4 declared divergences**, and three of the four are HOLDINGS GAPS rather than design: the mainframe has **no manual** to catalogue, **no portal feed** (its two channels are engraved and dark), and **no questions of its own** that THE NAME does not already answer. The fourth — THE NAME — is permanent and is a property of the objects. **Nothing here is a defect and nothing blocks:** each has a written reason and the check fails only on an UNDECLARED difference. It is a row because the three gaps close from the same direction — more NIAC material — and because that is exactly what your own canon says is coming ([R5](#m32): *NIAC is the next chapter*). | v56/R2 | OPEN | Mike | 2026-08-05 |
| <a id="m48"></a>**M48** | **THE MANUAL'S FACE NOW SHIPS WITH NO PICTURE, WHICH IS [M29](#m29)'s CONFLICT ON A DIFFERENT FACE.** *Known:* [P2] struck the plate on Mike's ruling, and the face is better for it — the prose runs the full width and the three honest absences (*PLATES none on file* · **REEL EMPTY** · *No pages on file*) are the whole of it. *Missing:* whether it stays imageless, and if not, what goes there. *Why it matters:* it is a live conflict with the standing **VISUAL HOOK LAW**, and this house has now resolved that conflict three different ways — `/booth` by ruling that a page whose own words are the hook needs no image (M23a), the robots FAQ by **INHERITING** a real photograph along with a job (M29), and this face by deletion. **M29's route is the bar here and the reason is in the ruling that created this row:** the one thing that must not happen is an object invented for the slot, because a rendering standing in for a photograph is exactly what [M4](#m4) was. **The honest answer may be that nothing goes there until P2's photographs exist** — the face's whole subject is a document the museum cannot yet show, and an image of anything else would be a picture about something the page is not about. | v56/G1 → ruled P2 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="m49"></a>**M49** | **THE SURFACING REPORT CANNOT TELL BUILDING-AHEAD FROM SUBTRACTION, AND THE TRIPWIRE HAS NOW FIRED ON THE WRONG ONE — TWICE.** *Known:* R7's proposed cadence is *the shelf must not grow two packets running* — one round of building ahead is stock, two is a habit. Idle files went **5 → 14** at v56 and **14 → 15** at P2, so by the number the shelf has grown two packets running. *Missing:* whether the count should separate the two ways a file lands on it. *Why it matters:* **BOTH growths are DELETIONS.** v56/R4 took seven robot plates off a wall under your mainframe canon; P2 took a plate off a face under your TEXT NOT SUPPLIED ruling. **A file that was shown and then struck lands on exactly the same shelf as a file built and never shown, and only the second is "building ahead"** — the tripwire was designed for over-production and is being tripped by the opposite of it. Nothing was adjusted: **a number adjusted to say what a round wants is not a measurement**, which is the log's own standing line. Ops' reading is that the report wants a second column (idle-because-struck vs idle-because-unspent) and that a round which strikes something should not read as a round which hoarded something. **It blocks nothing** — it is a reading, not a defect, and the files themselves are all real photographs the museum owns. | P5 · 2026-08-05 | OPEN | Ops | 2026-08-05 |
| <a id="m50"></a>**M50** | ~~THE FIGURES ON HUNTER ROOT'S OWN CARD ARE THE ONES THE POSTER LINE WAS JUST REWRITTEN TO STOP PRINTING.~~ **DONE (W1, MIKE'S READING PASS — ROUND ONE).** All six sites are rebuilt from a count re-derived this round rather than carried forward: **9 album containers · 93 track rows · 78 of them carrying a `song:` slug, and the 15 without are exactly Run With The Hunt.** The tombstone reads *93 tracks* and *Seven, plus an EP and a set of singles and rarities*; the label, the metrics note, the records note and the scroller follow it; *"whose catalogue the museum holds itself"* is gone, because a reader takes it for all of it and the wing's own records note says sixteen sit on his Bandcamp. **TWO YEARS WERE WRONG TOO AND THE MECHANISM IS NOW NAMED IN THE FILE:** the records board was printing `era-buckets.json` **bucket START years** as release dates — an era is a span a record opens, not the day it came out. Crooked Home 2024 → **2025** (vault `MV-HR-20260707-053`, *"released October 17, 2025 via Tolok"*, and his own Bandcamp album page read 2026-08-05) and Arkansas 2022 → **2023** (his own Bandcamp, *"released June 30, 2023"*). Both corrected years also agree with `hunter-root.js`, which had disagreed with this board for as long as both existed. **AND ONE OF THIS ROW'S OWN CLAIMS WAS WRONG, WHICH IS THE FINDING WORTH KEEPING.** M50 recorded that *"Half of Crooked Home is about his brother"* is a quantity **the vault does not carry**. **IT DOES:** `MV-HR-20260707-068`, Hunter's own words to Americana Highways in 2025 — *"Half the songs ended up being about my brother… '94' was almost the album title. It's the heart of it all."* — tagged to `crooked_home`. It stays on all three faces that print it, and **a search that missed it is not an absence**: a register row is a claim like any other and this one was checked against the vault before it was acted on. | W2 · THE WAL POSTER EDIT; closed W1 · READING PASS ROUND ONE | **DONE** | Mike | 2026-08-05 |
| <a id="m51"></a>**M51** | **THE BILL'S FOUR PANELS HAVE NO WRITTEN AFFORDANCE, AND THAT REVERSES P6 ON THIS ONE OBJECT.** *Known:* W1(c) struck the per-act **"Open the room"** chip along with the bill's foot. The chip existed because of **P6's ruling** — *what is drawn must be readable, and what is not readable must be written*, so the panel's affordance was a WORD rather than a chevron. *Missing:* a press cue, or a ruling that the panel is self-evidently a door. *Why it matters:* verified live — all four are still `<button>`, still lift on hover, still take a pointer cursor, and pressing one still opens that artist's room. **On a coarse pointer there is now no cue at all**, because hover is the only remaining signal. It blocks nothing and the doors work; it is a judgement about whether a photograph with a name under it reads as pressable on a phone. **Deliberately not replaced with copy nobody asked for.** | W1c · THE WAL POSTER EDIT | OPEN | Mike | 2026-08-05 |
| <a id="m52"></a>**M52** | **THE POSTER'S BLURB SAYS THE OPPOSITE OF ITS FOURTH PANEL.** *Known:* the blurb reads *"Four of them. Two songs each, all playable. Every one of them is somebody's favourite record and none of them is ours."* Four inches below it, Hunter Root's panel reads *"The one artist in this room whose records the museum holds in its own vault."* *Missing:* which of the two sentences moves. *Why it matters:* the contradiction predates this round — the old Hunter Root line opened with the word *"Ours"* under the same blurb — but W1 struck the ~110 words that sat between them, so the two now read almost together. **The blurb was not on the deletion list and was not rewritten**, per the surgical instruction. Ops' reading is that *"none of them is ours"* means *we did not make these records*, which is true of all four and is worth keeping; if so it is a wording problem, not a fact problem. **[R4 2026-08-06] CLOSED, AND NOT BY CHOOSING BETWEEN THE TWO SENTENCES.** Mike struck the holdings announcement everywhere, so the panel this blurb contradicted no longer says it — Hunter Root's bill line now reads *"Seven records, an EP and a set of singles and rarities. Half of Crooked Home is about his brother Nick…"*, which is a claim about HIM and does not touch what the museum owns. *"None of them is ours"* stands unchallenged and unedited, which was Ops' reading of which sentence was worth keeping. | W3 · THE WAL POSTER EDIT; closed R4 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-05 |
| <a id="m53"></a>**M53** | ~~A `[PAPA]` MARKER'S SECOND SENTENCE RENDERS, AND IT IS THE CLAIM W1(c) WAS STRUCK FOR.~~ *Known:* the face's `papa` field reads *"[PAPA] — the promotional copy, the running order and the four house accents. **Every claim about an artist here is already on that artist's own card, sourced there.**"* The scrubber drops only the sentence carrying the marker, so the bolded half **prints** — confirmed on the built bundle, `.vp-face-papa`, at desktop and at 386px. **The scrubber is not the defect and must not be "fixed":** `src/lib/visitor-prose.js` cuts by SENTENCE on purpose and its header says why — a field routinely carries real provenance and then the marker, and dropping the field would take the sourcing down with it. *Missing:* whether it stays. *Why it matters:* it is the museum vouching for its own sourcing practice on the glass, which is exactly what you struck from the bill's foot one line above it, and it is now **the last line on the poster**. It is also a note to the operator printed to visitors. ~~**Not deleted here.**~~ **CLOSED D3a (CLEAR THE DECK), AND THE RULE QUESTION WAS THE RIGHT QUESTION.** Mike struck the sentence; the field is now ONE marked sentence and the scrubber is untouched. **A sweep of every `[PAPA]` string in `src/` against `visitorProse` found the same defect live on a second face** — the MGK-NIAC plates wall was printing *"The uncropped originals are all on file and any of them can be published from this file alone."*, a maintainer talking to a maintainer, under a comment claiming the whole string was one marked sentence. Both fixed the same way. **THE RULE, SINCE IT IS NOW TWICE: a `papa` string is not a comment. Write the whole note inside ONE marked sentence, or expect the rest on the glass.** | W3 · THE WAL POSTER EDIT | **DONE** | Mike | 2026-08-05 |
| <a id="m54"></a>**M54** | **MIKEY MIKE HAS A CLEAN DOMAIN, AND IT IS WIRED IN ONE ROOM AND NOT THE OTHER.** *Known:* you supplied **weekendatmikeys.com** and it was opened and read before anything was wired — it is his ("The Family Ranch"; *STEP INTO THE WORLD OF MIKEY MIKE*; Nashville TN), its Instagram is **@findmikeymike**, which is the handle of the very channel L1 verified by oEmbed from his own upload, it is named as his in the music press independently of itself, and **it carries no injected link farm** — the thing that shut the other door. **R-a's refusal of findmikeymike.com is untouched and still correct**; this is a different address. *Missing:* whether his ARTIST CARD moves too. *Why it matters:* his gift-shop tile now goes to his own site while his card still opens on the YouTube channel and his `siteNote` still explains why his other domain is linked nowhere. That is not wrong — the channel is genuinely where his work lands first — but the museum now holds a verified front door for him and prints it in exactly one room. **Deliberately not widened:** you named the shop tile. | S2 · READING PASS ROUND ONE | OPEN | Mike | 2026-08-05 |
| <a id="m55"></a>**M55** | **BEHIND THE CURTAIN — ledgered, and its own constraint is the hard part.** *Known:* your F1, verbatim — a place where you tell the real truth about things, why you really build robots or anything else that is not in character; **NOT a clearing house, a room used only when it is needed.** Ledgered as `room.curtain`, NOT_BUILT · HELD · `shown: false`. *Missing:* where it lives, what it looks like, and **the first thing you actually want to say in it.** *Why it matters:* a room with a standing appetite for content fills itself with filler, which this museum has paid for once already in the Record's ten invented entries — **so the first entry has to exist before the room does**, and building the vessel first would invert exactly the order you specified. One collision named ahead of time: Doctrine 11 bars a line whose subject is the making of this museum, and *"why I really build robots"* is a line about the man and the machines. Thin enough to be worth knowing before the writing starts, not after. | F1 · READING PASS ROUND ONE | OPEN | Mike | 2026-08-05 |
| <a id="m56"></a>**M56** | **WHEN THINGS GET SLOW — ledgered, and the source folder was not touched.** *Known:* your F2 — old photos of you from birth onward, funny stuff, the room to open when there is nothing new. Ledgered as `room.slow`, NOT_BUILT · HELD · `shown: false`. The folder is recorded in the ledger row and **nothing was read, copied or opened from it this round**, on instruction. *Missing:* your selection, and whether the frames are captioned. *Why it matters:* **the folder's own name says it is NOT GUARANTEED TO BE BACKED UP ANYWHERE ELSE**, so the first act on it is a copy, never a move and never an edit in place. And the path names two other people — it is a family set, not a Mike set — so which frames may be published is your call and nobody else's, the same consent rule that kept two real households off the Foundation's invoice ([M38](#m38)). | F2 · READING PASS ROUND ONE | OPEN | Mike | 2026-08-05 |
| <a id="m57"></a>**M57** | **WHAT IS WEIRD.BABY ROBOTS? — the slot is held and empty, on your instruction.** *Known:* your P3 — *"[PAPA] — Mike will vibe this; the only seed is 'Purveyors of the Weird.Baby'. Backlog it, do not write it."* *Missing:* the answer. *Why it matters:* it is the second question on the wing's front desk and **a visitor never sees it** — the entry carries the marker in BOTH its title and its answer, so the scrubber drops the whole row rather than printing a question with a silence under it. That is deliberate and it is the same path /foundation's billionaires answer takes; the cost is that the FAQ's own count and a visitor's count are different numbers again. Nothing was written toward it. | P3 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m58"></a>**M58** | **THE ROBOTS FAQ LOST THE ONLY LINE IN THE WING THAT SAID WHICH DOOR IS WHICH.** *Known:* the old START row read *"The image archives are what the machines look like… The record is what happened… The portal is the only thing in this wing that is running."* Your replacement is *"Finish the FAQ, then follow The Record."* *Missing:* nothing you asked for — this is a consequence, reported rather than absorbed. *Why it matters:* the wing now has four albums and eleven faces and no sentence anywhere that says what any of them is for. The new answer sends a visitor to ONE door, which may be exactly right for a first read; the other three are found by clicking. | P3 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m59"></a>**M59** | **THE NAME IS DELETED AND THREE THINGS ARE NOW STATED NOWHERE.** *Known:* your P2 — *"THE NAME is DELETED IN TOTAL."* Done: track, face, register and three entries. *Missing:* whether any of it comes back on another face. *Why it matters:* the wing no longer says (a) that the machine was BUILT as MGK-NIAC and SOLD as MGK-VIII, or that ABEAL did the selling and the renaming; (b) how the mainframe differs from the portable — the classic answer set here, the adjustable personality and the named engines there; (c) why the door reads MGK-NIAC when the folder, the firmware and the parts all say MGK-VIII. The album title still ANSWERS (c); nothing EXPLAINS it. Closes [M36](#m36) by deletion. | P2 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m60"></a>**M60** | **"FOLLOW US ON SOCIAL MEDIA" IS ON THE GLASS AND THERE IS NO HANDLE BEHIND IT.** *Known:* your P3 answer to *"Can I buy one?"*, verbatim, with the tracking warning you asked for attached. *Missing:* the accounts. **Nothing in this repository names a Weird.Baby account on any platform** — grepped for instagram, facebook, tiktok and @weird; the only hits are other artists' and the museum's own outbound embed URLs. *Why it matters:* it is an instruction a visitor cannot act on. It is your sentence so it ships as written; a handle is not something Ops may invent, and the tracking warning is honest whether or not the accounts exist. | P3 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m61"></a>**M61** | **THE MANUAL CANNOT GO ONLINE WITHOUT REVERSING YOUR OWN RULING FROM YESTERDAY.** *Known:* your P8 — *"THE MANUAL GOES ONLINE in a document viewer (microfiche register) — visitors page through what there is to page through, and no further."* **THE VIEWER IS BUILT AND WAS VERIFIED THIS ROUND**: `RobotsExhibitFlow`'s reader pages with ‹ Prev / Next ›, wraps at both ends, prints `title · date · frame n of m` on its rail and toggles Fit ↔ Magnify. A reel is a data block and nothing else moves. *Missing:* a page. **The museum holds exactly ONE manual document — the 61-page STRUCTURE ISSUE — and its cover reads `STRUCTURE AND ARRANGEMENT ONLY / TEXT NOT SUPPLIED` while its interior pages read `[ TEXT REQUIRED ]` and `[ ART REQUIRED ]`.** *Why it matters:* **P2, one round ago, struck a single page of that document from this same face**, in your words — *"it was the museum admitting it had not written the manual, wearing a fiction as cover"* — with the rule *either the plate shows a page actually written, or there is no plate until one exists*. Publishing 61 of them is that ruling reversed at 61× the scale. And B8 stands beside it: the artifact is a PHOTOGRAPH of a printed sheet, never a rendering, so even written pages need your printer and your camera. **Ops did not publish and did not silently do nothing** — the viewer is proved, and this is the one question. **RULED D6 (CLEAR THE DECK): THE MANUAL STAYS OFFLINE UNTIL REAL PAGES EXIST.** Mike: *"the viewer is built and stays built; nothing is published from the 61-page structure issue — same ruling as the single struck plate, one scale up."* **Nothing shipped, which is the ruling working:** the reader still pages, wraps, counts frames and toggles Fit ↔ Magnify; `reel.plates` is still `[]`; the face still says *No pages on file*. What it now waits on is a WRITTEN page, printed and photographed — which is [P2](#p2) in the art register and B8's standing spec, not a build. | P8 · REMOTE CONTROL | **RULED — HELD** | Mike | 2026-08-05 |
| <a id="m62"></a>**M62** | **THE FOUNDATION'S ALBUMS AND TRACKLIST ARE NOT BUILT.** *Known:* your P10 — *"it needs ALBUMS (Ledger, FAQ, Contribute) and a TRACKLIST, like every other wing."* **The other two corrections and the tone ruling shipped in full** (see [§5i](#p-closed)). *Missing:* the conversion. *Why it matters, stated rather than dressed up as a schedule:* /foundation is not a `face` wing — it is a sheet page carrying **three bespoke objects the face model has no equivalent for**: the $0.00 ACCOUNT CARD (the room's only visual hook, and a typographic object rather than a picture), the two-sided REGISTER (a table whose LIVE / NOT BUILT column is read live off `reveal/ledger.json` — the room's honesty mechanism), and the zero-cost LEDGER. A straight port to `blurb` / `lines` / `entries` **deletes all three**, and two of them are mechanisms you specified yourself. The honest build is three new face-level object kinds in `Exhibit.jsx` (the pattern `panel`, `reel` and `bill` already set), three album covers, an artist config and a route change — a round's work with real regression risk on a room you wrote the copy for eight days ago. **Ops chose to ship the corrections and hand this back rather than half-convert it.** **CLOSED D7 (CLEAR THE DECK) — MIKE RULED OPTION A AND ALL THREE MECHANISMS CROSSED INTACT.** `face.account`, `face.register` and `face.ledger` are three new object kinds in `Exhibit.jsx`, mounted on the presence of a field exactly the way `InstrumentPanel` is; `src/routes/exhibit/FoundationObjects.jsx` is **the sheet's own markup and the sheet's own stylesheet, moved rather than rewritten**, which is the cheapest guarantee available that nothing was lost — if a rule still matches, the object still looks like itself. The reveal-ledger wiring is untouched: flipping `channel.qr` to LIVE still changes this wing and no other file. Three albums in his order (Ledger · FAQ · Contribute), three typographic covers, and **the address did not move**. **THE LAP CAUGHT THREE THINGS NO GATE COULD** — the billionaires question printing with silence under it (the face model's entry filter is an OR where the sheet's was an AND), a player bar on a wing with nothing to play, and the objects drawing above their own heading. All three fixed. The one cost is named at [M70](#m70). | P10 · REMOTE CONTROL | **DONE** | Code | 2026-08-05 |
| <a id="m63"></a>**M63** | **THE WB LOGO IS MISSING A THIN BLACK OUTLINE.** *Known:* your P11 — *"LEDGER, build nothing: the WB logo's missing thin black outline (Mike will supply art if asked — ask when it matters)."* Recorded, nothing built, nothing asked for. *Missing:* the art, when it matters. *Why it matters:* `public/WeirdBaby_PhotoID.png` is the house mark and it is now on THREE surfaces — the Lobby, the robots front desk's cover, and (new this round) the /wb ABOUT THE ARTIST album cover. Whatever the outline fixes, it is fixed in one file. | P11 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m64"></a>**M64** | **"ABOUT THE SONGS" FOR WEIRD.BABY MUSIC — wanted someday, not today.** *Known:* your P9, verbatim. *Missing:* the songs' own labels. *Why it matters:* **it is not scaffolded**, on the NO-COMING-SOON credo — an empty container at a live address is the thing that credo kills, and the renderer is generic, so building it on the day there is something to put in it costs a data block and no code. The wing now has an ABOUT THE ARTIST album first, whose own biography slot is a marked [PAPA] that prints nothing; the two are the same shape of gap and neither invented a word. | P9 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m65"></a>**M65** | **THE ROBOTS WING NOW OPENS ON THE RECORD RATHER THAN ON THE FAQ.** *Known:* the viewer opens on the FIRST TRACK of the landing album — measured on the built bundle, not inferred — and your P2 put The Record first. So /robots introduces itself with a log of one entry, and the FAQ is one click away. *Missing:* whether that is what you meant by the ordering. *Why it matters:* your own first FAQ answer is *"Finish the FAQ, then follow The Record"*, which reads as an instruction to a visitor who has already met the FAQ. It is a consequence of the order you gave rather than a separate decision, so **nothing was reversed** — the tracklist is exactly as you ordered it and this row is here so the change is not silent. One line either way: the order stands, or the FAQ leads and the Record sits under it. | P2 · REMOTE CONTROL | OPEN | Mike | 2026-08-05 |
| <a id="m66"></a>**M66** | **THE CONTACT ANSWER EXISTS TWICE AND THE TWO COPIES HAVE DIVERGED — WHICH IS THE DEFECT YOU REPORTED, FOUND IN THE PLACE YOU DID NOT LOOK.** *Known:* your D1 was *"he edited the contact answer once and it survived elsewhere"*, and D2 gave the booth its new wording — *"Write to the guy running the place: papa@weird.baby."* The robots front desk answers *"How do I get in touch?"* with the bare address and nothing else. *Missing:* one word. *Why it matters:* **it was NOT folded in, deliberately.** Every answer on that face is yours, word for word (P3), so rewriting one under cover of a refactor is the exact thing D1 forbids — it says where two copies have DIVERGED, say so and let you pick. The day you say yes it is one import: `CONTACT` is already declared in `src/data/house-copy.js` and the booth already reads it. **[R1 2026-08-06] RULED — FOLD IT IN.** Mike: *"both rooms use my verbatim contact answer. No follow-on sentences."* `robots.js` imports `CONTACT` from `src/data/house-copy.js`; the bare address is gone. It was one import, exactly as this row predicted, and **there is no longer any known unlinked copy of a passage in that file** — the note in `house-copy.js`'s header that named this exception is rewritten rather than left standing. Verified on the built bundle: the front desk's last answer reads *"Write to the guy running the place: papa@weird.baby."* | D1/D2 · CLEAR THE DECK; ruled R1 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-06 |
| <a id="m67"></a>**M67** | **THE TWO MACHINES' FAQ FACES DIFFER IN WORDING WHERE THEY SHOULD ONLY DIFFER IN NOUN.** *Known:* parity is absolute (P1), so both machines carry the same menu items — and three passages inside those items were typed twice and are now declared once (D1). Two more were left alone: each FAQ's blurb (*"The questions that get asked about **the cabinet**"* against *"The questions that **actually** get asked about **the unit**"*) and *"Does it still work?"* (the portable's answer carries a second sentence about the Portal that the mainframe has no equivalent for). *Missing:* one word on the first. *Why it matters:* the NOUN differing is the point — one is a cabinet and one is a unit. The word *actually* is not a noun and nothing says why it is on one face and not the other; `npm run parity` polices the menu ITEMS and cannot see the words inside them, so a drift here is invisible to the one tool built to catch drift. **The second half is probably right as it stands** — the portable has a Portal to talk about and the mainframe does not. Not merged, because folding two passages about two different objects would be Ops inventing a shared house line out of a coincidence of wording. **[R2 2026-08-06] RULED — AND HE DID NOT PICK A WORDING, HE STRUCK BOTH.** Mike: *"BOTH FAQ intro blurbs are DELETED. A line introducing a list of questions is the page explaining what the page is."* Law of Subtraction. The divergence resolves by there being no sentence left to diverge, which is the stronger form — two matching blurbs would still have been two copies to keep in step, invisible to `npm run parity`. Both faces now run title, plate, questions. **THE SECOND HALF OF THIS ROW IS UNTOUCHED:** *"Does it still work?"* still carries a Portal sentence on the portable that the mainframe has no equivalent for, he ruled on the blurb and said nothing about it, and D1's reading of it stands. **Ops checked `/booth` and `/foundation` too**, because his instruction named those two rooms: neither has an FAQ intro blurb at all — the booth has an `<h2>Questions</h2>` heading and the Foundation's face has a title and a subtitle, and a heading is not a blurb. Nothing was deleted there. | D1 · CLEAR THE DECK; ruled R2 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-06 |
| <a id="m68"></a>**M68** | **THE SHARE CARD AND THE TWITTER CARD ARE TWO DIFFERENT DESCRIPTIONS OF ONE MUSEUM.** *Known:* `index.html` carries three. `og:description` reads *"A museum of weird things worth keeping. The MGK robots, and music worth a listen. No ads, no affiliate links, no cut of anything you buy from an artist."*; `twitter:description` is the same sentence **with the wings clause dropped**; `meta name="description"` is a third, shorter line. *Missing:* one word — which description is the museum's. *Why it matters:* C4's own note says these are *the handshake before the email* — an artist sent a link sees this before they see the site — and the two social cards say different things about what is in the building. **Nothing in the file says the shortening was deliberate**, and a length limit would be the honest reason if it is one (og is 154 characters, twitter 108, and Twitter's own limit is 200). Not merged: a share card is the museum introducing itself and that is not Ops' sentence to pick. **[R3 2026-08-06] PRINTED VERBATIM FOR HIM, PER HIS OWN INSTRUCTION — *"Mike could not review them from a phone; PRINT BOTH VERBATIM so he can pick, and make them identical only after he rules."* Not merged.** All three, exactly as `index.html` carries them today:<br>· `og:description` (154 chars) — *"A museum of weird things worth keeping. The MGK robots, and music worth a listen. No ads, no affiliate links, no cut of anything you buy from an artist."*<br>· `twitter:description` (108) — *"A museum of weird things worth keeping. No ads, no affiliate links, no cut of anything you buy from an artist."*<br>· `meta name="description"` (66) — *"Weird.Baby Museum. Exhibiting the MGK robots and Worth A Listen."*<br>**HIS SECOND QUESTION — is one of them plainly stale or wrong rather than merely different — HAS AN ANSWER, AND IT IS THE THIRD ONE.** *"Exhibiting the MGK robots and Worth A Listen"* names TWO exhibits; the lobby board lists THREE — Weird.Baby Robots, **Weird.Baby Music**, Other Music Worth a Listen. The house's own music wing has been missing from the line every search result shows since R1 wrote it on 2026-08-02 to fix a different staleness (it used to say *"Currently exhibiting Hunter Root"*). The og line's *"music worth a listen"* is lowercase and generic enough to cover both; the twitter line names no wings at all. **THE SHORTENING IS NOT A LENGTH DECISION:** Twitter's own limit is 200 and the og line is 154, so it would have fitted. Nothing in the file says why it was cut. Still not merged — a share card is the museum introducing itself and that is not Ops' sentence to pick. | D1 · CLEAR THE DECK; printed R3 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m69"></a>**M69** | **HUNTER ROOT'S HOLDINGS SENTENCE HAS TWO WORDINGS AND BOTH ARE CORRECT.** *Known:* his artist card reads *"The house artist — the one musician in this wing whose records the museum holds in its own vault: seven of them, an EP and a set of singles and rarities, ninety-three tracks in all, accessioned from April 2026."*; the poster's panel reads *"The one artist in this room whose records the museum holds in its own vault: seven of them, an EP and a set of singles and rarities — ninety-three tracks."* Every figure in both was re-verified against the vault this round. *Missing:* one word. *Why it matters:* it is the only near-duplicate the D1 sweep found where **both copies are right**, which is exactly when a divergence is cheapest to leave and most likely to rot — the next correction will land on one of them. Ops' reading is that a CARD and a POSTER are allowed to read differently, and that is a reading rather than a ruling. **[R4 2026-08-06] RULED, AND THE RULING IS BIGGER THAN THE DUPLICATE.** Mike: *"DELETE THE HOLDINGS SENTENCE ENTIRELY, everywhere. Nobody asked for a holdings announcement; it exists only because it was typed for Hunter. A visitor presses play and it plays. The fact stays in the vault; the bragging leaves the page. All four artists become identical — name, songs, cards, doors. Same only the data."* **NEITHER WORDING SURVIVES.** Seven sites struck or trimmed: the card's whole two-paragraph label · the tombstone's *Catalogue — 93 tracks on file in the museum's own vault* and *Surfaced here — Two* (the only two rows no other artist has) · the *Nothin' Wrong* card's first paragraph · the metrics note's second half · the records note's second clause · the aboutNote's *"he is our artist and this is our record of him"* · the poster's `what` and `why`. **WHAT DELIBERATELY STAYED, so the boundary is on the record rather than in Ops' head:** the *Records* tombstone row (a count of what he MADE, the same claim Carsie's *"Twelve, every one of them independent"* makes, naming neither museum nor vault), the two song accessions, the plate caption and the deck attributions — all four are PROVENANCE, which Doctrine 11 ships. Two consequences are their own rows: [M72](#m72) (his card has no biography now) and [M75](#m75) (`/hr/archive`'s header still announces the holding). | D1/D3c · CLEAR THE DECK; ruled R4 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-06 |
| <a id="m70"></a>**M70** | ~~The Foundation’s FAQ is an entry list now; its accordion is gone.~~ **CLOSED R7 — RULED AND BUILT.** Mike: *“the Information Booth IS an FAQ under a better name and keeps that name for UX value. Sub-exhibits carry their own FAQs — a visitor must never have to run back to the lobby. Conform every wing FAQ to the booth’s format.”* D7’s flattening is UNDONE rather than defended, and three more faces conformed with it (the robots front desk and both machine FAQs). M1 is not bent: a question is the description of its own answer, which is `/booth`’s own recorded reasoning. | D7 · CLEAR THE DECK | DONE | Mike | 2026-08-06 |
| <a id="m71"></a>**M71** | **FORTY-SEVEN OF HIS NINETY-THREE TRACKS ARE NOW REACHABLE THROUGH NOTHING, AND TWO WHOLE RECORDS GO DARK.** *Known:* R5 stopped the site serving Hunter Root's vault audio. Measured on the built bundle: **33 of 93 tracks carry an official YouTube upload and 60 do not**, so 60 rows on `/hr` render `.tl-novid` — dead, 32% opacity, no cursor, which is a shape the tracklist already had and no render path was invented for it. **Run With The Hunt (15/15) and the Phone Recordings EP (5/5) have nothing playable at all**; They Finally Cracked Me is 10 of 11 dark, Life Inside A Wheel 11 of 12. The deck's album overlay marks the same tracks *unavailable*, its own existing state. **R7 does not rescue them**: adding his Bandcamp lifts the reachable count from 33 to 46, and 47 remain nowhere. *Missing:* a ruling on what the page should LOOK like. *Why it matters:* **the rights call was yours and Ops made it; this one is a design call and Ops did not.** Two shapes: keep the 93 rows and let 60 read dead — a catalogue that says plainly what it cannot play — or show only what plays, which empties two covers out of the coverflow. Under the Law of Subtraction the second is arguable; under *empty and honest* the first is. Ops shipped the first because it deletes nothing. **[H1 2026-08-06] VERIFIED AGAINST HIS QUESTION, AND THE ANSWER IS HALF.** He asked whether `/hr` going private makes this moot. The visitor-facing half is gone: the 60 dead rows and the 47 unreachable tracks are now seen only by him and by Ops, so nothing a member of the public meets is affected and there is no urgency left in it. **The design question is not moot** — it is a page that still exists and that Ops still works from, and *what it should look like without its audio* is still his call. **Downgraded from a public-facing defect to a reference-copy question, and named as downgraded rather than quietly closed.** | R5 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m72"></a>**M72** | **HUNTER ROOT'S ARTIST CARD HAS NO BIOGRAPHY, AND IT IS THE SECOND TIME THAT SLOT HAS FAILED THE SAME WAY.** *Known:* R4 struck both of its paragraphs because both WERE the holdings announcement — *"the house artist — the one musician in this wing whose records the museum holds in its own vault…"* and *"Two of those songs are surfaced in this room, both from the vault's own copies…"*. Before CS struck the pair in front of them, the same two slots described this WEBSITE. The card now runs plate, marker, register, records board, decks and doors; the other three carry three paragraphs of life and work between the register and the shelf. `face.label` is conditional (`Exhibit.jsx:3379`) so nothing draws empty — verified on the built bundle. *Missing:* the paragraphs. *Why it matters:* **Ops will not draft them (Doctrine 12)** — nothing in this repository holds his life except vault quotes already printed on the same card's decks. **One sourced fact IS in hand and is not enough on its own:** `MV-HR-20260707-035`, his own words to Whiskey Riff in 2023 — *"I am Pennsylvania raised but I was born in Fayetteville, Arkansas. My brother and I both."* The R7 research added a second from his own Bandcamp bio (*"Solo artist/musician from Lancaster, PA"*), which is now on the poster. Two facts are a tombstone row, not a biography. | R4 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m73"></a>**M73** | **THE SITE STOPPED SERVING HIS AUDIO AND STILL SERVES HIS PICTURES.** *Known:* your instruction was *"verify nothing else on the public site serves vault audio"*, and Ops did exactly that and no more. `/hr` requests **`assets.weird.baby` eighteen times** on a full pass — album covers, artifact thumbnails and three photographs — all off the museum's own CDN, all of his material, and the same permission question the audio just failed. *Missing:* one word. *Why it matters:* **extending an audio ruling to images would be Ops widening your instruction, and that is the one thing a lane whose job is to comply must not do.** It is stated here rather than acted on. Note the asymmetry it leaves: the site will not play his record but will show its sleeve. **[H1 2026-08-06] HALF-ANSWERED BY MECHANISM, AND THE HALF THAT REMAINS MOVED HOUSE.** Measured on the built bundle: all **107** vault image URLs are now inside `assets/held/hunter-root-catalogue-*.js` and **zero** reach a public visitor. **That happened because the wing went private, not because anybody ruled on images** — the rule you were asked for is still unwritten, and it now bites somewhere else: `/wal` serves `hunter-root-cover.jpg` and `hunter-root-plate.jpg` from the museum's own origin, publicly, today ([M82](#m82)). The asymmetry this row named has changed shape rather than gone: the site will not play his record, will not show its sleeve to the public, and still shows his portrait. | R5 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m75"></a>**M75** | **`/hr/archive` STILL ANNOUNCES THE HOLDING, AND IT IS THE ONE PLACE R4 DID NOT STRIKE.** *Known:* its header reads *"7 records, plus an EP and a set of singles and rarities · 93 tracks on file"*, every figure derived off the spine since D3c. Your instruction said *everywhere*. *Missing:* one word. *Why it matters:* **Ops read it as different in kind and says so rather than quietly applying or quietly exempting it.** On an artist's card a holdings figure is the museum talking about itself beside a musician; on a catalogue page it is the index line of the list directly beneath it, and striking it leaves a page of nine records with nothing saying what they are. The page is also unlinked — reachable by URL only. If the answer is that it goes too, it is one line. **[H1 2026-08-06] VERIFIED STILL TRUE AND NOW BEHIND THE PASSWORD.** Read off the built bundle this round: the header is unchanged. What changed is the audience — it is now a catalogue page saying what it is a catalogue of, read by two people. The one-word ruling stays open and is cheaper than it was. | R4 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m76"></a>**M76** | **THE SIZE OF THE KNOB IS THIRTEEN TRACKS, NOT NINETY-THREE — AND THAT IS THE HEADLINE OF THE R7 RESEARCH.** *Known:* you asked how big the ad-free-legal-playback knob is per artist, and named the prize: *"if a whole discography can be served cleanly, that is a very big knob we can turn at any point."* Full map, sourced and dated: **`docs/AD_FREE_PLAYBACK_RESEARCH-20260806.md`**. The short of it: **embedding needs no permission on either platform** — Bandcamp's Share/Embed control is on every public release page and generates a plain iframe, ad-free and unbranded — so this is a SOURCE question, as you said. **For three of the four artists the knob is real and large** (Carsie Blanton 16 releases, Jesse Welles 7 albums, both complete on their own Bandcamp). **For Hunter Root it is small and that is the finding**: his Bandcamp carries 3 albums and 13 singles, which covers 38 of the vault's 93 tracks and **adds only 13 the site cannot already reach through YouTube**. Mikey Mike has no Bandcamp and no hosted audio anywhere Ops could find. *Missing:* whether to build it, given that. *Why it matters:* the honest version of the prize is that it re-opens **three catalogues the museum currently shows two songs of each** — and does almost nothing for the wing that just went quiet. | R7 · THE PORCH RULINGS | OPEN | Mike | 2026-08-06 |
| <a id="m77"></a>**M77** | **A `/CB` WING IS POSSIBLE, AND THE REASON IT IS NOT OBVIOUSLY WORTH IT IS YOURS.** *Known:* your H3 — a Carsie Blanton wing is possible someday, **but Carsie’s own homepage is already strong, so a museum wing helps her far less than it would have helped Hunter, whose web presence is poor.** *Missing:* whether you want it, and when. *Why it matters:* it changes the VALUE PROPOSITION rather than the feasibility, which is the half a build brief would have got wrong — the machinery is generic and `/wal` already holds her sixteen releases as a two-song sample. **Nothing was built.** Looking for the route DID find one thing: `/admin` carried a `/cb` jump button and **there has never been a `/cb` route in `App.jsx`**, so it fell through the catch-all onto the Lobby — a dead control on the one page whose job is to report the true state of things. It is deleted. | H3 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m78"></a>**M78** | **THE ALBUM-ART EGG — HIDE SOMETHING IN THE SCREEN OF THE UNIT.** *Known:* your H6, graded high, and the grade is your own reasoning: **once a visitor finds one, they will check every cover forever — and every time the art changes.** That is the only mechanism proposed for this building that turns a static asset into a recurring reason to look. *Missing:* whether to do it, on which covers, and what is in the screen. *Why it matters:* **your constraint is the harder half and is recorded with it — this does not mean eggs everywhere.** The failure mode of a good egg is a house that starts hiding things by habit, and the poke ([M1](#m1)) is already the gate on every future egg. **Nothing was built and no art was touched.** It is deliberately NOT a `reveal/ledger.json` row yet, because a ledger row is a claim that a thing exists. | H6 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m79"></a>**M79** | **THE BOOTH’S PRIVACY ANSWER NOW OVER-DISCLOSES, AND IT IS THE FIRST TIME THAT CLAUSE HAS BEEN WRONG AGAINST THE MUSEUM.** *Known:* it says *“one exhibit carries posts embedded from Facebook”* and *“Google, YouTube and Facebook each know you turned up, and they are the whole of the outside.”* The only Facebook embeds in the building are `HrExhibitFlow.jsx`’s sixteen plugin frames; with `/hr` held, **no public visitor’s browser touches Facebook at all.** *Missing:* one word. *Why it matters:* the answer’s own last line is *“if that ever stops being true, this answer changes first”*, and R6 ruled that disclosure IS the whole of the remedy, which makes its accuracy the whole of the remedy. **It was NOT edited.** It is your answer, R6 is one round old, and the error runs in the safe direction for the first time in three versions — it claims a leak the museum no longer has rather than hiding one it does. The fix is one clause: strike the Facebook sentence, and the word Facebook from the sentence after it, leaving Google and YouTube. | H1 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m80"></a>**M80** | **HIS OWN POST TEXT IS COPIED INTO THE MUSEUM’S DATA, AND NO ROUND HAD NAMED IT.** *Known:* the 49 artifacts in `hunter_root.json` each carry `title` and `description`; for the 22 social ones those fields are **his Facebook, Instagram and ReverbNation writing, verbatim**, and `HrExhibitFlow.jsx` prints them on the card face and in the overlay caption. *Missing:* a ruling. *Why it matters:* **an embed licence covers the iframe and not a separate copy of the same words in another system** — that is what embedding exists to avoid. **This item survived every remedy so far:** R5 took the audio, H1 took the media, and the text only went behind the door because it happens to live in the same file. A fix aimed at media would have missed it entirely. Audit: `docs/HR_PERMISSION_AUDIT-20260806.md` §2. | H2 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m81"></a>**M81** | **ALL 97 VAULT FACTS ABOUT HIM ARE PUBLIC, AND `/hr` GOING PRIVATE CHANGED NONE OF IT.** *Known:* `worth-a-listen-facts.js` imports the whole of `hunter_root.facts.json` and re-tags every fact into the `/wal` pool (*“one record, two rooms”*, its own header). Measured on this round’s built bundle: the set is in `assets/tokens-*.js`, a **public** chunk. *Missing:* whether the whole vault fact set belongs in a room that shows two of his songs. *Why it matters:* it is the sentence that stops *“/hr is private”* being read as *“his material is off the site”*. **And there is a second counterparty inside it, which is not him:** 95 of the 97 are extracted from press, ten from one writer and eight from one agency, and a publication may reasonably regard ten of its best lines as a substitute for reading it. That is a volume question about quotation, and the party with the grievance is the publication. | H2 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m82"></a>**M82** | **`/wal` SERVES TWO OF HIS IMAGES FROM THE MUSEUM’S OWN ORIGIN AND THEY ARE PUBLIC TODAY.** *Known:* `/images/wal/hunter-root-cover.jpg` and `/images/wal/hunter-root-plate.jpg`, referenced from `worth-a-listen.js`. Same class as the vault images [M73](#m73) asks about; different host, same act. *Missing:* one word, and it is the same word M73 wants. *Why it matters:* **this is where the image question actually lives now.** H1 took `/hr`’s 107 out of public reach and left these two exactly where they were. The plate is also the selfie-in-a-vehicle of [M3](#m3), so two open rows now point at one file. | H2 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m83"></a>**M83** | **THE SHARE CARDS DESCRIBE AN OPEN MUSEUM AND THE FRONT DOOR SAYS IT IS NOT OPEN — PLUS THE GIFT SHOP TYPEFACE TEST.** *Known:* three redrafted descriptions are printed **verbatim** in `docs/MUSEUM_PRE_COMMENTARY_LOG-20260806.md` §H4 and **`index.html` is untouched**. They are one sentence in three lengths, and the search line is a PREFIX of the social one rather than a second wording — which is the structural answer to [M68](#m68)’s *“which description is the museum’s”*. The Lobby reads *“SOMETHING IS BEING BUILT HERE”* and *“We’re not open yet.”* *Missing:* your correction to the drafts, and one line on whether a share card should carry the not-open-yet fact. *Why it matters:* C4 built these tags as the handshake before the email; a card that leads with a disclaimer is a different decision from one that leads with what the place is, and Ops did not make it. **The typeface test rides here:** `/shop`’s room name is in `--wb-brand` at weight 600, scoped to `.gift-shop`, and reverting is one CSS block deleted. Measured at 390px it is 71px wide against Syne’s 81px, so it improves the tightest case rather than costing it. | H4/H7 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m84"></a>**M84** | **THE `/hr` WING IS NOW INVISIBLE TO THE PROVENANCE SWEEP’S REACHABILITY WALK, AND THE GATE STILL PASSES.** *Known:* `provenance:gate` follows STATIC imports from `src/main.jsx`; H1 made `/hr` a dynamic one, so nine Hunter Root files now sit in the sweep’s *“unreachable from src/main.jsx”* bucket — **485 strings**, of which only 154 were there before (`hr_facts.js` 124 and `hr_journal_prompts.js` 30, which are [M5](#m5)’s genuinely dead files). *Missing:* nothing to decide; this is a note so nobody acts on a wrong reading. *Why it matters:* **the other 331 are the live wing, not dead code**, and a future round doing a cleanup pass on that bucket could delete a working exhibit. Every one of them is still DECLARED and the gate is PASS. | H1 · PRE-COMMENTARY | OPEN | Ops | 2026-08-06 |
| <a id="m85"></a>**M85** | **`HR_KEY` HAS NO DEFAULT, SO THE HELD WING IS SHUT UNTIL YOU SET IT — INCLUDING TO YOU.** *Known:* `src/worker.js` reads `env.HR_KEY` and there is no fallback value anywhere; with it unset `/api/held` answers **503** and says *“No key is set on this deployment. Run: npx wrangler secret put HR_KEY”*, which is a deliberately different sentence from *Wrong key*. *Missing:* one host-side command, once per environment. *Why it matters:* **failing closed is the design and a default would have been a published password.** It also means the first deploy after this packet leaves `/hr` unreachable to everybody until the secret exists, and that is the intended order rather than an outage. | H1 · PRE-COMMENTARY | OPEN | Mike | 2026-08-06 |
| <a id="m86"></a>**M86** | **THE VIEWER HAS NO IMAGE DESCRIPTIONS AND THE WALL STILL HAS THEM, AND THAT IS A SPLIT RATHER THAN A FIX.** *Known:* N6 struck the description off the reader's rail, which now carries the archive's name, the frame's date and `Frame n of m` — exactly what you read and named. The same `label` still captions the tile on the WALL. *Missing:* one line — the wall's captions go too, or a description is worth printing where a visitor is CHOOSING a photograph and not worth printing once they are looking at it. *Why it matters:* the wall's caption is currently the only thing that tells a visitor which tile is which, so striking it there is a different instruction from the one you gave; and leaving them means two surfaces in one wing disagree about whether the words are good enough. | N6 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m87"></a>**M87** | **THE BUILDER'S OWN LINE ABOUT THE POWER DRAW HAS NO HOME.** *Known:* N2 converted Technical Specifications into a one-sheet, and every figure survived as a register row. One thing did not: “If I make too bright at once the Nano will shut down due to power draw.” — a real quotation, in his own words, and the best sentence the old face carried. A spec sheet has no row for a quotation. *Missing:* whether it comes back, and on which face. *Why it matters:* it is the one human sentence in the wing's most technical corner and it is now in git and nowhere else. | N2 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m88"></a>**M88** | **MGK-NIAC'S TECHNICAL SPECIFICATIONS FACE NOW HAS NO PICTURE.** *Known:* you struck the output-row photograph with the three sentences beside it, and a spec sheet is not illustrated — that is most of what makes it a spec sheet. The photograph is not deleted; it is a tile on the wall one row down. *Missing:* a ruling, or nothing. *Why it matters:* it is a live conflict with the standing Visual Hook Law and it is the third face in this wing left imageless deliberately ([M29](#m29), [M48](#m48) are the others). Three is a pattern rather than three exceptions. | N2 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m89"></a>**M89** | **THE MANUAL'S SIX ATTESTED SECTIONS ARE STRUCK, AND ONE OF THEM CARRIED A `[PAPA]` SLOT OF YOURS.** *Known:* N3 said to strike everything on that face except what we are actually holding, and the contents page went with it — §1 Start-up, §2 Operating the answer engine, §3 Bias, §4 BIST and AMMMS, APP. 1 The passcode landscape, and **MARGINS**, whose note was “[PAPA] — which hands, and what they wrote”. Every row was attested and each said *attested · no plate on file*. *Missing:* whether any of it comes back, and where — the document card, a face of its own, or nowhere. *Why it matters:* the MARGINS slot was a thing you had marked as owed, and it is now recorded in no register but this one. | N3 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m90"></a>**M90** | **BOTH ARCHIVE TOMBSTONES ARE STRUCK AND TWO CLAIMS WENT WITH THEM.** *Known:* N4 struck the SUBJECT / STATE / PLATES / FRAME registers on both walls. Two rows had no other home: **`Frame`** was the only place in the museum saying the mainframe's withholding is AUTHORED rather than a shortage of photographs, and **`Rights`** was the only statement that the photographs are the museum's own. *Missing:* whether either sentence comes back, and where. *Why it matters:* the first is the difference between metering a reveal and being short of pictures; the second is the only rights statement on a wall of images. | N4 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m91"></a>**M91** | **THE WING HAS TWO IMAGE ARCHIVES AND THE GROUPINGS CANNOT CROSS THEM.** *Known:* N9 built `face.presets` per wall, so each album's archive has its own curated cuts and its own coarse everything. Your own example named “all NIAC, all VIIIp, everything”, which spans BOTH machines. *Missing:* whether the wing's two archives become one room with one set of groupings — and if so, on which album it lives, given parity requires both to carry the same menu row. *Why it matters:* a cross-machine everything on the mainframe's album would put the portable's photographs under the mainframe's cover. | N9 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m92"></a>~~**M92**~~ | **THE TWO TECHNICAL SPECIFICATIONS FACES ARE NOW IN TWO DIFFERENT FORMS.** *Known:* N2 is written under the MGK-NIAC heading and names that face's own four contents, so it was applied there and there only. The mainframe's is a one-sheet register; the portable's is still a plate, a blurb and two prose entries about the two source trees. *Missing:* one line — the portable conforms, or the two faces are allowed to differ because their subjects do. *Why it matters:* parity governs the menu ITEM and says nothing about the face inside it, so no gate will ever catch this; it is exactly the kind of divergence Doctrine 17 says to report rather than merge. **[N2 2026-08-06] RESOLVED, AND IN THE DIRECTION NOBODY CHOSE.** The question was *conform or allow the difference*; Mike's global standard answered a third way — **both faces lose their real-build register**, because both were describing the workshop. The portable did not conform TO the mainframe; they were both cut back to the object. See [N-g](#n-g) and [N-h](#n-h) for what each now needs. | N2 · PAGE-BY-PAGE · closed N2 | **DONE** | Ops | 2026-08-06 |
| <a id="m93"></a>**M93** | **THE FRAME DATES: NINE OF FOURTEEN ARE NOT DATES, AND THE FIVE THAT ARE DO NOT FIT THE STORY.** *Known, and this is the N7 flag in full.* (a) The portable's nine tiles carry SLOT LABELS in the `date` field — FAMILY SHOT · FRONT · SCREEN · BEZEL · TOP · BASE · BASE, NEW · REAR · COVER — and the reader prints them where a date goes, so a visitor reads FRONT as the date. One field, two jobs, two walls, one wing. (b) The mainframe's five all read **MAR 2021**, on a wing whose front desk says the delivery arrived sixty years after 1965 and whose Record's one entry IS that delivery. A photograph of the machine dated 2021 asserts the museum had it years before it arrived. *Missing:* which one moves — the dates, the story, or the field. *Why it matters:* (b) is true of the PHOTOGRAPH and false of the STORY, and Ops cannot choose between those. | N7 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m94"></a>**M94** | **THE NAMEPLATE HAS A HAND-STAMPED SERIAL FIELD AND NO SERIAL.** *Known:* P2's reference is bright metal with black ink and a space where a serial is hand-stamped; the space is built, holds its width, and renders as an unstamped plate. *Missing:* the number. *Why it matters:* Ops may not mint one — a serial is a specific nobody supplied and a plausible one is Doctrine 12's exact failure. It is one string in `robots.js` (`panel.serial`). | P2 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m95"></a>**M95** | **THE CONTROL PANEL OCCUPIES THE CENTRE AND THE WHOLE RIGHT OF ITS AREA IS EMPTY.** *Known:* your own reading, given as NOTED, NOT FOR ACTION — probably a phone-fit consequence, and you do not want it changed or filled now. Recorded so it is a someday rather than a thing you said once. *Missing:* nothing, until you want it. *Why it matters:* it is the only row in this register that exists purely because you asked for it to exist. **Nothing was moved, filled or re-laid out this round.** | P6 · PAGE-BY-PAGE | OPEN | Mike | 2026-08-06 |
| <a id="m96"></a>**M96** | **THE RECORD'S EVIDENCE CLASS IS IN THE MODEL AND RENDERS NOWHERE.** *Known:* R5 struck the badge in all three places it drew, because the word opened nothing and there is no registry of objects to pop — inventing one would have been Doctrine 12 with a button on it. The `evidence` field survives in `record-model.js` with a note saying plainly that nothing draws it. *Missing:* nothing today. *Why it matters:* it is your own B9 vocabulary sitting unrendered, and the obvious day it comes back is the day a long Record gets a FILTER — which is the same mechanism as N9's archive groupings, one surface over. | R5 · PAGE-BY-PAGE | OPEN | Ops | 2026-08-06 |
| <a id="m97"></a>~~**M97**~~ | ~~**THE 390px HALF OF THE LAP DID NOT RUN.**~~ **CLOSED N5 2026-08-06, after four rounds.** *The window's size was never the museum's viewport.* A **403px same-origin iframe** gives a document whose `innerWidth` is 390 exactly, and same-origin means the driver takes real measurements rather than reading pixels off a screenshot. The recipe is COMMITTED this time (`tools/lap/harness.html` · `npm run lap` / `npm run lap:clean`), because v50 solved this once and lost it with a gitignored scratch file. **The lap ran on eleven routes at 390px: zero page-level horizontal scroll, zero console errors, no non-200 image from the museum’s own origin.** The four rounds of unverified phone rules **held** — the 13px screws, the nameplate padding, the groupings’ unit noun and the grouping strip’s wrap all survive. What it found is on `/hr` alone and is [N-j](#n-j). | LAP · PAGE-BY-PAGE · closed N5 | **DONE** | Ops | 2026-08-06 |
| <a id="m98"></a>**M98** | **THE POSTURE TRAVELLED WITH THE LEDGER OBJECT, AND F2 DID NOT SAY IT COULD.** *Known:* F2 says BURN DOWN the old "The ledger" track — discard, do not port — and F7 says preserve the LEDGER OBJECT, so the track's framing burns and the document survives. That track also carried `POSTURE`: *"We still do a fair amount of the donating ourselves, and will simply continue until we no longer can."* — your own sentence, recorded by P10 as the room's heart, and the thing that says WHO IS CARRYING the costs the invoice lists at zero. Ops kept it, beside the object it explains, on The Ledger · Money in, out. *Missing:* one line — it stays, or it burns with the track. *Why it matters:* a bill for nothing is a gesture until somebody says out loud who is carrying the cost; and this is Ops reading past the letter of an instruction, which is a thing you should be told rather than have to notice. | F2/F7 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m99"></a>**M99** | **`provenance/assets-declare.mjs` HAD DRIFTED FROM THE FILE IT WRITES, AND THE NEXT `--write` WOULD HAVE DELETED FIVE ROWS IN SILENCE.** *Known:* the declarer regenerates the whole of `assets.json` from one array. Diffing that array against the JSON found five rows the array did not know about — the Foundation's three covers (added D7) and two robots rows added by hand at P2 and P7. All five are re-declared verbatim and the drift is zero today. *Missing:* nothing from you. *Why it matters:* **nothing runs that diff.** It is the same failure class as a stale provenance chain — a generator whose output has been edited by hand — and the only reason it was caught is that this round happened to add a cover. A `--check` mode that fails on drift is one function. | A3 · COMBINED BRIEF | OPEN | Code | 2026-08-06 |
| <a id="m100"></a>**M100** | **V1's SECOND STRIKE IS NOT IN THIS REPOSITORY.** *Known:* you asked for two sentences struck. The first — *"Four of them. Two songs each, all playable…"* — was on `/wal`'s poster and is gone. The second, *"Artists that command Weird.Baby's respect and deserve your awareness."*, **appears nowhere in `src/` or `index.html`**: searched for the whole sentence and for *respect*, *deserve* and *awareness* separately, and checked the rendered text of `/wal` and `/shop`. Nothing was struck in its place. *Missing:* where you are reading it — a screenshot, a URL, or the room. *Why it matters:* the two live possibilities are that the deployed site is behind `main`, or that it is on a surface Ops cannot see; and a round that quietly struck a NEARBY sentence and reported it as yours would be worse than this row. | V1 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m101"></a>**M101** | **C3's PRESETS — PROPOSED, AND THE MATERIAL DOES NOT SUPPORT THEM TODAY.** *Known:* your word was *"Propose"*, so it is a proposal. Counted off the four feeds themselves: Carsie 5 "official" · 1 live · 1 award (the FAI 2026 speech); Jesse 0 · 1 · 0; Hunter Root 1 · 1 · 0; Mikey Mike 2 · 0 · 0. **The whole wing holds one award video and four live ones across four artists.** A strip built on that is a control with one useful button, and *"notable moments"* is a judgement nobody has made — deriving it from title keywords would be invention wearing a classifier. *Missing:* either richer feeds, or your own grouping per artist. *Why it matters:* the mechanism already exists (N9's `face.presets`, every button carrying its count, coarse last, no strip on a single grouping) — what is absent is the material, not the code. | C3 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m102"></a>**M102** | **"ABOUT THE SONGS" IS DELETED AND EACH SONG'S OWN PARAGRAPH IS NOW RENDERED BY NOTHING.** *Known:* C4 struck the face and the builder went with it. Every song's `card.label` — the written paragraph about that song — stays in `worth-a-listen.js` and prints nowhere; the `card.tombstone`s stay for the reason that file already gives (they are the verification record and were never furniture). *Missing:* one word — re-home the song copy, or strike it. *Why it matters:* it is written work, and deleting it as a side effect of deleting its container is not a decision Ops should take. Eight paragraphs, two per artist. | C4 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m103"></a>**M103** | **/wb's ABOUT THE ARTIST IS SIX CATEGORIES AND FIVE OF THEM ARE YOURS.** *Known:* W1 burned the old card down and rebuilt it as slots any artist can be described by — where he is from · what he sounds like · in his own words · start with · what the museum holds · what he is doing now. Only the fifth can be filled from the record, so the card renders ONE item. The other five are marked and print nothing. *Missing:* a sentence each, at your pace, in any order. *Why it matters:* Doctrine 12 forbids the obvious alternative — a plausible paragraph about a musician nobody has described to Ops would read true and be invented — so the page is honest and nearly empty until you write. | W1 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m104"></a>**M104** | **FOUR `pick` SENTENCES ON THE POSTER — ONE PER ACT, AND ALL FOUR ARE YOURS.** *Known:* V2 asked for 2–3 sentences of substance from verified facts (already there, sourced, as each act's `why`) plus ONE SENTENCE on why you picked them. That slot is built, marked in its only sentence, and prints nothing — no placeholder and no dash. *Missing:* four sentences, one each for Carsie Blanton, Jesse Welles, Mikey Mike and Hunter Root. *Why it matters:* it is the only line on that poster that is the house speaking about its own choice, which is the thing a bill is for and the thing Ops must not write. | V2 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m105"></a>**M105** | **THE PROFILE TEMPLATE IS BUILT FOR ANY ARTIST AND IS WIRED ON ONE WING.** *Known:* `face.profile` is data and the renderer knows no category names, so a wing declaring different slots renders without a code change — which is W1's *"can be filled for ANY artist"* as a mechanism. Only `/wb` declares one. `/wal`'s four artist cards still carry their own `tombstone` + `sideboxes` composition. *Missing:* nothing until you want it — the C-items governed the artist pages this round and none of them asked for this. *Why it matters:* two shapes for one job is how the museum grows a second convention, and the day to reconcile them is before there are six artists rather than after. | W1 · COMBINED BRIEF | OPEN | Ops | 2026-08-06 |
| <a id="m106"></a>**M106** | **CONTRIBUTE IS BURNED DOWN, NOT REBUILT, AND WAITING ON YOU.** *Known:* F6, your words — *"it waits on a Mike prompt. Leave the album and its three tracks empty and honest."* The album and its three tracks (Just help a little · Financial help · Operational help) exist and each says the one true thing there is to say, in the register's own vocabulary, declared once. *Missing:* the prompt. *Why it matters:* it is the only album in the museum that is deliberately empty by instruction, and it will read as an oversight to anyone who does not know that. | F6 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m107"></a>**M107** | **THE KEEPER'S ANSWER LEFT /wb's ARTIST CARD.** *Known:* W1 called the old card useless and burned it down; *"Who keeps this place?"* went with it, because it is an answer about the HOUSE printed under an ARTIST's name. It is not deleted from the museum — it is at `/booth`, which is its room, and D1's hoist into `house-copy.js` is why removing it here was one deleted import rather than a decision about which of two copies was real. *Missing:* one word if you want it back. *Why it matters:* P9 put it there deliberately, and it is one of the best passages in the building; Ops removing it on a reading of "useless" is a call you should get to reverse. | W1 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="m108"></a>**M108** | **THE GUEST BOOK'S BUDGETS ARE 88 CHARACTERS AND 32, AND THEY ARE THE NARROWEST DISPLAY'S CAPACITY.** *Known:* L1 deleted the ellipsis and enforced the length instead, in two places (`maxLength` and a `slice` in `src/worker.js`). 88 is two lines of Courier Prime at 0.72rem across the 310px row a 390px viewport produces; 32 is what fits on line one beside the longest date this formatter prints. The old limit was 280 into a box that displayed about 88. *Missing:* your word if 88 is too short — it is a real constraint on *"what brought you here?"*. *Why it matters:* a longer budget is not a bigger number, it is a taller row: three lines instead of two, which changes the height of the book on the lobby. | L1 · COMBINED BRIEF | OPEN | Mike | 2026-08-06 |
| <a id="h-a"></a>**H-a** | **THE FRONT DESK'S PICTURE IS GONE AGAIN, AND M29 RE-OPENS BY CONSEQUENCE.** *Known:* H2's pull-back took the family shot off `face.wbr.faq` — no Record entry delivers it — so the wing's landing face is a heading and eleven questions, which is exactly the state M29 closed. The Visual Hook Law's own recorded exception applies cleanly (a page whose own words are the hook needs no image), so this is not a defect; it is a fact about the wing today. *Missing:* nothing until a Record entry delivers a photograph, at which point that face is the obvious place for it. *Why it matters:* four faces in the robots wing lost their still in one round and none of them is broken — but the wing now has ONE photograph on it in total, and that is a design consequence of the rule rather than an accident of it. | H2 · THE PORTAL HOLD | OPEN | Mike | 2026-08-06 |
| <a id="h-b"></a>**H-b** | **`assets-declare.mjs` HAS DRIFTED 45 ROWS FROM THE FILE IT WRITES, AND ONE OF THE TWO HAS TO BE THE SOURCE.** *Known:* M99 recorded five drifted rows as a hazard; this round moved 28 pictures behind the door and the drift is now forty-five — every artist cover, every Foundation sleeve, the house mark, every held plate. A `--write` would have deleted them all in silence, and now REFUSES to (the guard is built, which is what M99 asked for). *Missing:* one word: fold the 45 declarations back into the generator's array, or retire the generator and let `provenance/assets.json` be the source. *Why it matters:* a generator that cannot be run is not a generator, and the guard makes the drift permanent-but-safe rather than fixed. | H2 · THE PORTAL HOLD; grows [M99](#m99) | **CLOSED [C1 2026-08-06]** — **answered by the round rather than by a preference. THE GENERATOR IS THE SOURCE.** V1 settled it: the data declares a governed picture at its PUBLIC address and `src/lib/placement.js` computes the stage prefix, so the declarer's array was right all along and H2's 28 hand-edited `/held/…` rows were the drift. Folding them back deleted NOTHING — the same twenty-eight pictures at the other address. The guard is untouched in substance and still refuses a genuine deletion; it learned only that a picture has two addresses. | Mike | 2026-08-06 |
| <a id="p-a"></a>**P-a** | **THE NAMEPLATE HAS THREE STAMPED FIELDS AND TWO OF THEM ARE EMPTY.** *Known:* H3a built the badge to your UNIVAC references — chrome bezel, black field, raised letterforms, and MODEL NO. / SER. NO. / DATE struck into a lighter recess. `MODEL NO.` reads TYPE 8p, which the plate already said. *Missing:* a serial and a date, or your word that the fields stay blank. *Why it matters:* Doctrine 12 forbids the obvious alternative — a plausible serial on a maker's plate is invention on the one object whose whole job is to be specific. The plate renders the wells empty, which is what an unstamped plate looks like. | H3a · THE PORTAL HOLD | OPEN | Mike | 2026-08-06 |
| <a id="p-b"></a>**P-b** | **YOU NAMED A ROW CALLED PORTAL AND OPS CHOSE WHAT STANDS BEHIND IT.** *Known:* H3c gave the album three tracks — PORTAL · Portal Feed Controller · FAQ — and said nothing about the first one's content. A row that opens nothing is the dead control Doctrine 11's corollary forbids, so Ops made it THE DOOR: it opens the portal as it stands, with no feed selected, while the latch one row down keeps its own job (it opens the feed the drum is rolled to, and only when the instrument is armed). *Missing:* your word if you meant something else. *Why it matters:* it is one field to change and nothing else in the album moves — but it is Ops deciding what a room contains, which is your call and is flagged rather than absorbed. | H3c · THE PORTAL HOLD | OPEN | Mike | 2026-08-06 |
| <a id="p-c"></a>**P-c** | **`/admin` STOPPED BEING "HELD" IN THE LEDGER, AND NOTHING ON THE GLASS CHANGED.** *Known:* the new reachability check faults any HELD row that says how a visitor reaches it, and `route.admin` said both — HELD, and *"by URL only"*. `/admin` is a real page anybody who types it opens in full, and it has to stay that way because it is the door the held wings are opened THROUGH. So the row is REVEALED with `shown: false`, which is the honest reading of the two axes. *Missing:* your word if you want the dashboard itself behind the same password the wings are. *Why it matters:* it is the one row in the table whose state changed without anything a visitor sees changing, and a state change nobody names is the kind that gets discovered a year later. | H1 · THE PORTAL HOLD | OPEN | Mike | 2026-08-06 |
| <a id="h-c"></a>**H-c** | **THE STOWED SHELF IS NOW EXERCISED BY NOTHING, AND `archiveUnit` BY TWO EMPTY WALLS.** *Known:* [C29](#c29) recorded that N2's `<details>` stow was live on exactly one surface — the mainframe's two-spread wall. H2 emptied both walls, so the mechanism now has no caller at all, and [C30](#c30)'s declarable unit noun is declared by two faces that draw no tiles. Neither is deleted: both come back the moment a Record entry delivers plates. *Missing:* nothing today. *Why it matters:* C29 and C30 are both "a container proved on one case"; they are now containers proved on none, which is worth knowing before somebody refactors either. | H2 · THE PORTAL HOLD; grows [C29](#c29), [C30](#c30) | OPEN | Code | 2026-08-06 |
| <a id="m36"></a>**M36** | ~~THE MGK-NIAC TRACK IS CALLED "THE NAME".~~ **CLOSED P2 BY DELETION** — you struck the track in total, so the collision it resolved no longer exists and neither does the judgement. Original text: **THE MGK-NIAC TRACK IS CALLED "THE NAME" AND THAT ONE STRING IS A JUDGEMENT, NOT A SUBSTITUTION.** Renaming the album to MGK-NIAC collided with its own first track, which was already called MGK-NIAC — the band, the tracklist's first row and the face's heading would all have read the same words at once. The track took its own `id`, its own subtitle and its own subject: THE NAME. Everything else in the Q3 sweep was a straight substitution; this was the round's one call on a visible label, and it is one edit back. | v52/Q3 | OPEN | Mike | 2026-08-05 |

---

## 2. ART PENDING — needs a camera or an artwork, not code

| ID | What it is | Where it came from | Status | Owner | Raised |
|---|---|---|---|---|---|
| **P1** | **The Record's evidence, photographed.** `.vp-fe-plate` is built and empty. The Record's real answer, and the one the visual-hook audit named. | VISUAL_HOOK_AUDIT · v43/E5 · v45/R5 #8 | OPEN | Mike | 2026-08-03 |
| **P2** | **The Manual's microfiche plates.** `plates: []`, with B8's shoot spec written into the face's own header (≥2400px long edge, whole page including margins, reel order = reading order, `label` + `date` per frame). | v43/E5 | OPEN | Mike | 2026-08-03 |
| **P3** | **`MV-HR-20260405-035`** — a good picture of Hunter Root trapped inside a Facebook screenshot. A second plate if re-captured clean. | v43/E5 | OPEN | Mike | 2026-08-03 |
| **P4** | **A photograph of the MGK-VIII to serve as its cover.** See M8 — the current one is keystoned and cannot be edited straight. | v49/A8 | OPEN | Mike | 2026-08-04 |
| **P5** | **Sixty entries are sixty hooks** against a wing that owns eight photographs. Record 013 met the hook law by borrowing a plate; that does not scale. | v45/R5 #8 · v46/C4 #11 | OPEN | Mike | 2026-08-04 |

---

## 3. CODE — buildable, museum repo

| ID | What it is | Where it came from | Status | Owner | Raised |
|---|---|---|---|---|---|
| **C1** | **Record pagination at scale.** `shouldBand` turns on at ≥14 entries and >1 month; the Record holds one entry, so that machinery has never run. Sixty entries trip it on day one. | v45/R5 #2 | OPEN | Code | 2026-08-04 |
| **C2** | **A timeline thermometer.** *"8 of 11"* is a count, not a position. At sixty entries a reader needs to know where in the run they are standing. | v45/R5 #3 | OPEN | Code | 2026-08-04 |
| **C3** | **Filter and search over the Record's index.** The evidence classes already ride the rows; nothing lets a reader ask for only the corrections. | v45/R5 #4 | OPEN | Code | 2026-08-04 |
| **C4** | **A walk that can skip.** ‹ NEWER / OLDER › is strictly adjacent; the log's spine is its corrections and its evidence, which is not its chronology. | v45/R5 #5 | OPEN | Code | 2026-08-04 |
| **C5** | **Cross-references have no reverse.** The entry being pointed AT does not know it, and it is the one a reader arrives at. | v45/R5 #6 | OPEN | Code | 2026-08-04 |
| <a id="c-a"></a>**C-a** | **CLOSED &mdash; RULED KILL (Mike, K1 2026-08-07).** *"None are very good, and if that view is ever needed it gets reshot."* The ten operator plates (bench_power, chest_grille, feet_plinth, head_lens, head_oblique, limbs_lower, matrix_lit, parts_drawer, slot_mockup, torso_unfinished) are off disk, out of `provenance/asset-table.json` and out of `provenance/assets.json`. **The decision was put to him with the irreversibility stated and he answered it**, which is exactly the condition the standing precedent set: *a real photograph this museum owns is not deleted on OPS' word.* That precedent is not reversed &mdash; it is unchanged, and this was not Ops' word. **What the egg is left with is written into its own ledger row:** three plates upstream in the robots repo's culled 2021 set (the eye, the shoulder, the hand on the control), every one regenerable from `IMG_1526.MOV` because the crop rectangles are recorded. **The museum now holds nothing of the robot.** | C1 &middot; K1 | **CLOSED** | &mdash; | 2026-08-07 |
| <a id="c-b"></a>**C-b** | **CLOSED &mdash; the row is corrected, and the correction is a deletion rather than a copy.** `egg.niac.operator`'s `where` no longer claims plates in two repositories; it reads *"the physical world; three plates in weird-baby-robots/robots/mgk-viii/plates/2021-03-19"*, which is checkable and true. **AND THE ORIGINAL FINDING WAS RIGHT WITH ONE REFINEMENT WORTH KEEPING:** the robots repo held no copy of *those ten files* &mdash; and it does hold three DIFFERENT photographs of the same subject, cut from the 2021 build video under that repo's own obfuscation law. The egg's note always named them correctly; only the `where` field conflated the two sets. | C1 &middot; K1 | **CLOSED** | &mdash; | 2026-08-07 |
| <a id="c-c"></a>**C-c** | **CLOSED &mdash; RULED KILL (Mike, K1 2026-08-07), and this one cost nothing at all.** `structure-issue-p1.png` was **byte-identical** to `weird-baby-robots/robots/mgk-viiip/manual/structure/pages/page-01.png` &mdash; same sha256 (`cfac1d3b…`), same 100,077 bytes &mdash; one of sixty-one pages that repo generates from `tools/manual_structure_build.py`. The museum was holding a COPY of a page whose source is upstream and regenerable, so the delete removed a duplicate and not an original. **M4 does not close with it**: whether the plate is a render rather than a photograph of a print is a question about the upstream document, and it stays upstream. | C1 &middot; K1 | **CLOSED** | &mdash; | 2026-08-07 |
| <a id="c-d"></a>**C-d** | **ONE SENTENCE WAS CARRIED OUT OF "THE DEAL" AND IT IS OPS' JUDGEMENT.** Mike said burn and discard. Every claim on that face survives elsewhere except the undertaking that the artists' pictures *"come down the day any of them asks"* — an undertaking to four real people rather than a description of the room. It is the third question on `/wal`'s new FAQ, wording MOVED not rewritten. *Missing:* keep or strike; one line either way. | F2 · THE VISIBILITY RULE | OPEN | Mike | 2026-08-06 |
| <a id="c-e"></a>**C-e** | **THE ROBOTS FRONT DESK'S FOOTER CARRIED A [PAPA] AND THE FOOTER IS STRUCK.** F1 conformed the face to the booth's shape, which has no footer. The marked clause it carried is Mike's note to himself and is preserved here rather than in a field nothing prints: *the final wording throughout, and the answers should be shorter than these.* | F1 · THE VISIBILITY RULE | OPEN | Mike | 2026-08-06 |
| <a id="n-g"></a>**N-g** | **THE MGK-NIAC SPEC SHEET LOST SIX ROWS AND NOTHING WAS WRITTEN TO REPLACE THEM.** *Known:* Doctrine 18 — *"Technical Specifications means THE IN-STORY SPECS, NEVER THE REAL ONES."* Struck: the board part number, the program version + date + line count, the workshop validation state, the January-2026 bench sketches, the bench-board brightness cap (**whole**, because its caveat could not be saved without the fact it qualifies) and the source-file half of the DECLARED row. Nine rows survive and every one describes the object. *Missing:* **the unit's own particulars — what an ABEAL spec sheet for a 1945 engine says.** It is drafted (robots repo, Section II) and porting it is authoring, which is Doctrine 12's line. *Why it matters:* the face is honest and thinner; every figure that left is preserved in `provenance/register.json` and in the round log. | N2 · NIGHT ROUND | OPEN | Mike | 2026-08-06 |
| <a id="n-h"></a>**N-h** | **THE MGK-VIIIp FACE NOW CARRIES NO SPECIFICATIONS AT ALL, AND IT RESOLVES M92 IN THE DIRECTION NOBODY CHOSE.** *Known:* its four register lines were the real source trees (two of them literal filenames carrying 2024-07-21 and 2026-07-24) and its ON FILE entry's subject was a repository. All struck. The previous round left this face alone on a scope argument — *"he did not read it this round"* — and reported the divergence as [M92](#m92)/N-f; the standard is global, so the argument is spent and **both** faces lose their real-build register. *Missing:* the same thing [N-g](#n-g) needs. *Why it matters:* the blurb, the lit glass and the WHY entry survive, so the face still says what the firmware IS — it no longer says what it is made of. | N2 · NIGHT ROUND | OPEN | Mike | 2026-08-06 |
| <a id="n-i"></a>**N-i** | **THE IN-STORY MANUAL'S SPECIFICATION SECTION IS SOURCED FROM THE REAL ARDUINO FIRMWARE, AND IN ONE ROW THE FIRMWARE WINS A CONTRADICTION AGAINST THE MANUAL.** *Known:* `weird-baby-robots/docs/MANUAL_STRUCTURE_FIT-20260805.md` §Section II marks six rows `FITS` with `FW` cited — `0x3C`, `setRotation(2)`, `BMI270/BMM150`, `PDM.begin(1,16000)`, `DFPlayer Mini on Serial1 at 9600`, `NUM_PIXELS = 2 on D6` — and row 16 overrules the in-story manual's *"one lamp"* in the firmware's favour. Under Doctrine 18 that is backwards: the manual is the authority for the in-story spec and the firmware is not evidence about a 1965 machine. *Missing:* a ruling on which source wins when they disagree, and whether Section II is re-derived. **Nothing in that repo was changed on Ops' word.** *Why it matters:* the manual is one authoring pass from printing an I²C address on a 1965 spec sheet, and `instory:gate` does not reach that repo. | N2 · NIGHT ROUND | OPEN | Mike | 2026-08-06 |
| <a id="n-j"></a>**N-j** | **`/hr`'s PLAYER BAR HAS NO ROOM FOR THE TRACK NAME AT 390px, AND FIVE VARIANT CHIPS ARE CLIPPED TO 41%.** *Known:* the first phone-width lap of that wing, ever. `div.pb-track` measures **`clientWidth: 0`** against 110px of content — the title is not narrow, it is absent. Five `b.tl-tt` chips measure 134–230px of text in 62–115px of box; they carry `text-overflow: ellipsis`, so the truncation is honest, but *OFFICIAL LYRIC VIDEO* in 94px is not a label. Page-level horizontal scroll is **zero** and there are no console errors. *Missing:* a phone layout call for the bar and the chips — which is a design pass, not a mechanical fix. *Why it matters:* the wing is behind the password, so this costs a visitor nothing today and costs Mike every time he opens it on a phone. | N5 · NIGHT ROUND | OPEN | Mike | 2026-08-06 |
| <a id="n-k"></a>**N-k** | **THE TWIN PRINTS 76 OPERATOR SLOTS ON ITS OWN GLASS AND NOTHING GOVERNS THEM.** *Known:* `public/held/robots/twin.html` is a 620 KB standalone emulator — not a module, not built, outside the React render seam. It renders `[PAPA - ARIES]`, `[PAPA slot 1 - traffic]`, `[PAPA - wording slot]` and 73 more, by its own declared content law. The museum's scrubber, this round's launch strip and the provenance boundary all stop at that file's edge. It is unreachable at LAUNCH for an unrelated reason — the stage door refuses `/held/` — so **no visitor can reach it, and that is the only thing standing between those slots and the glass.** *Missing:* whether the twin's own placeholders get an unmistakable treatment (N3's rule, applied inside somebody else's artifact), or whether the stage door is accepted as the whole of the answer. *Why it matters:* the day the twin is delivered, N3's guarantee does not travel with it. | N3 · NIGHT ROUND | OPEN | Mike | 2026-08-06 |
| <a id="k-a"></a>**K-a** | **`assets:orphans` CANNOT SEE AN UNJUDGED ORPHAN, AND THREE ARE SITTING IN THE TABLE.** *Known:* the tracker built this round joins every addressable asset row to the delivery rule, and it found **three rows with `missing:true`** &mdash; `images/foundation/faq-cover.png`, `robots/art/portal-cover.png` and `robots/reference/mgk-viii/cabinet_whole.jpg`. `npm run assets:orphans` reports **0**, correctly by its own definition: it counts `missing && isJudged`, and none of these three carries a `what`, a `quality` or a verdict. **Two of the three are §8's two-addresses hazard in its quietest form** &mdash; they are the PUBLIC-side twins of pictures that moved behind the stage door, so the same photograph is in the table twice, once at each address, and the held copy is the live one. *Missing:* whether the orphan check should report unjudged missing rows in a second count, and whether a governed file's two rows should ever have coexisted. *Why it matters:* an instrument whose output is a DELETION LIST that reads 0 while three rows have no file is not wrong, it is narrow &mdash; and a future session reading the 0 will believe the table is clean. **Nothing was deleted; the new tracker draws them `no file` and excludes them from every reach count.** | K3 &middot; DICTATION PREP | OPEN | Code | 2026-08-07 |
| <a id="k-b"></a>**K-b** | **THERE IS NO AUTHORED STORY OUTLINE FOR WEEK ONE, AND THE INSTRUCTION ASSUMED THERE WAS.** *Known:* K5 asked for *week 1 as it stands* &mdash; the week's headline, each day's headline, each day's topics with their weights. **Both repositories were searched and none of it exists:** no day-by-day document, no data file, no `weight` field on anything story-shaped, no week headline. What exists is the transfer arc (weekly, and about how material ARRIVED rather than what a day is about), the three-act story arc (about the whole product, naming no days) and one Record entry. *Missing:* the content &mdash; which is yours, and is the thing the dictation produces. *Why it matters:* **the frame was built and every content slot left empty on purpose.** Five plausible day headlines would have been unmistakable tomorrow and unattributable next week, which is the one failure mode your own instruction named. `docs/dictation-20260807/week1.html` prints the finding at the top of the page rather than burying it. **[W1 2026-08-07] AN OUTLINE EXISTS NOW AND IT IS OPS', WHICH IS A DIFFERENT THING FROM AN ANSWER.** On your instruction Ops structured what you said aloud on 2026-08-02 into `reveal/week-one.mjs` &mdash; a week headline, the weekend it is named after, five days with topics, the Friday formula and five standing rules for the Record &mdash; and the page renders all of it **on the blue rail, none of it quoted, every gold slot still empty.** The finding above is unchanged: **there is still nothing in either repository authored by you**, and no `weight` field on anything story-shaped, so the topics are lists and not a ranking. What K-b now has is a working draft to argue with. *Still missing:* your own words in the gold slots, and a weighting if you want one. | K5 &middot; DICTATION PREP &middot; W1 | **IN PROGRESS** | Mike | 2026-08-07 |
| <a id="k-c"></a>**K-c** | **THE SPEC SHEET N-g AND N-h BOTH ASK FOR IS ASSEMBLED AND IS NOT WRITTEN, WHICH IS THE WHOLE DISTINCTION.** *Known:* `docs/dictation-20260807/specsheet.html` carries every piece of story-generated technical data about both machines from both repositories &mdash; grouped as a period one-sheet, set in the typed-page register, each row marked ASSERTED / IMPLIED / CONTRADICTED / ABSENT with its source. **18 rows are contradicted and both readings print; 20 positions are absent and say so; 8 rows are flagged REAL-BUILD SOURCE** under Doctrine 18. *Missing:* the authoring &mdash; choosing between the readings and filling the absences, which is Doctrine 12's line and is yours. **N-g and N-h stay OPEN**; what changed is that neither is now blocked on finding the material. *Why it matters:* twelve of those rows are places where writing the row IS taking a decision, and the sheet gathers them in one list so a dictation pass knows when it is deciding rather than describing. | K2 &middot; DICTATION PREP | OPEN | Mike | 2026-08-07 |
| <a id="k-d"></a>**K-d** | **THE MANUAL'S OWN SECTION STRUCTURE WAS OFFERED AS THE SPEC SHEET'S GROUPING AND DOES NOT SERVE &mdash; ANSWERED, AS THE INSTRUCTION ASKED.** *Known:* you named it as a candidate and said to say so if it did not serve. The structure issue is twelve sections and eight appendices describing a whole operating and maintenance manual, and **ten of the twelve are procedures**; the specification is Section II plus pieces of VII and XII. Grouping a ONE-SHEET by the manual's twelve puts nine near-empty headings on a page whose only virtue is that it is one page. **What is used instead is the period specification's own grouping** &mdash; the shape Tables 2-1 and 2-2 already have &mdash; and **every heading carries its manual paragraph position**, so an authored row lands where the structure says it goes and nothing needs re-deriving. The structure is the destination; it is not the arrangement. *Missing:* nothing &mdash; recorded so the reasoning is not re-argued. | K2 &middot; DICTATION PREP | **ANSWERED** | &mdash; | 2026-08-07 |
| <a id="w-1"></a>**W-1** | **THE ONE ENTRY THE RECORD HOLDS IS NOT ABOUT THE TRANSMISSIONS, AND THE WEEK-ONE OUTLINE OPENS ON IT.** *Known:* day 1 of the outline says the wing opens holding one entry, and **that is true of the tree today** &mdash; the Record holds exactly one. But it is **entry 013**, and its subject is a physical delivery: a sealed modern bag, a USB-C adapter, a unit on charge (`reveal/record-entries.mjs`). Nothing in it touches a transmission, a manifesto or a weekend. *Missing:* which of three readings you want &mdash; **day 1 opens with the entry that is there, or that entry is re-dated, or day 1 adds one.** Three different weeks; all three are yours. *Why it matters:* it is the only one of the outline's five checks against the tree that does not agree, and **`M19 &mdash; what a record number means` is already open**, which is exactly the question "opens with one entry" numbered 013 asks. Printed in red at the foot of `week1.html`, named and deliberately unresolved. | W1 &middot; WEEK ONE OUTLINE | OPEN | Mike | 2026-08-07 |
| **C6** | **The `[[n]]` door-marker syntax is unpoliced.** A marker with no matching door renders nothing and drops the door at the end — the right failure, and it wants a build-time check. | v45/R5 #7 | OPEN | Code | 2026-08-04 |
| <a id="c7"></a>**C7** | **The v45 inline-door container is exercised nowhere.** All four doors were anchored to material v47 deleted. Correct machinery, no data. | v47 exposure #3 | OPEN | Code | 2026-08-04 |
| **C8** | **`Exhibit.jsx` threads `epoch={face.recordEpoch}` and the Record declares none.** Correct machinery with no data; it works the moment M19 is answered. | v47 exposure #8 | OPEN | Code | 2026-08-04 |
| **C9** | **The `/robots` contents column is 76% blank paper** since the photo strip came off. Reported, unfilled by ruling. | v47 exposure #6 | OPEN | Code | 2026-08-04 |
| **C10** | **The Record's closed face has no visual hook** — a heading and one row of type. A live conflict with the standing Visual Hook Law, resolved in favour of Mike's H1 ruling. | v47 exposure #2 | OPEN | Code | 2026-08-04 |
| **C11** | **DECK-SCROLL-OCCLUSION** — the player bar hides the deck's bottom. Confirmed reproducing 2026-06-17. Minor, consistent. Piggyback when deck work opens the file. | STATE known issues | OPEN | Code | 2026-06-17 |
| <a id="c12"></a>**C12** | **DECKBUG-FBBLOCKS** — an FB embed renders as a black/white block. **[v53/B1] SEEN, AND STILL NOT CONFIRMED, WHICH IS THE HONEST STATE.** On `/hr` at desktop width the artifact grid is seventeen `hr-card-fbembed` cards, each 706px tall, whose plugin iframes do not paint — a run of empty rectangles below the player bar, which is exactly the shape this row describes. **It is NOT marked confirmed because the lap ran on `localhost`**, and Facebook's plugin endpoint may simply refuse that origin; the sixteen requests it makes all return, but nothing is drawn. What would settle it is one look at `/hr` on `weird.baby`. If it reproduces there it is a defect; if it does not, it is a localhost artifact and this row closes. Either way [M37](#m37) may remove the embeds and take the question with it. **[R6 2026-08-06] AND [M37](#m37) WILL NOT REMOVE ITS SUBJECT — HE RULED A, DISCLOSE.** The seventeen cards stay, so this row stops being a question that might dissolve and becomes a defect somebody has to look at. **One measurement was added this round and it narrows the repro:** the frames are `loading="lazy"` and do not request anything until they scroll into view; scrolled to the bottom on the built bundle, all sixteen requests return and nothing paints — same shape as v53, same localhost caveat. **What would still settle it is one look at `/hr` on `weird.baby`.** | STATE known issues; observed v53/B1; narrowed R6 | OPEN | Code | — |
| **C13** | **Video-panel YT-thumb fallback** is an unclassed full-bleed `img`. Mostly moot since albums carry art. | STATE known issues | OPEN | Code | — |
| <a id="c14"></a>**C14** | ~~`jesse-welles-plate.jpg` is a WebP named `.jpg`.~~ **DONE (v51/A7)** — renamed to `.webp`, its one reference updated in `worth-a-listen.js`, and its declaration in `provenance/assets-declare.mjs` with it. File-integrity mismatches in the asset table: 3 → 2, and the two that remain are unreferenced orphans inside M9. **The rename exposed C32.** | v49/A5; closed v51/A7 | DONE | Code | 2026-08-04 |
| <a id="c15"></a>**C15** | ~~Collage tile labels are not scrubbed for `[PAPA]`.~~ **DONE (v51/A7)** — `scrubFace` now scrubs every tile's `label` and `date`, on `face.collage` and on every spread's `tiles`. A tile whose caption is entirely the operator's **keeps its picture and loses its words**, which is the rule the spread heads already set and which also protects a tombstone that counts its plates out loud; the caption strip renders only when there is something in it. Proved live by writing a marker into one tile's label: 8 tiles still rendered, that tile printed its date alone, and `[PAPA]` appeared nowhere on the page. | v49/A3; closed v51/A7 | DONE | Code | 2026-08-04 |
| **C16** | **The image archive's two siblings are defined and not built — and they now carry the plain names too:** VIDEO ARCHIVE and AUDIO ARCHIVE, the same component with different data (THE REEL / THE TAPE LIBRARY retired with THE MORGUE). Neither has content: every `videos:` array in the robots wing is empty and the wing has no audio. `/wb` has six tracks and no archive face, which is where an audio archive would land first. **Mike's N2 instruction was "build only what has content", so nothing was scaffolded.** | v49/A3 · v50/N2 | RULED-AWAITING-BUILD | Code | 2026-08-04 |
| <a id="c29"></a>**C29** | **[v56/R4] IT GOT WORSE: THE STOWED SHELF IS NOW EXERCISED BY NOTHING AT ALL.** MGK-NIAC was the ONE wall anywhere with more than one spread, and its FEBRUARY 2013 spread was three plates of the ROBOT — struck under Mike's mainframe canon along with everything else showing the figure. The wall is one unheaded collage now, so N2's `<details>` mechanism has **zero** live callers. That is the same shape as C1's pagination and C7's doors, and it is now the most extreme case of it in the tree: **a mechanism with no data is at least exercised by a test; this one has neither.** Original text: | v50/N2 · worse v56/R4 | OPEN | Code | 2026-08-04 |
| **C29 (orig)** | **The stowed shelf was built and only one wall in the museum had more than one spread.** MGK-VIII stacks MARCH 2021 (open, 5 plates) over FEBRUARY 2013 (stowed, 3 plates); the MGK-VIIIp wall is one unheaded spread and renders the DOM it always did. The mechanism is therefore exercised on exactly one surface, which is a container proved on one case again — the same shape as C1's pagination and C7's doors. | v50/N2 | OPEN | Code | 2026-08-04 |
| **C30** | **The archive's unit noun is declarable and only the robots wing declares it.** `face.archiveUnit` gives the stowed-shelf count its word; the default is "image(s)". A video or audio archive will want its own, and nothing yet forces the question. | v50/N2 | OPEN | Code | 2026-08-04 |
| **C31** | ~~DOC CONTROL announces no mechanism, and three of its four rows describe things a visitor cannot reach.~~ **CLOSED BY DELETION v56/R3.** Mike struck the face. Its two rows that said something no other face says — the manual came in pieces, and the originals are held and not published — are FAQ answers now, with their `[PAPA]` markers intact; the other two (PAGES, FILES) were deleted because The Manual's own face and both Technical Specifications faces already say them better. Original text: | v50/N3; closed R3 · v56 | DONE | Code | 2026-08-04 |
| **C31 (orig)** | **DOC CONTROL announced no mechanism, and three of its four rows described things a visitor cannot reach.** The manuals are unimaged, the originals are unpublished, the firmware trees are named but not readable from the glass. That is honest and it is also a page of holdings statements with no object behind any of them except the stamp card. If the reel ever fills (P2), this face is where the door belongs. | v50/N3 | OPEN | Code | 2026-08-04 |
| <a id="c32"></a>**C32** | ~~**THE ASSET TABLE IS KEYED BY PATH, SO A RENAME SILENTLY DROPS EVERY JUDGEMENT ON A FILE.**~~ **DONE (v52/R3), and the fix caught the case that raised it plus one more on the first run.** Found by doing one: renaming `jesse-welles-plate.jpg` to `.webp` produced a fresh row with `what`, `quality`, `qualityNote`, `verdict` and `revealArc` all null, and the old row vanished with the old path. Nothing warned. That file's verdict happened to be unset, so nothing of Mike's was lost this time; the next rename may not be so lucky, and the Record Approval Gate would then report a pass over a row nobody had inspected. `provenance/README.md` §4 already states the sibling hole — an approved picture can be REPLACED under its own verdict — and this is that hole's other half. A `--scan` that carried judgements across an unambiguous rename, or that refused to drop a judged row in silence, would close it. **BOTH were built, and a third thing that matters more than either: `uid` is minted once and never rewritten so a judgement hangs off a name rather than an address; `sha256` resolves a pure move automatically; and where NEITHER can answer — a rename that also re-renders the file, which is the ordinary case because the name is usually IN the picture — the scan REFUSES TO GUESS and reports the judged row under its own banner, with `--rename` as the explicit human declaration. The silence was the defect; a hash only makes it rarer.** The first run reported two orphans: `jesse-welles-plate.jpg` (C14's own rename, still stranded) and `mgk-viii-cover.png` (this round's). Both carried across; 255 rows to 253. | v51/A7; closed v52/R3 | DONE | Code | 2026-08-04 |
| <a id="c33"></a>**C33** | **THE ADMIN DASHBOARD wants three things**, backlogged on Mike's instruction and none of them urgent: exclude Mike's and Ops' own hits from the counts; add a return-visitor signal, or a coarser same-area proxy; and extend the logbook to hold **at least a week**. The second has a constraint attached that the other two do not — `/booth`'s privacy answer states that three columns are the whole of what the site records, so a returning-visitor signal either fits inside that claim or the claim changes first. | Mike's A6 brief · v51 | OPEN | Code | 2026-08-04 |
| <a id="c34"></a>**C34** | **THE YOUTUBE PLAYER MOUNTS ON ROOM LOAD, NOT ON PLAY.** `Exhibit.jsx:333` injects `iframe_api` and the player mounts an embed with it, so `/robots`, `/wal`, `/wb` and `/hr` each make three requests to `www.youtube.com` for a visitor who never presses anything. A click-to-load facade — a still and a play button that mount the real player on the first press — removes the request entirely for everyone who only reads the room, and costs one interaction for everyone who plays. It is the middle option in [M37](#m37) and it does not need that ruling first: it is strictly less outbound traffic for the same behaviour. **[R6 2026-08-06] CLOSED — RULED AGAINST.** Mike ruled [M37](#m37) option A: *"disclosed, not deferred, not click-to-load."* This row IS option B and it does not get built. It was Ops' recommendation and it was wrong to keep recommending it once he had chosen; it is closed rather than parked so that no future round reads a standing recommendation as an unbuilt task. **The three YouTube requests per music room stay, and the booth's answer names them.** | v53/B1; ruled against R6 · THE PORCH RULINGS | **DONE** | Mike | 2026-08-05 |
| <a id="c35"></a>**C35** | ~~`/admin` IS UNUSABLE AT A PHONE WIDTH.~~ **DONE (P5).** The header was `space-between` with **no wrap**, so a row of fixed-size controls longer than its container ran off the edge of a document that does not scroll sideways. **`flex-wrap` on both rows is the whole fix, and a media query would have been the wrong one** — the failure is not a width, it is a row longer than whatever it is put in; `flex-wrap` is true at every width and a breakpoint is only true at the one somebody measured. The inline flex became `.adm-controls` in the same edit. **Measured at 386px: all five controls in view, `allReachable: true`, no horizontal scroll** (was: only `/hr` and `/cb`, with the other three clipped past x=633). [C33](#c33)'s three admin wants are untouched. | v53/B1; closed P5 · 2026-08-05 | DONE | Code | 2026-08-05 |
| <a id="c36"></a>**C36** | **`/wal`'s TITLE BAR HAS 6px OF AIR AT 390px, AND IT IS THE FLOOR.** Measured between the text boxes: `Weird.Baby` ends at 91, `Worth A Listen` starts at 97, `Gift Shop` starts at 241 — six pixels either side of the room name. Nothing collides today. The other rooms measure `/booth` 9 · `/wb` 24 · `/foundation` 26 · `/robots` 42. **`Other Music Worth A Listen` is already the longest name the board carries and the bar is already shortening it**, so there is no headroom left: the next room with a long name collides rather than tightens. The bar wants a narrow-width rule (shorten, wrap, or drop the brand) before that happens, not after. | v53/B1 | OPEN | Code | 2026-08-05 |
| <a id="c37"></a>**C37** | **`/wb`'s TITLE BAR PRINTS "Weird.Baby" TWICE**, 24px apart — the brand at x=16 and the room name at x=115. Correct by construction (the room IS Weird.Baby Music and the bar shortens the name), and at 390px it reads as a stutter rather than as two different things. One word either way. LOW. | v53/B1 | OPEN | Mike | 2026-08-05 |
| <a id="c38"></a>**C38** | **`doc.record.evidence` IS ONE LEDGER ROW FOR A PER-ENTRY THING, AND [v55/R1] JUST MOVED EVERYTHING AROUND IT TO PER-ENTRY.** The Record's own entries are now `record.NNN` rows carrying their own assets — 013's plate joined the asset table automatically the moment the granularity changed. `doc.record.evidence` (the built-and-empty `.vp-fe-plate` container, P1) did not move: it is still one row saying *the Record's evidence is unphotographed*, which will be true and useless once three entries have photographs and four do not. **It is not urgent and it is not wrong today** — with one entry, one row is the right cut. It becomes wrong at the second entry that carries evidence, and the fix is the same shape R1 already built: derive it per entry. Recorded now because the round that would naturally have done it is the round that just walked past it. | v55/R1 | OPEN | Code | 2026-08-05 |
| <a id="c40"></a>**C40** | **THE POKE — RAISED BY MIKE, GRADED A+++++, DELIBERATELY NOT BUILT.** *"Pixel-perfect tap on the Weird.Baby logo's EYE and he BLINKS, as if poked. Three pokes in a row and he wears SAFETY GLASSES for the rest of the session."* Ledgered as `egg.lobby.poke` — NOT_BUILT · HELD · **`shown: false`**, which must stay false: the moment any surface hints at it, it stops being an egg and becomes a debt. **The grade's reasons ARE the specification and they are written into `reveal/ledger-declare.mjs`:** it is found by doing something nobody is told to do · it costs nothing on the glass (zero words, zero controls — the strongest position an addition can occupy under the Law of Subtraction) · **the escalation is the part no other egg in the table has** (all thirteen others are a single state; this one answers back on the third try, and the second reward is a joke about the first that only the person who caused it can read) · it sits on the one object every visitor meets. **WHAT IT WAITS ON IS ART, NOT CODE:** a closed-eye state of the mark and a glasses state, both Mike's — the mechanism after that is a hit region, a three-count and a session flag. **One constraint already checked so nobody finds it late:** *"for the rest of the session"* means browser storage, which `/booth`'s privacy answer already covers exactly (the machine remembers you, the museum does not; no cookie travels). **Building it with a server round-trip would change that answer, and that is the version not to build.** **[M1 2026-08-05] AND IT IS NOW THE GATE ON EVERY FUTURE EGG.** Mike, reading the lobby: *do not bother with a next-level egg until this one at least catches on.* Recorded above the row in `reveal/ledger-declare.mjs` as well, because it is a rule about the whole egg table and not about this row. **Two things follow that are worth stating rather than inferring.** It is a condition on a thing that does not exist yet, so it cannot be met by BUILDING the poke — it is met by the poke being FOUND, by somebody nobody told. And **the museum cannot see that today**: `/admin` counts visits and the guest book, no egg in the ledger reports being tripped, and `egg.replay` — the one a visitor can already trip — has never been instrumented either. So the condition is met by your word rather than by a number. **That is not a proposal to build analytics**; `/booth`'s privacy answer is why it would be a bigger decision than it looks. | Mike's P4 · 2026-08-05; condition M1 · 2026-08-05 | OPEN | Mike + Code | 2026-08-05 |
| <a id="c41"></a>**C41** | ~~**`docs/SURFACING_LOG.md` DATES ITS ROWS IN UTC WHILE EVERY OTHER DATE IN THIS REPOSITORY IS LOCAL.** This round's reading was taken at 21:2x local on 2026-08-05 and logged as **2026-08-06**. The log is a TREND file whose whole value is that the rows line up with the packets that produced them, and a row a day ahead of its own commit is a row that will be read as a different day's reading. **Small, real, and deliberately not fixed inside a round that was reading it:** the file's own header says *do not hand-edit — a measurement somebody adjusted is not a measurement*, and quietly re-stamping a date while using the tool is the shape of exactly that. The fix is one line in `tools/surfacing.mjs`; the row already written stays as it is.~~ **DONE (D8, CLEAR THE DECK).** `new Date().toISOString()` → `new Date().toLocaleDateString("sv-SE")`, which gives ISO ordering off the LOCAL clock. **The rows already written are NOT touched** — the two UTC rows stand as they were recorded and the comment in the tool is why the ones after them differ. Landed by a round that was not reading the tool, which is the condition the row set. | READING PASS ROUND ONE | **DONE** | Code | 2026-08-05 |
| <a id="c39"></a>**C39** | ~~`tools/asset-table.mjs` CONTAINS TWO LITERAL NUL BYTES.~~ **DONE (P5), AND IT WAS NEVER ONE FILE.** A scan of every `.mjs` in `tools/` and `reveal/` found **FOUR files carrying six bytes** — `asset-table.mjs` (2), **`menu-parity.mjs` (2, built at v56, one round AFTER this row was written)**, `provenance-sweep.mjs` (1) and `migrate-vocabulary-pass1.mjs` (1). **The third is the finding: it is `keyOf`, the function that hashes every visitor-facing string in the museum, so a grep for its name inside the provenance boundary's own source came back "binary file matches".** This round hit the defect twice while working — a grep for the parity tool's own code returned nothing, and then an `Edit` failed to match a line it had just read, **because the Read tool renders a NUL as a space.** Fixed as the row specified, with the two-character escape. **PROVED TO CHANGE NOTHING BY THE STRONGEST TEST AVAILABLE: `provenance:gate` still PASSES** — had `keyOf` changed value, every register key would have changed and every declaration would have gone undeclared in one run. **The lesson is the row's shape, not its content: it named an INSTANCE of a habit, so it would have been closed on the instance while the habit kept spreading.** Original text: | v55/R2; closed P5 · 2026-08-05 | DONE | Code | 2026-08-05 |
| <a id="c39-orig"></a>**C39 (orig)** | **`tools/asset-table.mjs` CONTAINS TWO LITERAL NUL BYTES, SO EVERY `grep` OVER `tools/` SILENTLY SKIPS IT.** They are deliberate — key separators written as raw `\0` characters inside two template literals (`mintUid`'s `${id}\0${hash}` and the rename matcher's `${repo}\0${sha256}`) — and the tool runs correctly. But git-grep and ripgrep both classify the file as binary and report *"binary file matches"* instead of the line, so a search for a function name in the asset table's own source comes back empty-handed. Found by doing it twice this round. **The fix is two characters: write them as the escape `\0` instead of the byte**, which is the same value to JavaScript and plain text to every tool that reads the file. LOW, and it touches a judged-field-carrying tool, so it wants its own commit rather than a ride-along. | v55/R2 | OPEN | Code | 2026-08-05 |
| **C17** | **Four pre-existing lint errors** (`WbAdmin.jsx:18`, `Exhibit.jsx:88/191/517`) each need semantic review, not a mechanical fix. | CLAUDE.md lint-debt table | OPEN | Code | pre-May 2026 |
| **C18** | **Gift shop wiring + Mike's own gift shop.** A full-launch gate. Gated on Mike bringing source. | STATE full-launch gates | OPEN | Code + Mike | — |
| **C19** | **Shirts and hats are not built.** The next durable merch work, storefront-agnostic. | STATE gift shop | OPEN | Mike | 2026-06-23 |
| **C20** | **`shop.weird.baby` custom domain** — deferred to launch; needs Big Cartel Platinum. | STOREFRONT DECISION | OPEN | Mike | 2026-06-23 |
| **C21** | **Substantially more content** before traffic is worth driving. The largest lever on the launch gate. | STATE full-launch gates | OPEN | Mike + Ops | — |
| **C22** | **Fact collection beyond the pilot 97**, plus the `hr_facts.js` unique-seed salvage. The largest remaining content lever on the FactScroller. | STATE NEXT #7 | OPEN | Ops | 2026-07-07 |
| **C23** | **Presets §8.1 phase 2** (mobile factory Show, peek-return chip) and the preset-as-artifact model. Not blockers. | `docs/UX_PRESETS_SPEC.md` | OPEN | Code | 2026-06-07 |
| **C24** | **The Kaleidoscope is mothballed** — built, never mounted, revives post-launch by Mike's choice. | STATE mothballed | OPEN | Mike | v28 |
| **C25** | **Brand-aligned aesthetic, the deliberately-untouched remainder:** variant-pill type colours, journal semantic green/red, per-album accents, mothballed palettes. Awaiting Mike's read. | STATE backlog | OPEN | Mike | 2026-06-07 |
| **C26** | **Backups: periodic re-mirror + the quarterly restore drill** (charter 3.4). `mirror_db_backups.ps1` makes the first a one-command step; the drill has never been run. *A backup nobody restored is a rumour.* | STATE backup status | OPEN | Mike | 2026-07-07 |
| **C27** | **Mobile UX, banner-match-nav and cover-pill render** need live narrow-width inspection by a person, not a measurement. | STATE cannot-verify | OPEN | Mike | — |
| **C28** | **All MediaVault-repo items** are unreachable from a museum session and need an MV-side pass. | STATE cannot-verify | OPEN | Ops | — |

---

## 4. THE ROBOTS REPO — `C:\AI\Projects\weird-baby-robots`

| ID | What it is | Where it came from | Status | Owner | Raised |
|---|---|---|---|---|---|
| **R1** | **Contract docs migration** — Numerical Envelope v1.0, Governance, BOM, UX — from OneDrive `MASTER FILES` into `robots/mgk-viii/contracts/`. The LANDING GATE applies. | robots STATE open issues #1 | OPEN | Mike | 2026-07-13 |
| **R2** | **WS0 bench compile + on-unit verify** of the curated VIIIp firmware head (Nano 33 BLE Sense — `Arduino_BMI270_BMM150` + `PDM`). | robots STATE #2 | OPEN | Mike | 2026-07-13 |
| **R3** | **Hash-verify the 11 VIIIp originals** against the MKR manifest. Sizes already match exactly; host-side pwsh. | robots STATE #3 | OPEN | Mike | 2026-07-13 |
| **R4** | **Retire or absorb `C:\AI\Projects\MGK-VIII\`.** Its viewer and libraries need a rule: gitignored here, or left in place. | robots STATE #4 | OPEN | Mike | 2026-07-13 |
| **R5** | **Confirm whether `Weird.Baby_Screen_Leak.MOV` is the Tal Avitzur video.** | robots STATE #5 | OPEN | Mike | 2026-07-13 |
| **R6** | **Twin game text formatting** — GAME OVER / score / front-card placement wants a pass. LOW, non-urgent. | robots STATE #11 | OPEN | Code | 2026-07-15 |
| **R7** | **Snow Globe needs general work** — drift/settle feel, snow density, shake response. Reads as a first cut. LOW. | robots STATE #12 | OPEN | Code | 2026-07-15 |
| **R8** | **PARKED — firmware 32-slot Answers menu vs the 31.4-unit fleet canon.** Revisit at the L3 / WS2 design gate. Do not work. | robots STATE parked | OPEN | Mike | 2026-07-14 |
| **R9** | **PARKED — the −03 character identity tangle** (CEO / Investor / Executive / "Stand Alone"). Revisit at L3. Do not work. | robots STATE parked | OPEN | Mike | 2026-07-14 |
| **R10** | **The RECONCILE list** (§B of `MARKUP_RULINGS+RECONCILE-20260718.md`) awaits Mike's porch-style cards. Accidental source conflicts get reconciled; contradictions are PLACED, never inherited. | robots STATE doctrine | OPEN | Mike | 2026-07-18 |

---

## 4a. THE ASSET TIMELINE — T1, 2026-08-05

The four transfer classes are built, all 152 rows are placed or exempted in
writing, and the rule *an asset may only be SHOWN after it has been TRANSFERRED*
is enforced by `reveal:build` and `reveal:check`. Full document:
`docs/ASSET_TIMELINE.md`. **None of these blocks anything.**

| ID | What it is | Where it came from | Status | Owner | Raised |
|---|---|---|---|---|---|
| <a id="ta"></a>**T-A** | ~~THE MANUAL IS 24 PAGES ON THE GLASS AND 61 IN THE BUILD.~~ **CLOSED v56/G1 BY A RULING THAT IS BIGGER THAN THE PATH. MIKE: the old 24-page manual is DEAD and its page numbering with it; 61 is the current number — and THE MANUAL IS AS LONG AS THE MANUAL NEEDS TO BE, AND NOT LONGER. Page count is a consequence of content, never a target.** So the fix is not a repointed string: `MANUAL_PAGES = 24` is **gone** and the count is now READ OFF THE SOURCE TREE every time it is asked for. A constant standing in for a fact about a document in another repository is exactly what this row was, and a constant cannot be kept in step by discipline. **Three failure modes now covered and each broken on purpose:** the tree moving reports one named fault rather than a stack trace; a SHRINKING manual is refused at write time AND faults any row already written past the end (that direction used to be invisible); a growing one needs nothing. The count prints on every `reveal:check` pass. **It also exposed a hole:** manual pages were declared PACKAGE in PROSE and enforced by nothing, so the vessel's own specimens fell through the transfer table — now one pattern rule, with a fault if a pattern ever overrules a hand-placed row. **And one thing on the glass:** the museum's manual plate was a page of the retired document; it is now page 1 of the live structure issue, and the one judgement that leaves is [M45](#m45). Original text: | T1 · 2026-08-05; closed G1 · v56 | DONE | Mike | 2026-08-05 |
| **T-A (orig)** | **THE MANUAL WAS 24 PAGES ON THE GLASS AND 61 IN THE BUILD.** *Known:* the museum's canon says a **24-page** manual, twice, where visitors read it (`face.viiip.manual`, `doc.manual`). The robots repo's typewriter pass (`4cd78ac`, 16:14 on 2026-08-05) **retired all 24 page renders** and replaced them with a **61-page structure issue** at `manual/structure/pages`. Museum v55 sealed at 13:38, before that, with the old path wired into `reveal/schema.mjs` — so the check has been dying on a Node stack trace ever since and nothing ran it in between. *Missing:* **which number is the object.** *Why it matters:* the manual-page vessel (M44) cannot name a page until it knows which document it is naming, and the gate stays red. **Ops did two things and refused a third:** the check now reports this as one named fault instead of crashing (a gate that crashes is not a gate), and the path was **NOT** repointed at the new render — page 7 of the 61 is not page 7 of the 24, because the structure issue renumbers everything, so repointing would make `doc.manual.page.07` quietly mean a different leaf. Same question the robots `STATE.md` already raised as *whether 61 pages is the right size for this object.* | T1 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="tb"></a>**T-B** | **WHICH FOUR OF THE FIVE FRIDAYS IN WEEKS 3–7 CARRY PACKAGES?** *Known:* four packages, window weeks 3–7. *Missing:* which of the five weeks goes empty. *Why it matters:* no PACKAGE row can carry an arrival week until then, and check (b) means a row with no named arrival can never be `REVEALED` — so all nine package rows are currently unshowable by rule. Ops assigned no week rather than guessing one (Doctrine 12). | T1 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="tc"></a>**T-C** | **WHAT DOES THE RECKONING ACTUALLY CONSIST OF?** *Known:* month 3 (weeks 9–12) is the reckoning, and the later transmissions are what reach it. *Missing:* what arrives in it. *Why it matters:* **the ledger's entire back half is six rows, and five of them are the same thing** — twin menu stubs reading DATA NOT LOADED (`advice.rest`, `predictions.rest`, `user.security`, `codes`, `mgkmodel`) plus one unbuilt set of advertising plates. Nothing in the 152 rows is *about* a reckoning: no conclusion, no consequence, no closing of the file, and the story arc's own Act I thrust — *who was the previous owner of this unit, and what happened to them?* — **has no row at all.** Related: across 152 rows the reveal `arc` field is set on nine, exactly one is `understood`, and **all nine are BLAST** — every stage the museum can express is carried by material that arrived before launch. | T1 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="td"></a>**T-D** | **DO THE NON-MGK ROWS BELONG IN THE TRANSFER MODEL AT ALL?** *Known:* the transfer classes are a fiction about one machine line's paperwork arriving. Worth A Listen, the shop and the Foundation's giving channels have no provenance fiction — the house makes or arranges them. Their LIVE counterparts (8 rows) sit in BLAST **by necessity**, because rule (b) admits no other answer for a thing that was on the glass on day one; the seven unbuilt ones are exempt. *Missing:* whether the museum wants a second, non-fiction provenance for house material, or whether exemption is the right permanent answer. *Why it matters:* **four promises already on the glass are backed by no transfer that can ever keep them** — `wal.artifacts` and the three unbuilt giving channels are `shown: true` and outside the timeline entirely. | T1 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="te"></a>**T-E** | **SHOULD DOCTRINE 12 AND THE ROBOTS REPO'S DRAFTING LAW BE RECONCILED?** *Known:* both govern invention and neither cites the other. **Doctrine 12** (museum `OPERATIONS.md` §7) governs **facts** — *where a fact is missing, Ops ASKS* — site-wide. **THE DRAFTING LAW** (robots `docs/JOBONE_CAPTURE-enigma-words-20260719.md` §10) governs **voice** — *assembles from established canon ONLY; no coined catchphrases, signatures, gags or terminology; all flavor is Papa's.* *Missing:* whether the Drafting Law is the voice specialisation of Doctrine 12's general form. *Why it matters:* **neither rule contains the other** — Doctrine 12 would permit a coined phrase whose underlying fact Mike supplied; the Drafting Law would forbid it, and says nothing about inventing a count. A round working across both repos is governed by whichever file it happened to read, which is the same failure the museum fixed at v55 for the ledger's validator. **Ops recommends one cross-reference in each file rather than a merge, and did not make it.** | T1 · 2026-08-05 | OPEN | Mike | 2026-08-05 |
| <a id="tf"></a>**T-F** | **FOUR `tool.*` ROWS ARE IN A CATALOGUE OF REVEALABLE THINGS AND ARE NOT REVEALABLE.** `tool.provenance`, `tool.assettable`, `tool.reveal` and `tool.openactions` are how the museum is *made*; Doctrine 11 keeps them off the glass permanently, so they are the only rows in the table that can never be spent. They are exempt from the transfer model for that reason. *Missing:* whether they belong in the reveal ledger at all. *Why it matters:* structural, low urgency — a catalogue whose failure mode is silence should not carry rows that are permanently out of scope. **Reported, not fixed.** | T1 · 2026-08-05 | OPEN | Ops | 2026-08-05 |

---

## 4h. CLOSED IN THE NIGHT ROUND (N1–N6, 2026-08-06)

| ID | What it was | How it closed |
|---|---|---|
| **N1** | The eleven kept photographs were a register row and nothing Mike could look at. | **DONE** — `C:\AI\Projects\_review\HELD-PHOTOGRAPHS-20260806\index.html`, all eleven with what each shows, verified byte-for-byte against `asset-table.json`'s sha256. **Outside the repository on purpose:** copies inside the tree would be eleven new rows for `assets:scan`, eleven new orphans for `--cull`, and a third address for a picture that already has two. |
| **N2** | "Technical Specifications" meant whatever the last round could verify. | **DONE, AND THE PROVENANCE REGISTER WAS THE EVIDENCE RATHER THAN THE DEFENCE.** Every struck row was classed `VERIFIED` with the source *"the unit's own `.ino` trees, read off the files"* — the boundary was working perfectly and recording in writing that a 1965 spec sheet came from an Arduino. **Doctrine 13 asks whether a string HAS an origin; it cannot ask whether the origin belongs to the fiction.** Law: Doctrine 18. Gate: `npm run instory:gate`, on every packet. Audit: 13 surfaces, 304 strings, `npm run instory:all`. |
| **N3** | `[PAPA]` markers were invisible to Mike and present in every public chunk. | **DONE, AND IT DOES NOT REVERSE P5 — it enforces P5 twice.** The notes are lifted OUT of the prose into a red NOT-UX block (`visitorProse` unchanged to the character, so the copy Mike reads is the copy that ships), and `wb-ops-notes` deletes the sentences from the SOURCE at launch. **35 markers in the built bundle → 1, and the one is the regex that removes them.** The strip is an AST pass because marker sentences straddle concatenated literals and a per-literal rule would have shipped different copy than Mike approved. |
| **N4** | Nobody had measured whether an expander disturbs the page above it. | **DONE — THE VERTICAL AXIS WAS ALREADY HONEST AND THE HORIZONTAL ONE WAS NOT.** `<details>` moved 0 of 24 above it in the booth and 0 of 47 in the wing FAQs. What moved was everything, sideways: opening Record 013 grows the document past the window, the scrollbar appears, the viewport goes 403 → 390 and **18 elements above the change shift**. One declaration fixes it (`scrollbar-gutter: stable`), plus `grid-template-rows: max-content 1fr` for a stacked grid row that was sharing slack with the thing being opened. After: **0 of 35 above, open and closed, at 390px and 1228px.** Law: Doctrine 19. |
| **N5** | [M97](#m97) — the 390px lap had not run for four rounds. | **DONE. The window's size was never the museum's viewport.** A 403px same-origin iframe gives `innerWidth: 390` exactly and lets the driver measure rather than read pixels. **The recipe is committed this time** — `tools/lap/harness.html`, `npm run lap` / `lap:clean` — because v50 solved this once and lost it with a gitignored scratch file. Eleven routes: zero page-level horizontal scroll, zero console errors. **The four rounds of unverified phone rules held**; what the lap found is on `/hr` alone ([N-j](#n-j)). |
| **N6** | — | **DONE** — this file, in the sealing commit. [M97](#m97) and [M92](#m92) closed; [N-g](#n-g)…[N-k](#n-k) raised; five rows added to the short list, each with the one line of what Mike must supply. |

## 4g. CLOSED IN THE VISIBILITY RULE + FORMAT CONFORMANCE (V1 · F1 · F2 · G1 · C1, 2026-08-06)

| # | The instruction | What happened |
|---|---|---|
| **V1** | During development, show everything that is PLACED. The pull-back is a launch-state rule. The Portal comes back immediately. Two states, switchable and unambiguous, and the gate checks the LAUNCH state. | **DONE, AND THE LAUNCH BUILD WAS BUILDING HALF THE APPLICATION.** `reveal/stage.mjs` is one word with two values, default DEVELOPMENT. **The two holds are two doors now** — `/assets/locked/` + `/locked/` is the PERMISSION hold (`/hr`, password in every stage) and `/assets/held/` + `/held/` is the STAGE hold — because one list guarding both would have handed Hunter Root's wing to a build flag. `placeRule` is pure, called by the browser, the build and check 9. Verified on the wire in both stages. |
| **F1** | Every wing FAQ takes the booth's layout and format EXACTLY. Third time the format has been ruled. | **DONE, AND THE THIRD TIME IS THE FINDING.** R7 conformed the ACCORDION and left every face free to declare a blurb, a register, a still and a footer. It is a FACTORY now — `faqFace()` in `src/data/faq-face.js` — so the fields are absent rather than unset. Six faces conform; `/booth` reads the same two declarations rather than typing them. |
| **F2** | Burn and discard "The Deal" on `/wal`. Add an FAQ in the booth's format. | **DONE.** The track and its face are gone. Every claim on it survives somewhere a visitor can reach except one — what the pictures are and that they come down on request — which is the third question on the new FAQ. Flagged as a judgement: [C-d](#c-d). |
| **G1** | The guest book rows are too tall. Answer the measured question, then fix to the tightest honest option. | **DONE, AND THE QUESTION DOES NOT BIND.** The old 30px cannot hold a wrap (a two-line row is 37.11px). But **no current entry needs one** — the longest clears a single line by **63.00px**. The row is MEASURED off the tallest signature: **23px today**, below the 30px he remembers, with the two-line capability intact. |
| **C1** | Sweep both repos for unreferenced images and delete them. | **DONE, AND THE CULL'S OWN INSTRUMENTS WOULD HAVE CONDEMNED THE PHOTOGRAPHS V1 HAD JUST RESTORED.** Four of them broke on one cause. Three files deleted (2.7 MB); eleven kept with reasons on the record ([C-a](#c-a), [C-c](#c-c)); robots repo **0 unreferenced**; `docs/` **61 of 61 cited**. `npm run assets:cull` is the deliberate counterpart of `--rename`. |

**And the finding that was not in any instruction.** `npm run build:launch`'s first cut called vite's node `build()` API. This project has TWO vite environments and that API drives only the client, so the client came out in the LAUNCH state while `dist/weird_baby/index.js` was left over from the previous DEVELOPMENT build — **both stage doors open on a launched museum, with one word in `/api/held` as the only symptom.** A build that builds half the app looks like a build. It spawns the CLI now. Recorded in OPERATIONS §8.

---

## 4f. CLOSED IN THE PORTAL HOLD + THE PULL-BACK (H1–H8, 2026-08-06)

| # | The instruction | What happened |
|---|---|---|
| **H1** | The Portal is held from launch; build the MECHANISM, and the gate must fail if a held thing becomes reachable. | **DONE, AND THE GATE FOUND SEVEN THINGS THAT WERE ALREADY UNTRUE.** The album is its own module behind `assets/held/`, its files behind a new `/held/` prefix, and `reveal/reachability.mjs` runs eight checks against the TREE rather than the row. **Nine deliberate breaches, nine caught.** Its eighth check was not in the design: proving the first seven meant reading the built bundle, and the bundle was shipping the whole reveal ledger. |
| **H2** | The pull-back rule, stated once and enforced: nothing publishes until the Record delivers it. | **DONE.** `reveal/delivery.mjs` — one sentence, one written exception (signage), no fall-through. **Twenty-six pictures went dark and one stayed**: the power switch, on the entry that delivers it. The files MOVED behind the door, because taking a picture off a page does not take it off the server. |
| **H3a** | The nameplate, to the UNIVAC references. | **DONE.** Four levels of depth — formed bezel, recessed field, letterforms in relief cut from a brushed gradient, data struck into a lighter recess. Two of three fields ship empty: [P-a](#p-a). |
| **H3b** | The instrument panel floats inside its window. | **DONE BY DELETION.** Four declarations struck; the panel takes the house frame every document face already has. **`kind:"portal"` is declared by nothing and has not been for a fortnight** — thirteen rules deleted, and F0 had spent a paragraph preserving one of them byte-for-byte on a face that did not exist. |
| **H3c** | PORTAL · Portal Feed Controller · FAQ. | **DONE**, and the FAQ's two questions were MOVED from the machines' FAQs rather than written — they were the only two rows on the public site naming a held room. One judgement is Ops': [P-b](#p-b). |
| **H4** | The selected track is highlighted on entry, every wing. | **DONE, AND DERIVED RATHER THAN SET.** The highlight falls where the viewer already is; `activeTrack` stays null until a click, so nothing downstream changes what it draws. |
| **H5** | Strip the year overlay off the VIIIp art. | **DONE, AND THE GRADIENT UNDER IT WENT TOO** — `.cf-overlay` was an empty div whose only job was to give the year a ground. It was printing on **two covers in the whole public museum**. |
| **H6** | "About the Artists", and sweep the shouty-caps pattern. | **DONE, AND THE DATA WAS HALF OF IT.** Ten pairs found, all one shape: a row in sentence case and its face heading holding the identical words shouted. `text-transform:uppercase` came off `.vp-face-title` in the same commit, or rewriting the strings would have changed nothing a visitor sees. |
| **H7** | A contact sheet to rule the photo cull from. | **DONE** — `npm run contact-sheet` → `docs/CONTACT_SHEET.html`. 257 images, both repos, 85.6 MB of source in 900 KB of one file. It computes nothing new; it puts a PICTURE on every row of the asset table. |
| **H8** | Work the register — mechanical and unambiguous only. | **DONE, AND ONE ITEM REFUSED.** [C39](#c39)'s NUL-byte class caught a seventh instance — this round's own patch, in `tools/asset-table.mjs` — and prevented an eighth. [M99](#m99) is a mechanism now instead of a note. **[C6](#c6) was looked at and left**: policing `[[n]]` markers needs a fourth reader in `record-entries.mjs`, whose reader split is a documented constraint, and that is not unambiguous. |

**And one defect nobody was looking for.** `face.presets` is read by TWO renderers: L2's `ARRIVE AS` recipe selector (2026-07-31) and N9's archive groupings (2026-08-06), which took the same field name on a different object. Both were live and unconditional, so **each Image Archive face was drawing a dropdown of photograph groupings wired to open the twin**, and a still on such a face was a portal door. The recipes have had no declaring face since P2 replaced the Portal's face with the panel. Deleted, with `recipeIdx`, `enterRecipe` and `.vp-face-door`.

**And C32's content-move carry had never once fired.** `tools/asset-table.mjs` keyed its hash pool `repo` + NUL + `sha` and read it back with a SPACE, eighteen lines apart. Its failure mode is the one it exists to fix — a moved file reads as a lost judgement — so it looked like the tool working. Twenty-six judged rows moved in this round and every one came up missing, which is the first time the shortfall was big enough to look twice at.

---

## 4e. CLOSED IN THE COMBINED BRIEF (D1–D3 · A1–A3 · L1 · F1–F8 · W1–W2 · V1–V2 · C1–C4, 2026-08-06)

**Twenty-two instructions. Twenty-one built, one answered as the proposal it
asked for, and one struck string does not exist in this repository.** Round log:
`docs/MUSEUM_COMBINED_BRIEF_LOG-20260806.md`.

| ID | Instruction | Result |
|---|---|---|
| **D1** | The viewer defaults so it and the PUV scroller both fit; justify its fixed edge against the tracklist. | **DONE.** The contents column is MEASURED (`max-content`, read back, restored inside a layout effect) and the picture is `left:0` rather than centred in its slack. Measured on /wb at 1706×810: document 1229px → **810 = the window**. It reverses this file's own *"the split is not a fit lever"*, in the open. |
| **D2** | The tracklist is no longer half the screen by default. | **DONE.** 832px → 521px on /wb, from the album's own longest row; clamped 22…46, and `TL_MAX` is D2 stated as a number. |
| **D3** | The carousel defaults a step or two above minimum; full range reachable; every session resets. | **DONE.** `CF_DEF = 200`, and it is the default AND the fit's ceiling — the fit may still bring it down to 160 and may no longer raise it. |
| **A1** | The black poster border dies, everywhere. | **DONE.** The dark stage, the compensating ring, the mat, twelve re-pinned tokens and six colour literals — about forty declarations — deleted. `[data-exhibit="wal"]` appears four times in `Exhibit.css` and two are comments. |
| **A2** | The only legitimate difference between wings is content colour. | **DONE.** The sheet-on-mat is `[data-flat="1"]` — every document wing — and C4's plate geometry, the trail doors and seven paper values moved out of a wing's scope into the base rules. Five of them also fix /robots, which V1's own note predicted and could not reach. |
| **A3** | The robots gray album art is the standard; replace every equivalent. | **DONE.** Six covers on `tools/make_house_covers.py`, which **proves** it is the same geometry by re-rendering the robots cover and comparing pixel for pixel. Reverses `make_foundation_covers.py`'s stated "one departure". |
| **L1** | People entries character-limited to fit the display; both lines to fill; enforced at entry. | **DONE.** Two-line grid, no truncation anywhere, 88/32 enforced in the input and in the worker. **M108** carries the number. |
| **F1** | Add an exit to the lobby, matching every other wing. | **DONE.** A per-wing `exit` slot; `shopEntryHidden` untouched. Reads LOBBY. |
| **F2** | Burn down the current "The Ledger" track — discard, do not port. | **DONE**, with one thing carried and flagged: the POSTURE. **M98.** |
| **F3** | The FAQ's content is usable — preserve it, move it to a track, booth format. | **DONE.** One declaration, split by an `on:` placement field into two tracks (5 + 7). R7's accordion untouched. |
| **F4** | The three albums and their tracks, as specified. | **DONE**, his names and his order. |
| **F5** | The cadence shape; propose what serves it. | **PROPOSED AND BUILT EMPTY.** Not a track per update — the Record's own `entriesMode:"log"`. A `logEmpty` field was added because an empty log rendered nothing at all. |
| **F6** | Contribute burned down, not rebuilt; empty and honest. | **DONE.** One sentence, declared once, on all three. **M106.** |
| **F7** | Preserve the three mechanisms; state where each landed. | **DONE and stated** — account card → Executive summary; register → Where, why, etc.; ledger object → Money in, out. |
| **F8** | The tone ruling, standing. | **HELD.** Nothing written this round asks, flatters, argues need, or reads differently to a richer reader. |
| **W1** | About the artist — burn it down; rebuild as small consistent flexible categories. | **DONE.** `face.profile`, six slots, renderer knows no category names. Five print nothing (**M103**); the keeper's answer left the card (**M107**). |
| **W2** | Conform the wing's albums to Robots and WAL. | **DONE** by construction — A3 replaced the art and A1/A2 made the chrome one declaration. |
| **V1** | Strike two sentences. | **ONE DONE, ONE NOT PRESENT.** The second is nowhere in `src/` or `index.html`. **M100.** |
| **V2** | Restructure the poster: one row of four, then each artist larger with a note and a [PAPA] sentence. | **DONE.** Two registers off one array. The bill's acts are now SCRUBBED, which they were not — a latent Doctrine 11 hole that `pick` would have printed straight through. **M104.** |
| **C1** | "WHAT ARE THEY UP TO?", and the sentence case wherever that pattern appears. | **DONE.** Five row titles conformed; `The Record` and `FAQ` left alone with the reason written down. |
| **C2** | Reduce the page to the tiles; answer whether we have tiles besides YouTube. | **DONE, and the answer is NO** — every tile on every artist page is a YouTube thumbnail opening a YouTube page. The count row went; the SOURCE row and the tour door stayed, with reasons. |
| **C3** | Presets would help here; propose per artist from verified material. | **PROPOSED, NOT BUILT.** One award video and four live ones in the whole wing. **M101.** |
| **C4** | "About the Songs" is repetitive — burn it down, discard. | **DONE.** The builder is deleted rather than left uncalled; the cost is **M102**. |

## 4d. CLOSED IN THE PAGE-BY-PAGE (L1 · R1–R7 · P1–P6 · N1–N11 · X1, 2026-08-06)

**Twenty-six instructions, all twenty-six answered, and the most useful finding
in the round is a MEASUREMENT that killed the thing it measured.** Round log:
`docs/MUSEUM_PAGE_BY_PAGE_LOG-20260806.md`.

| ID | Instruction | Result |
|---|---|---|
| **L1** | THE RECORD off the lobby directory. | **DONE.** The row is gone; `/robots/record` is still a route and still lands on the Record. |
| **R1** | A date and/or day number, far left, or something better. | **DONE — the RECORD NUMBER**, `013`, in a fixed rail, with the date under it the moment an entry has one. Chosen over a date because the dates were invented and you deleted them. |
| **R2** | The entry format is good; keep it. | **UNTOUCHED**, and recorded rather than assumed. |
| **R3** | Headline + executive summary, same height, whole summary, no ellipsis. | **DONE, AND "BY CONSTRUCTION" IS A MECHANISM.** No truncation left in the index at all; `reveal:check` refuses a headline past 62 characters or a summary past 130. Proved by breaking it. |
| **R4** | The narrow measure — explain, and render options. | **ANSWERED WITH A NUMBER.** Two columns was BUILT and measured at **33 characters a column** — half the band — and removed. The entry body went 56ch → 68ch, which is the finding: it was UNDER the band, not over it. |
| **R5** | The "object" icon: make it serve, or strike it. | **STRUCK**, in all three places it drew. It could not be made to serve — the word opened nothing and there is no object registry. [M96](#m96). |
| **R6** | *Purveyors of the Weird* only on the first album. | **DONE**, three covers re-rendered; same font, size, tracking, ink and baseline. |
| **R7** | Conform every wing FAQ to the booth's format. | **DONE**, four faces. [M70](#m70) closes and D7's flattening is reversed in the open. |
| **P1** | ABEAL alone; INSTRUMENT DIVISION is drift. | **DONE** — and it is the second time the same drift regrew, a fortnight after you struck it off the manual cover. |
| **P2** | The placard must look like a metal nameplate with hand-stamped serial. | **DONE.** Bright metal, black ink, two rivets, a struck field. The field ships EMPTY: [M94](#m94). |
| **P3** | Remove the unit-as-it-stands readout. | **DONE.** Three instruments already said it. |
| **P4** | More obvious screws. | **DONE**, 11px → 18px — and the dish around them, which did not exist, is what makes them read as fixings. |
| **P5** | The feed selector wraps and makes no sense. | **DONE** — STANDBY · COLD START · FIRST RUN. The `id`s did not move, so M33's five levers are unchanged; three ledger row names travelled with the engravings. |
| **P6** | Noted, not for action: the empty right side. | **RECORDED, NOT TOUCHED.** [M95](#m95). |
| **N1** | Technical Specifications to the top. | **DONE, ON BOTH MACHINES** — `parity:gate` compares ORDER, so one album alone would have failed the next commit. |
| **N2** | The one sheet. | **DONE.** Four things struck by name, every fact kept, nothing new. Two costs named: [M87](#m87), [M88](#m88). |
| **N3** | The Manual becomes DOCUMENTATION, on a template. | **DONE, AND NO NEW MACHINERY.** L6's document card was lifted out of the Record's renderer into one shared `DocList`. Largest deletion in the round: [M89](#m89). |
| **N4** | Strike the lead-in and the register beside the images. | **DONE, BOTH WALLS.** No replacement lead-in — that is the answer to your "if". [M25](#m25) closes with it; cost is [M90](#m90). |
| **N5** | ZOOM IN / ZOOM OUT, and make the button act. | **DONE.** The old control was answering two questions in opposite directions at once. Measured: 856px → 1048px on one press. |
| **N6** | Remove the image descriptions in the viewer. | **DONE**, scoped to the viewer as you scoped it. The wall keeps its captions: [M86](#m86). |
| **N7** | Flag any per-frame date that cannot fit the story. | **FLAGGED, AND THERE ARE TWO PROBLEMS.** [M93](#m93). |
| **N8** | The footer, and PLATES becomes PHOTOGRAPHS or IMAGES. | **DONE** — the wing's standard UNIT · OBJECT sign-off, and **PHOTOGRAPHS**, proposed with three reasons. SLIDES is ledgered and not built. |
| **N9** | Build presets that curate the archive. | **DONE**, seven curated cuts and two coarse ones. Every button carries its count. The egg consequence is `egg.presets` — ledgered, not built. [M91](#m91). |
| **N10** | The FAQ to the bottom, conformed per R7. | **DONE**, both machines. |
| **N11** | Leave the gift shop and lobby lines alone. | **UNTOUCHED**, recorded rather than assumed. |
| **X1** | Every egg and future idea consultable. | **THE LEDGER HELD ALL OF IT AND NOBODY COULD ASK IT.** `npm run reveal:eggs` — planted 6 · spent 4 · waiting 5 — and it names `OPEN_ACTIONS.md` as the other half rather than pretending to hold it. |
| **—** | This file. | **DONE** — two rows closed, twelve added, nine promoted to the short list, in the sealing commit. |

---

## 4c. CLOSED IN THE PORCH RULINGS (R1–R8, 2026-08-06)

**Six rulings, one research map, and the ruling he cared most about turned out to
be aimed at the wrong page.** Round log:
`docs/MUSEUM_PORCH_RULINGS_LOG-20260806.md`. Research: `docs/AD_FREE_PLAYBACK_RESEARCH-20260806.md`.

| ID | Ruling | Result |
|---|---|---|
| **R1** | [M66](#m66) — fold the contact answer in. | **DONE.** `robots.js` imports `CONTACT`; the bare address is gone; no known unlinked copy of a house passage remains. |
| **R2** | [M67](#m67) — delete both FAQ intro blurbs. | **DONE**, by subtraction rather than by picking a wording. `/booth` and `/foundation` were checked and have no such line to delete. |
| **R3** | [M68](#m68) — print the share tags verbatim, do not merge. | **REPORTED, NOT MERGED.** All three printed in [M68](#m68). One is plainly stale: the search description names two exhibits where the board lists three. |
| **R4** | [M69](#m69) — delete the holdings sentence everywhere. | **DONE**, seven sites. [M52](#m52) closes with it. Two consequences named: [M72](#m72), [M75](#m75). |
| **R5** | Hunter Root's tracks move to reference. | **DONE, AND THE PREMISE WAS INVERTED.** The two /wal songs were already his own uploads; `/hr` was serving **93**. Zero vault audio now reaches a browser, and the addresses no longer ship in the bundle. New: [M71](#m71), [M73](#m73). |
| **R6** | [M37](#m37) — option A, disclose. | **DONE**, and the disclosure was re-measured and corrected. [C34](#c34) closes ruled-against. [C12](#c12) narrowed. |
| **R7** | Size the ad-free playback knob. | **DELIVERED.** [M76](#m76) — thirteen tracks for Hunter Root, three whole catalogues for the others. |
| **R8** | This file. | **DONE** — six rows closed, one amended, five added, in the sealing commit. |

---

## 4b. CLOSED IN CLEAR THE DECK (D1–D9, 2026-08-06)

**Nine instructions, all nine landed, and the biggest of them was found by
looking for something else.** Round log:
`docs/MUSEUM_CLEAR_THE_DECK_LOG-20260806.md`.

| ID | What it was | How it closed |
|---|---|---|
| **D1** | **The same passage lived in more than one room with no link between the copies**, so fixing one never fixed the other. | **MAPPED AND FIXED, and the map was made from the provenance sweep's own extractor** — so the population is exactly the population `provenance:gate` polices, not a grep. **Three exact cross-file duplicates hoisted:** the keeper's answer (now `src/data/house-copy.js`, read by the booth and by /wb), and — inside `robots.js` — the manual reader's FORMAT and NAV lines, the empty reel's note and the shop answer, all four typed twice because parity is absolute and `npm run parity` polices the menu ITEMS and cannot see the words inside them. **Four DIVERGED pairs were reported, not merged** ([M66](#m66)–[M69](#m69)), which is what his instruction says to do. **And repeated source citations were deliberately left alone** — 54 of *"— Wikipedia, read 2026"* is one citation per fact, not one passage in 54 places. |
| **D2** | The booth's contact answer was three sentences. | **DONE, HIS WORDING, RULED.** *"Write to the guy running the place: papa@weird.baby."* **Two sentences struck with it**: one restated the keeper answer four rows above it, and the other was the museum advertising its own diligence — the subject the WAL poster's foot was struck for one round ago. |
| **D3a** | [M53](#m53) — a `[PAPA]` note's unmarked second sentence printing on the poster. | **CLOSED, AND THE SAME DEFECT WAS LIVE ON A SECOND FACE.** See the row. The scrubber is untouched, which M53 insisted on. |
| **D3b** | The viewer read out of proportion with the page around it. | **DONE — `--face-zoom: .94`, one dial, and it is on the FLOORS too.** Scaling only the dial would have left the rem floors under `--fs-micro` and `--fs-small` standing, and the ramp would have FLATTENED rather than shrunk — P7's failure mode read backwards, which A4 declared it could not do. The face's three measured sizes (body padding, head gap, plate height) carry it as well, because "everything" is not only type. Every ratio A4 set survives. |
| **D3c** | [M50](#m50) again — *"Hunter Root's text is still incorrect."* | **DONE, AND THE SURFACE NOBODY HAD LOOKED AT WAS A WHOLE SECOND CATALOGUE.** `/hr/archive` held a hand-typed ALBUMS array: six containers where the vault holds nine, two whole records missing, four SINGLES & RARITIES tracks filed under three other records, two track titles that are not on the record they were printed under, and a header reading *"6 albums · 71 songs · 2018 – 2025"*. **It is deleted, not corrected** — a corrected mirror is a mirror that will drift again, and this one drifted through six museum-wide figure sweeps unnoticed. The page reads the spine now. **And W1's own fix carried the defect it was curing:** *"out of ninety-three"* counts TRACK ROWS and was printed as a count of SONGS, the same unit swap as *"78 songs"*, one round later, by the round that named the class. The unit is corrected; **no second number entered the building.** |
| **D4** | The billed gift-shop tile led without looking like it led. | **DONE — IT OWNS THE TOP ROW, CENTRED, AT ONE COLUMN'S WIDTH.** The column count is explicit now (`--gs-cols`) and that is the whole mechanism: `auto-fit` is unimprovable until you have to ask it how wide one column is. **Measured on the built bundle: five tiles, 445×298 every one**, the billed tile centred to half a pixel. At one column all five are 912 wide. **The breakpoint is not a new decision** — 910px is the width `auto-fit` was already switching at. |
| **D5** | The unit sat INSIDE the oval; the mark it copies puts the subject OVER it. | **DONE, BOTH MACHINES, AND THE TWO TREATMENTS ARE THE SOURCE'S DOING RATHER THAN THE EFFORT'S.** The ring is drawn first and the unit stands over it; the disc did not move, the ORDER moved. **The VIIIp gets a real photographic silhouette** (threshold 70, because the cast shadow bottoms out at 66). **The mainframe rides over the ring as a PLATE**: its frame was cut at the cabinet's own bounding box so there is no background in it to remove, and **three mattes were rendered and each damaged the object**. A crop above the feet composes better than any of them and throws the feet away — and *the entire mainframe* is not something Ops trades for a nicer edge. The Portal keeps its masked disc: it is a door, not a machine. |
| **D6** | [M61](#m61) — the manual and the built viewer. | **RULED — HELD. Nothing shipped, which is the ruling working.** |
| **D7** | [M62](#m62) — the Foundation's albums and tracklist. | **DONE, OPTION A, ALL THREE MECHANISMS INTACT.** And **the lap caught three things no gate could** — see the row. |
| **D8** | [C41](#c41) — the surfacing log stamped its rows in UTC. | **DONE.** One line. The rows already written are not touched. |
| **—** | **A prune plus a MOVE is the same hazard as a prune plus a rename.** | **THE PROCEDURE HELD, AND IT FOUND ELEVEN CHAINS EXACTLY.** 106 register rows moved file without changing a character, so their classifications were CARRIED (matched on exact text from the exact file they left — a string whose old row cannot be found is left undeclared and reported, never invented a row). 31 rows were genuinely new. Then, per §9's corrected procedure: **prune against a copy and let the gate name the broken chains.** It named eleven. Nine were the WAL poster's acts pointing at the `papa` note D3a shortened — **repointed onto the artist-card rows that actually carry each claim, which is a better chain than the one that broke**, since the old anchor merely ASSERTED that the sourcing existed. The other two pointed at register rows the carry had re-keyed. **Gate green: 0 undeclared · 0 stale · 0 invention.** |

---

## 5. CLOSED IN MIKE'S READING PASS — ROUND ONE (2026-08-05)

**He read two rooms and every finding was real.** Round log:
`docs/MUSEUM_READING_PASS_ROUND_ONE_LOG-20260805.md`.

| ID | What it was | How it closed |
|---|---|---|
| **L1** | **The lobby called the building "The Museum" while every other line on its own board said WEIRD.BABY something.** | **DONE.** `SUBTITLE` is `"Weird.Baby Museum"` and `.wb-subtitle` uppercases it, so the glass reads WEIRD.BABY MUSEUM in the source casing the rest of the data uses. **It does not reverse M-ID** — that ruling struck four names that each fenced the museum to a class of artist; a house name narrows nothing. **The sweep he asked for found ONE instance:** the share tags, the booth's credo and the Foundation's invoice all already said Weird.Baby Museum, so the lobby was the last holdout rather than one of many. |
| **L2** | **The watermark was set in lowercase — a third spelling of the house name.** | **DONE.** The bottom-right corner stamp reads `Weird.Baby`. **It also settles a question that has been open in a round log since 2026-08-03**: the morning rip's own M10 — *"the watermark"* — was closed **CANNOT REPRODUCE** with the note *"no element in the museum answers to 'the watermark' as described"*, for want of a room. Mike reading the Lobby gave it one. **That M10 is the morning rip's numbering, not this register's** ([M10](#m10) here is the Foundation's Q7) — the two namespaces have collided and this is the first time it has mattered. `weird.baby` is the DOMAIN — what you type, not what a museum stamps on its own paper. **The WB monogram beside it is left alone:** he named the text, and the circle is the mark. |
| **S1** | **The gift shop's own house tile was about four times the area of its guests'.** | **DONE — ONE GRID, FIVE TILES, MEASURED IDENTICAL** at 445×298 (desktop) and 338×386 (386px). **The billing law is not repealed, it is re-expressed as ORDER:** all three clauses still run, `data-billing` still reports which branch answered, and the billed entry is still the first tile read. Verified on the built bundle across all four billing branches — owner, house-on-direct-arrival, WAL-set fallback and named WAL owner. `--half` and `.featured-artist__cta` are deleted; `.wal-banners__grid` is `.gift-shop__grid`, because it is no longer four of the five things it holds. |
| **S2** | **Every artist tile pointed somewhere that was not the artist's own front door.** | **DONE, FOUR ADDRESSES, ALL FOUR OPENED AND READ 2026-08-05.** Hunter Root fell through `shop: null` to Bandcamp while his own site opens on *Official Merch Store*; Jesse Welles went straight to the merch VENDOR; Carsie Blanton to a /shop/ subpage; **Mikey Mike to a YouTube channel — a gift shop tile that opened a video feed.** `shopExit` on the artist entry is read ahead of the old three-step fallback. **weekendatmikeys.com verified before wiring** — see [M54](#m54); **R-a's refusal of findmikeymike.com stands and is untouched.** The tile's label stopped claiming a store, because one of the four front doors does not have one. |
| **W1** | [M50](#m50) — six sites printing 78 songs and nine albums. | **DONE, AND THE COUNT WAS RE-DERIVED RATHER THAN CARRIED FORWARD.** 9 containers · 93 track rows · 78 with a `song:` slug, the 15 without being exactly Run With The Hunt. **Two years were wrong as well and the mechanism is named:** the records board printed `era-buckets.json` BUCKET START YEARS as release dates — Crooked Home 2024→**2025** (vault `MV-HR-20260707-053`, *"released October 17, 2025"*, and his own Bandcamp) and Arkansas 2022→**2023** (his own Bandcamp, *"released June 30, 2023"*). **And one of M50's own claims was wrong** — see the row. |
| **M1** | **The poke's successor had no precondition.** | **RECORDED, IN BOTH PLACES IT HAS TO BE.** Mike: *do not bother with a next-level egg until this one at least catches on.* It is a rule about the whole egg table, not about one row, so it sits above `egg.lobby.poke` in `reveal/ledger-declare.mjs` and in [C40](#c40). **The cost is stated rather than glossed:** nothing in this museum reports an egg being tripped, so "catches on" is currently unmeasurable and the condition is met by his word. |
| **F1/F2** | **Two rooms named, nothing built.** | **LEDGERED — `room.curtain` and `room.slow`, both NOT_BUILT · HELD · `shown: false`**, plus [M55](#m55) and [M56](#m56). No route, no component, no stub. Both are exempt from the transfer classes under kind (iv) AUTHORSHIP, which is the honest reason: one has to be written and the other has to be chosen. |
| **—** | **A prune waited for its anchors again, and this time there were two.** | **PROCEDURE FOLLOWED IN ORDER.** `"78 songs on file in the museum's own vault"` and `"Nine"` were both about to go stale and both anchored three RESTATED chains. One of the three was itself stale; the two survivors were repointed onto the replacement tombstone rows BEFORE the prune. 10 rows added · 11 pruned · **gate green, 0 undeclared, 0 stale, 0 invention.** |

---

## 5a. CLOSED IN THE PARITY RULING + TRIM (2026-08-05)

> **THE WAL POSTER EDIT (2026-08-05) closed no row and there is no section for
> it.** It struck three passages on Mike's instruction, rebuilt one line from
> the vault, and opened **M50–M53**. A round that closes nothing says so rather
> than borrowing the heading below. Round log:
> `docs/MUSEUM_WAL_POSTER_EDIT_LOG-20260805.md`.

**Two rulings landed, one behaviour built, one idea banked, and a defect that
turned out to be four defects.** Round log:
`docs/MUSEUM_PARITY_RULING_AND_TRIM_LOG-20260805.md`.

| ID | What it was | How it closed |
|---|---|---|
| **P1** | **Four written reasons for four menu divergences, and no way to tell *answered* from *excused*.** | **RULED AND BUILT.** Mike: the three gaps are HOLDINGS, not design; forcing parity would print rows leading nowhere, which THE STUB LAW forbids on those grounds. **A flag justified in writing by a holdings gap is RESOLVED rather than overridden.** A justification now carries a `kind` (HOLDINGS · PROPERTY resolve; DESIGN stands, and is declared while unused so the first real preference has an honest box), and **a holdings gap must name the ledger row that would exist if the museum held the material** — `parity:gate` reads `reveal/ledger.json` and faults the day it is built, which is what makes "it clears itself" a mechanism. **Four new refusals, each broken on purpose and each fired.** 2 shared · 4 divergences · **4 resolved · 0 standing · 0 undeclared.** See [M47](#m47). |
| **P2** | **A plate reading TEXT NOT SUPPLIED.** | **STRUCK, ON THE RULING THAT IT WAS THE MUSEUM ADMITTING IT HAD NOT WRITTEN THE MANUAL, WEARING A FICTION AS COVER.** Either the plate shows a page actually written or there is no plate — **empty beats a placeholder in fiction's clothing.** Not re-captioned: the caption was already doing the work, which is the tell. `reel.plates` untouched. Closes [M45](#m45) and, by there being nothing left to be a render, **[M4](#m4)**. Exposes [M48](#m48); grows [M9](#m9) by one. |
| **P3** | **The guest list moved on a clock and nobody could touch it.** | **DONE — DRAG, plus ARROWS as the keyboard equivalent any draggable owes.** Drag fits the register because the book's own file already calls it a hinged board being read down, and because **it lands on a row**: the track follows the hand with the transition off, then settles to the nearest signature with the timer's own bounce. **The wheel is REFUSED with a reason** — a 92px strip inside a page people scroll cannot take the wheel without stealing it. The vertical TOUCH gesture IS taken (`touch-action: pan-x pinch-zoom`, pinch never blocked) because the plain fallback list already takes it natively. `↑` **stops at the first signature** — a guest book has a beginning. **"Resumes after a rest" is one dependency, not a second clock**, which is exactly what Q1 was. **The clamp did not move.** One hazard caught before it shipped: hover-pause is guarded on `pointerType === "mouse"`, or a phone's synthetic `mouseenter` would have frozen the book under the finger that just dragged it. |
| **P4** | **The poke.** | **LEDGERED, GRADED A+++++, AND BUILT NOT AT ALL** — on instruction. `egg.lobby.poke`, NOT_BUILT · HELD · `shown: false`. **The grade's reasons are the specification** and they are in the ledger beside the row: found by doing what nobody tells you to do · costs nothing on the glass · **an escalation, which no other egg in this table has** · on the one object every visitor meets. What it waits on is ART, not code. [C40](#c40). |
| **P5a** | [C39](#c39) — two NUL bytes in one tool. | **DONE, AND IT WAS FOUR FILES CARRYING SIX BYTES** — including **`keyOf` in `provenance-sweep.mjs`**, the function that hashes every visitor-facing string in the museum. The row named an INSTANCE of a habit and the habit had already spread to a tool built one round after it was written. **Proved to change nothing by the strongest test available: `provenance:gate` still passes**, which it could not if a single key had shifted. |
| **P5b** | [C35](#c35) — `/admin` unreachable at a phone width. | **DONE.** Both rows wrap; the inline flex became a class. **Measured at 386px: all five controls in view, none clipped, no horizontal scroll.** A media query was the wrong fix and the reason is in the stylesheet. |
| **—** | **A stale register row invited a prune, and it was the anchor of 18 RESTATED chains.** | **CHECKED FIRST, FOR ONCE.** v52 and v56 both found this ordering *after* the prune. This round read the anchors before touching anything, repointed all 18 onto the two survivors (a VERIFIED row and a MIKE row on the same face), pruned, then re-gated. **The hazard is now a procedure: check anchors → repoint → prune → re-gate.** |
| **—** | **The surfacing tripwire fired on the opposite of what it was built for.** | **REPORTED, NOT ADJUSTED — [M49](#m49).** Idle files grew two packets running, and both growths are DELETIONS: v56 struck seven plates, P2 struck one. A struck file and a hoarded file land on the same shelf and only the second is *building ahead*. **A number adjusted to say what a round wants is not a measurement.** |

---

## 5b. CLOSED IN v56 (THE ROBOTS SIMPLIFICATION, 2026-08-05)

**Cross-repo, and it deleted more than it built.** Round log:
`docs/MUSEUM_ROBOTS_SIMPLIFICATION_LOG-20260805.md`.

| ID | What it was | How it closed |
|---|---|---|
| **G1** | `reveal:check` RED since T1, and the 24-page manual dead. | **DONE, and the count is derived rather than declared.** See [T-A](#ta). Three failure modes, each broken on purpose. It exposed the manual pages falling through the transfer table — declared PACKAGE in prose since T1 and enforced by nothing — and closed that with **one pattern rule and a fault if a pattern ever overrules a written decision.** The vessel's self-test now REQUIRES the arrival-week fault on a PLACED page rather than being excused from the rule. |
| **R1** | **The Record sat on the MGK-VIIIp album** and is about all things robots. | **DONE, moved whole and not one sentence changed** — the block was moved by script rather than retyped. `face.viiip.record` → `face.wbr.record` is the ONLY ledger id ever renamed; nothing outside `reveal/` reads it. **And it got an address**, because a directory line has to land on the thing it names: `Exhibit.jsx` takes an `open` prop (a track id), `/robots/record` passes it, and the lobby board carries **The Record indented under Weird.Baby Robots** — reusing F7's own indent rule. It is the only line on that board that is not a room. |
| **R3** | **Four front-desk faces where one would do.** | **DONE — WELCOME, DOC CONTROL and CONTACT struck, eleven questions where there were four rooms.** What survived each is folded into the FAQ and named in `robots.js`; what was deleted outright is named too, each because a face a visitor can already reach says it. Both typographic cards went with their faces (~90 lines of generator). Closes [M29](#m29) and [C31](#c31). |
| **R4** | **The MGK-NIAC album named the mainframe in every sentence and photographed the ROBOT in every frame** — six of eight plates and the cover badge. | **DONE.** Cover, poster, two stills and the wall all re-cut to the cabinet; three plates carried in from the robots repo's own culled set. Seven photographs left the glass and **none was deleted** ([M9](#m9)). The claim that makes the robot interesting is printed nowhere — it is `egg.niac.operator`, LIVE, HELD, not shown. Closes [M30](#m30); worsens [C29](#c29). **In the robots repo: the reel is out of the design entirely**, generator first. |
| **R5** | **NIAC's story position, unrecorded.** | **DONE, and the table already agreed.** Every `face.niac.*` row is BLAST — forced by rule (b), not chosen — so canon and mechanism reached the same answer from opposite ends. Recorded as `NIAC_STORY_POSITION` and `ASSET_TIMELINE.md` §5.3. **Nothing was read into it about REVEAL:** every `when` is still null. And the round made *the mother lode of detail is VIIIp* TRUE, which it was not: eight NIAC plates against nine became four. |
| **R6** | **The channel egg.** | **DONE — eight positions engraved 1…8, NIAC on 1 and 2, STANDARD on 3.** The reason is written down **once**, in `egg.channels`, and nowhere else in either repository; no page explains it. The NIAC positions carry the machine's name and **no invented feed title**. M33's five levers are untouched. **The lap caught what the data could not:** two dead channels at the top of the drum made the Portal open NOT ARMED with the latch two rolls away, so the drum now opens on the first position that arms. |
| **R2** | **Menu parity, with teeth.** | **DONE** — `npm run parity` / `parity:gate`, reading both albums out of the AST. **2 shared · 4 declared divergences · 0 undeclared.** Both failure directions broken on purpose, including a justification for a divergence that no longer exists — a table checked one way rots into a list of excuses. **Not a packet gate**, on `assets:gate`'s reasoning. The findings are [M47](#m47). |
| **R7** | **Nothing asks what has not been shown.** | **DONE, and the diagnosis was sharper than the ask.** Three instruments already knew the answer and none was ASKED. `npm run surfacing` computes nothing new — it re-cuts the ledger and the asset table **BY WING**, which is the only cut a person can act on. **13 spendable · 13 promised · 14 idle files today**, ten of the spendable in one wing. Cadence proposed (**one surfacing per packet; the shelf must not grow two packets running**) and it runs beside lint and build because **the packet is the only clock this repo has**. `--log` makes the number a trend. **It refuses to count shorts and refuses to print a zero for them** — [M46](#m46). |
| **—** | **A prune broke 44 RESTATED chains**, which is v52's own recorded hazard (*a rename plus a prune is two safe operations that are unsafe in sequence*). | **CAUGHT BY THE GATE, AGAIN, AND FIXED PROPERLY.** Four anchor rows were pruned with the faces that carried them; each has a direct successor this round created, and all 44 references were repointed at those rather than at whatever still resolved. **The property is worth restating: the RESTATED class's requirement that a reference RESOLVE is the only thing in the boundary that notices a deletion.** |
| **—** | `git checkout --` on a file carrying uncommitted work, to undo a deliberate test break. | **SELF-INFLICTED, CAUGHT IMMEDIATELY, AND RECORDED SO IT IS NOT REPEATED.** It reverted the round's own G1 edits along with the break. **Breakage tests sandbox by file copy, never by git**, when the file also holds work that is not committed. |

---

## 5c. CLOSED IN v55 (RECORD MACHINERY, 2026-08-05)

**The round built vessels and invented no contents.** Mike is about to produce
the Record's first two weeks in voice sessions; this is what had to exist to
catch what he says.

| ID | What it was | How it closed |
|---|---|---|
| **R1** | **The ledger held ONE `doc.record` row against a Record that will hold sixty entries**, each naming its own assets (audit §8a). | **DONE, and the rows are DERIVED rather than typed — which is the enforcement, not a convenience.** `reveal/record-entries.mjs` parses `src/data/artists/robots.js` with the acorn + acorn-jsx pair the provenance sweep already uses (the file imports a JSX component, so node can never import it) and is **split in two on purpose**: `entries()` returns entry numbers and asset paths **and nothing else**, and it is the only half the ledger builds from — so a headline, a dateline or a section has no route into the table. Sixty entries will produce sixty rows with no edit to the declaration. `prose()` is the other half, it builds nothing, and it exists so the check can police the rule. **Today that is one row, `record.013`**, and the granularity paid immediately: the entry's plate joined the asset table on its own (rows joined to an asset 11 → 12) and **M18's twenty-seven questions moved off the volume onto the entry that has them**, while M19 — what a record NUMBER means — stayed on `doc.record`, which is now the volume and only the volume. **An entry the Record has not numbered FAILS THE BUILD** rather than being given an id: minting one would be Ops answering M19 with a guess. |
| **R1b** | **The constraint the audit stated and nothing enforced: the ledger must never duplicate the Record's own data.** | **DONE — three ways, and each was broken on purpose to prove it fires.** (1) The generator cannot see the words. (2) `reveal/schema.mjs` refuses a row carrying the Record's FIELD NAMES — `headline`, `dateline`, `sections`, `lead`, `body`, `tomb`, `still`, `title`. (3) `reveal:check` refuses its SENTENCES: **six consecutive words** of the Record's prose, or any whole Record line of four words or more, anywhere in a row's `name`, `note`, `reach`, `where` or `deps`. Six is chosen against the failure it is for — a row that has started restating an entry restates a clause, and a clause is longer than five words; below six, ordinary English collides. **Proved by injection:** a six-word clause lifted from entry 013, a four-word whole line with no six-word window, a `sections` field, and a phantom `record.014` were each written into the built ledger, and the check reported all four and exited 1. **A fourth check came with it** — the rows and the Record's entries must be the same set, in both directions, which catches a hand-edit of `ledger.json` (forbidden, and until now unenforced) and a rebuild that silently dropped an entry. |
| **R1c** | **The ledger's rules lived in two places and the two lists disagreed.** `ledger-declare.mjs` checked five as it wrote; `reveal:check` checked four afterwards; neither was a superset of the other, so a rule was enforced at whichever moment the author happened to run. | **DONE** — one `validate()` in `reveal/schema.mjs`, and both callers run it. |
| **R2** | **The cue-card checklist — audit §8b, designed and deliberately not built.** | **DONE.** `npm run reveal:cards`, the same shape and the same reasoning as `assets:checklist`: an **Ops instrument, not a route, never shipped**, printing to a terminal — a scheduling UI at a live address would be a museum surface whose subject is the making of the museum. One card per row in Doctrine 12's order: what it is · where it stands · what has to happen first · one blank. `--spendable` · `--cls` · `--record` · `--revealed` · `--all`. **The default deck is 49 cards and not 143, and the first draft was wrong about this:** 93 undated rows are already REVEALED, and asking what day a thing comes out when it came out months ago has no honest answer — nobody wrote those days down. The deck is the same 49 the audit's §4 counts. See [M32](#m32). |
| **R3** | **The manual is one row for one PDF and cannot track page-by-page production.** Mike's ruling: it **arrived in pieces**, so the museum needs only the pages the story reaches for. | **DONE as a vessel, populated with nothing.** `manualPageRow()` in `reveal/schema.mjs` builds `doc.manual.page.NN` carrying its own **production arc — needed · printed · photographed · placed** — and the `record.NNN` entry that called for it. **`prod` is NOT `arc`**, and they are kept apart deliberately: `arc` is how the house REVEALS a thing it has, `prod` is whether the house HAS it, and a page can be `photographed` with a null reveal arc. **`build` is DERIVED from `prod`**, so a row cannot claim a state the world is not in. **It refuses a page the manual does not have** — the range is the object's own 24 and the source render is checked on disk — and it validates `calledBy` against real entry rows. `doc.manual.plates` stays as the set-level row and keeps the one promise the glass actually makes. Nothing populated: [M44](#m44). |
| **R3b** | **An empty container nobody has exercised is [C7](#c7)'s shape** — the Record's inline doors, built at v45, exercised by nothing since. | **DONE — the vessel is proved with zero rows shipped.** `reveal:check` builds specimens at all four production stages, asserts the derived build/state/reach, runs each through the same validator the declaration uses, and asserts that every refusal actually refuses (page 25, page 0, an unknown stage, an entry that does not exist, a placed page with no photograph, `prod` on a row that is not a page). Then throws them away. **No page of the manual is invented to prove the container.** |
| **—** | **The first version of that self-test could not fail.** It compared the vessel's output against `BUILD_FOR_PROD` — the table the vessel derives FROM — so corrupting the mapping corrupted the expectation with it. | **CAUGHT BY BREAKING IT ON PURPOSE.** `photographed → PARTIAL` was deliberately changed to `LIVE` and the suite reported PASS. The expected values are literals now, the stage names are asserted too, and both breaks were re-run and both fail. **Worth recording as a property and not as an anecdote: a self-test that reads its answer out of the thing under test is not a test, and the only way to find out is to break the thing and watch.** |
| **R4** | **The stale ancestor.** `weird-baby-robots/docs/ASSET_REVEAL_CHECKLIST.md` recorded the Record as **436 entries with 10 surfaced** — both invented, both deleted at v47 — and still called a face "The Firmware", renamed at v50. | **DONE, marked and not deleted.** A superseded banner at the top points at `reveal/ledger.json` and the commands, **names what it is kept for — it is where the four reveal classes were named and where the proposed fifth was raised, and a source is not superseded by the thing built on it** — and tabulates the three wrong claims against the truth. **The body is deliberately not rewritten:** a first pass edited to agree with what came after it stops being a record of what was thought at the time, which is the only thing it is still for. |
| **R5** | **The fifth reveal class, carried three rounds as a description.** | **DONE as ONE QUESTION, and Ops adopted nothing.** [M35](#m35) is rewritten in Doctrine 12's three-part format — what is known · what is missing · why it matters — and it gained a second case that is not an artifact at all: **building the cue cards found that `route.hr` is HELD PERMANENTLY BY RULING and its card asks what day it comes out.** `when: null` has to carry two meanings it cannot distinguish — *nobody has scheduled this* and *this is deliberately never scheduled* — which is the same gap `SEALED` names for the nickels. That is the argument that it is a class and not a note about nickels. A `no` is equally usable. |
| **R6** | **The register.** | **DONE** — [M32](#m32) and [M35](#m35) rewritten, [M44](#m44), [C38](#c38), [C39](#c39) added, the short list at §0 amended, and the closed sections renumbered 5a–5e. |
| **—** | **v54 sealed without a closed-this-round section**, breaking the pattern v49–v53 all follow. | **NAMED, NOT RECONSTRUCTED.** Its F1–F8 outcomes are in `CLAUDE.md`, `OPERATIONS.md` §5 and `docs/MUSEUM_FOUNDATION_COPY_LOG-20260805.md`, and it did amend and add register ROWS (M38–M43) correctly — only the closed table is missing. Rebuilding it from secondhand notes would put a reconstruction in the one file that is supposed to be the ledger, so it is reported instead. |

---

## 5d. CLOSED IN v53 (THE BOOTH EDIT + THE MISSING LAP, 2026-08-05)

| ID | What it was | How it closed |
|---|---|---|
| **B1** | **THE BROWSER LAP v52 SEALED WITHOUT.** Nobody had seen `/robots` with MGK-NIAC on it or watched the guest book step. | **DONE, and it was five findings deep.** Eleven routes, desktop and 390px: zero horizontal scroll, zero console errors, zero console warnings. **The guest book steps by ONE NAME every five seconds and wraps invisibly** — offset 1→2→3→4…→9 (the seam, showing `Ines · Ada · Bram`) →0 with `snap`, and **every sample in both probes showed exactly three rows, `blanks: 0`**. **MGK-NIAC collides nowhere**: the band reads MGK-NIAC and is centred under its own cover to the pixel at 390px (123 = 123); the new cover renders; the tracklist reads `The Name · Image Archive · Technical Specificatio…`. **What the bundle could not have caught:** `/hr` requests `www.facebook.com` sixteen times across seventeen iframes on arrival ([M37](#m37)); `/wal`'s title bar is down to 6px of air at 390 ([C36](#c36)); `/admin`'s controls are clipped and unreachable at 390 ([C35](#c35)); `/hr`'s FB cards paint as blank blocks ([C12](#c12)); and [M30](#m30) is visible in one screenshot. **Two method limits are stated rather than hidden:** the 390px viewport is a same-origin 390×800 iframe rig, because this Chrome window is maximised and bottoms out at a 540px client area — content width is therefore **373px, tighter than a real phone**. And the guest book will not advance while `document.hidden`, which was confirmed FIRST and unpatched (40+ seconds at offset 0) before `document.hidden` was overridden to watch it move. |
| **B2** | **The tracking answer undersold the truth, and its outbound clause was wrong.** | **DONE.** Rewritten to Mike's line — **the machine remembers you and we don't** — with the cookie/stored-settings distinction stated as the mechanism it is: a cookie travels to the server with every request, browser storage is read where it sits, and both *"this site sets no cookies"* and *"your settings survive"* are true at once. The MGK's record is named for what it is (name, birth date, install level, condition, best score) with the fact that **it has no way to send any of it — zero `fetch`, zero `XMLHttpRequest` in the whole twin** — and the purge surface is named on the glass: **Preferences ▸ User**. The outbound clause is this answer's THIRD and the first two were both wrong; it now names **Google, YouTube and Facebook** and says the last two arrive before a visitor presses anything. |
| **B3** | **Two forced questions.** | **DONE, deleted, not replaced.** *"What are the rooms?"* is answered by the directory on the front page; *"There is a gift shop. What is it?"* is answered by one of its rows. Eleven questions to nine. The shop answer's `[PAPA]` went with it rather than being re-homed — see [M21](#m21). |
| **B4** | **"Can I use what is here?" led with ours.** | **DONE** — it opens *"The artists' work is the artists'"*, gives the doors their sentence, and the museum's half is one clause behind it. Shorter by a line, same facts, `[PAPA]` unchanged. |
| **B5** | **The sweep for the same two faults.** | **DONE — two hits fixed of nine answers read, and the seven left alone are listed with their reason** in the round log, because a sweep that changes everything it touches is not a sweep. *"Is it really free?"* was restating the credo 400px above it and now says only what the credo does not. *"What is this place?"* led with *"Some of it is ours"*; the two sentences swapped and nothing else changed. |
| **B6** | **The register.** | **DONE** — [M37](#m37), [C34](#c34), [C35](#c35), [C36](#c36), [C37](#c37) added; [C12](#c12), [M21](#m21), [M30](#m30) amended. |

---

## 5e. CLOSED IN v52 (THE REVEAL LEDGER, 2026-08-05)

| ID | What it was | How it closed |
|---|---|---|
| **Q1** | **THE GUEST BOOK WENT BLANK, LIVE.** Mike saw it. | **DONE, and the arithmetic was never the fault.** The stepped book advanced on a `setTimeout` and wrapped on a `transitionend` — **two different clocks, and only one of them stops when the page stops being rendered.** A hidden tab throttles timers but SUSPENDS RENDERING, so the timeouts kept adding to the offset while no transition ever completed to subtract a copy back off; ten minutes in a background tab walks the track hundreds of rows past the end of twelve, and it recovers at one net copy per step, which is minutes of empty box. **The fix is two guarantees that need no event to arrive**, both properties of the render rather than of the scheduling: the offset is CLAMPED to `[0, n]` where it is used, and the copy count is DERIVED (`1 + ceil(VISIBLE / n)`) so the track is always long enough for that clamp. **A blank row is now unreachable at any list length, including lengths shorter than the window.** Proved by exhaustive simulation, n = 1…40, with `transitionend` and the backstop both adversarially dropped: the new machine never leaves the track; the old one goes blank at tick 4 and reaches row 119 of 12. The scheduling was fixed too and is now only required to be RIGHT, not SAFE — the book pauses on `document.hidden`, and the wrap has a timeout backstop beside the event. |
| **Q2** | **The stride.** | **DONE** — `STEP` is 1. Mike: *"step by ONE NAME per bounce, not a whole page. Everything else about the rhythm is good."* So `REST_MS` (5.0s), the `cubic-bezier(.34,1.3,.64,1)` bounce, the 520ms move, hover-and-focus pause and the reduced-motion fallback are all untouched. The previous round's argument FOR a page-sized stop is not restated in the code, because it is no longer the behaviour. |
| **Q3** | **MGK-VIII becomes MGK-NIAC.** | **DONE, and the sweep found a question this face was still printing.** The name track's third entry was stamped OPEN, titled *"Which name goes on the door"*, and carried `[PAPA] — whether the carousel reads MGK-NIAC or MGK-VIII`. Mike answered it, so the marker is gone and the entry states the decision. **What conformed:** the album title, the cover file AND the lettering rendered into it, the archive subtitle, the archive tombstone's Subject row, the poster caption, both face footers, the tracklist's first row, and Welcome's own contents line. **What did NOT, and it is the whole of it:** every sentence where MGK-VIII is a FACT OF THE RECORD — *"SOLD AS MGK-VIII — ABEAL's 1965 rebrand"*, *"It was built as MGK-NIAC and sold as MGK-VIII"* — because conforming those deletes the fact the rename is derived FROM and leaves the face saying a machine was built as MGK-NIAC and sold as MGK-NIAC. **Also deliberately not conformed:** `id: "mgk-viii"` and the eleven photographs under `/robots/reference/mgk-viii/`, which are a key and a directory shared with the robots repo, print nowhere, and would cost two repositories a rename to change a string nobody sees. The one judgement call is [M36](#m36). |
| **R1** | **The inventory — every asset and feature in both repos.** | **DONE, and DERIVED rather than transcribed wherever it could be.** The museum's 18 albums and 134 tracks are read out of the data files by script, not typed. The twin's apps are read off **the DISPATCHER** (`Run_EXE`), on Mike's instruction that *"the DISPATCHER is truth — the CSVs have lied twice"*: a row is a STUB exactly when selecting it starts `SCAFFOLD_PROC`. Twelve games, twenty-six apps, six Portal drum positions, sixteen sounds, eleven eggs, ten documents, six physical objects, ten commerce rows, nine retired surfaces. |
| **R2** | **The table, as data.** | **DONE** — `reveal/ledger.json`, **151 rows**, authored in `reveal/ledger-declare.mjs` and validated as it builds. Carries reveal state (HELD / REVEALED / RETIRED), build state (LIVE / PARTIAL / STUB / NOT_BUILT), the story `when`, dependencies, the ruled `revealArc`, and a `shown` flag that separates a PROMISE from a private gap. **`build` and `state` are two axes on purpose:** a thing can be finished and held, and a thing can be reachable and half-built. **It is not a rival to the asset table** — that stays one row per FILE and the authority on files; this is one row per REVEALABLE THING, most of which are not files at all, and it restates no byte count, dimension, quality read or verdict. They meet at `assets: [uid]`, and the declaration FAILS at build time if a ref is not in the asset table. |
| **R3** | ~~C32 — the asset table keyed by path.~~ | **DONE, and it caught the case that raised it.** See [C32](#c32). |
| **R4** | **The audit.** | **DONE** — `docs/REVEAL_LEDGER_AUDIT.md`, generated from the ledger by `npm run reveal:audit` rather than typed, so it can be re-run. **12 built-but-never-revealed** (four eggs and two sounds, all waiting on a sentence rather than on work — and three of the four eggs are already on the same `ask_count_20/40/60` trigger family, which means they are a sequence nobody has decided is one). **11 promised but not built, and six of the eleven are one object** — the Portal's drum. **22 unbuilt and unpromised**, five of them quiet BY LAW rather than by luck. **`doc.manual.plates` is the museum's only two-consumer bottleneck**, and one photograph session closes both. And **55 of the 58 dependency chains terminate outside the table**, in a person, a ruling or a camera — the third independent instrument in four rounds to say the same thing: the museum is not blocked on code. |
| **R5** | **One consumer, wired.** | **DONE** — `/foundation`'s LIVE / NOT BUILT column, which is literally hard-coded availability on the one page in the building whose entire argument is that the state must be true. Five rows now carry a ledger id instead of a typed state. **Proved by flipping `channel.qr` to LIVE, rebuilding, and reading the change out of the built bundle**, then reverting it. **The ledger supplies the STATE and the page keeps the WORDS**, and that is load-bearing: `provenance:gate` sweeps `src/` and `index.html`, so a ledger row supplying the actual letters would have moved copy off the provenance boundary in the same round that mechanised it. |
| **R6** | **What the table needs when the Record goes live.** | **DONE as a design, not a build** — audit §8. Per-entry rows at `record.013` granularity (the schema does not change, only the granularity, and the join/audit/check all work unaltered), with the explicit constraint that the ledger must never become a second copy of the Record. And a **cue-card input view** — one row, one question, the same format Doctrine 12 already specifies for gaps — designed and NOT built, because building an input for a schedule nobody has started is the same mistake as a menu row that leads to "not built". It is [M32](#m32). |
| **—** | **Pruning a stale provenance row can break a RESTATED chain that pointed at it.** | **CAUGHT BY THE GATE, on this round's own edit.** `--prune` dropped the superseded *"MGK-VIII, on the bench"* row, and the archive blurb's `r` chain had been resolving through it; the gate failed on an unresolvable reference in the next run. Repointed to the renamed row. **Worth recording as a property of the boundary:** a rename plus a prune is two safe operations that are unsafe in sequence, and the only thing that noticed was the RESTATED class's own requirement that a reference must resolve. |
| **—** | **The provenance sweep counts an identifier passed to a lookup as a visitor-facing string.** | **DECLARED, NOT EXCLUDED.** Seven ledger row ids and enum values (`channel.qr`, `NOT_BUILT`, `HELD`) tripped the gate. They are keys, never rendered — but the sweep is default-deny and cannot tell an identifier from a label, **which is the correct bias**. Adding an exclusion rule to absorb them would have widened the boundary for everything, so they carry seven ordinary register rows saying what they are. |

---

## 5f. CLOSED IN v51 (M23 RULED + THE ALBUM ROUND, 2026-08-04)

| ID | What it was | How it closed |
|---|---|---|
| **M23a** | **The booth's hook — both candidates.** | **DONE.** Mike struck the pair AND the premise under it: *"the booth image dies — both of them. No ticket, no enamel sign, no replacement visual. THE TITLE IS THE GRAB, and the copy already says it plainly."* F1 and N5 had both accepted that this room was the building's purest failure of the Visual Hook Law and argued only about WHICH object; the ruling rejects that reading, because every object either round built was made out of the credo's own words and then set on top of the credo. `BoothTicket`, `BoothSign`, the `?hook=` selector and ~180 lines of `InfoBooth.css` all went in one commit. **The exception is recorded as Mike instructed: a page whose own words are the hook needs no image** — and it was invoked twice this round (see M29). |
| **M23b** | **The guest book — the scrolling version wins, and then changes.** | **DONE.** The static list and `?book=` are deleted. The winner does not ship as it was: **three rows visible instead of seven, and a STEPPED advance** — it bounces to the next stop, rests, bounces again. The stop is a whole PAGE of three, which is what makes "long enough to read three entries" a tunable quantity; the rest is 5.0s against a 0.52s move, so the book is still 90% of the time. The bounce is one easing (`cubic-bezier(.34,1.3,.64,1)`). The wrap is arithmetic now rather than a keyframe, and two copies are provably enough for any book of five or more. N6's fade mask came off WITH the drift: a stepped book rests on row boundaries, so the same gradient would permanently half-dim two of the three rows a reader is being given time to read. **A plain list still renders under `prefers-reduced-motion` and below five signatures — that is the winner degrading, not the loser surviving; no address serves it by choice.** |
| **A1** | **The album art, third revision.** | **DONE** — `tools/make_unit_covers.py`, whose constants are LIFTED from `make_robots_cover.py` rather than re-chosen, because "one theme" is a claim about geometry and a hand-matched cover drifts the first time either is re-rendered. Same square, ground, border, Georgia setting, rule and strapline; the photograph moves into the disc the WB mark occupied and the model number takes the word ROBOTS' place. Two departures, both applied to BOTH machine covers so the pair cannot diverge: the rule drops 14px so MGK-VIIIp's descender clears it, and the tracking is solved per cover so nine glyphs set inside the measure six do. MGK-VIIIp gets the unit whole; **MGK-VIII cannot and the reason is on its own wall** — see M30. `viiip.png` stays in the build as the tenth tile of its own archive; `mgk-viii-cover.jpg` is orphaned and left on disk (M9). |
| **A2/A3** | **The model number centred, and the band shortened.** | **DONE** — the band spans the WHOLE body and its title sat 40px from the left edge, which read as the tracklist's heading and was 700px from the cover it names. It is now the middle track of a `1fr auto 1fr` grid, so it is centred BY CONSTRUCTION and lands under the active album: measured 845 = 845 at 1706px and 185 = 185 at 386px. Vertical padding 8/7 → 4/3 takes the band from 39.8px to 31.8px. **One thing does not get the centre**, and it is stated rather than hidden: at ≤720px the ONE wing with a transport falls back to two columns, because equal side tracks make a transport of width w cost the centre 2w and no transport is narrow enough at 386px. |
| **A4** | **The top of the type ramp.** | **DONE** — the small end could not come up (its rem floors are P7's answer to unreadable small type) and the dial could not move (it takes the small type with it), so the three steps above body moved and nothing else did. Measured at the ramp's ceiling: title 27.03 → **24.37**, lead 23.35 → **22.32**, display 31.95 → **26.62**. Below body the steps stay wide at 1.18×; above it they close to 1.09×. Quietest-to-loudest falls from 2.17× to 1.80×. **Not one call site changed** — P7's law that a face picks a step rather than setting a size is intact. |
| **A5** | **The 31½ logo, and the law behind it.** | **DONE, and it emptied the invention pen.** Mike: *"if it does not help, it hurts; if it does not need to be there, it needs to not be there"* — recorded in `OPERATIONS.md` §7 as Doctrine 16. The sweep he asked for found the count in three places and all three are gone: the 132pt card, its caption, and the FAQ's *"How many are there?"*, which is REMOVED rather than re-answered because there is no honest short answer that does not print the number. **Those three strings were the museum's entire `INVENTION` class**, so the provenance register reports 0 and its ceiling is ratcheted to 0 with it — declaring one again now requires raising that number in the same commit, which is a visible edit and is the point. Closes M1 by deletion; exposes M29. |
| **A6** | **Two things backlogged, not built.** | **DONE as rows** — PAPA EMAIL MANAGEMENT is [M31](#m31); THE ADMIN DASHBOARD is [C33](#c33). Neither was designed, scoped or started, on instruction. |
| **A7** | **The mechanical and unambiguous items.** | **DONE — two of them, and the register says why the rest were left.** [C14](#c14) (a WebP named `.jpg`) and [C15](#c15) (unscrubbed collage captions) were both mechanical and both had a measurable right answer. Nothing else in the register qualified: M9, M26 and the Foundation rows are Mike's rulings; C17's lint debt is explicitly semantic; C16, C29 and C30 need content that does not exist. **C14 exposed [C32](#c32)** — the asset table is keyed by path, so the rename dropped every judgement on that file in silence. |

---

## 5g. CLOSED IN v50 (THE OVERNIGHT, 2026-08-04)

| ID | What it was | How it closed |
|---|---|---|
| **N1** | **The renames and the removals.** | **DONE** — THE MORGUE struck for **IMAGE ARCHIVE** on both walls (closes M6); **The Firmware → TECHNICAL SPECIFICATIONS** on both faces that carried the name (see M24); **The Parts REMOVED** whole, which orphaned a photograph (see M9) and took three observations off the wing, named in the round log rather than dropped quietly; the directory's one article dropped (**Weird.Baby Foundation**, see M26) and the **Information Booth moved to the bottom of the board** with the exhibit order under it unchanged. |
| **N2** | **The image archive's structure and its siblings.** | **DONE** — A4 built "latest spread at top" and printed every spread at full height; this round built the other half of Mike's sentence. **The newest spread is open paper and everything older is stowed** in a native `<details>` whose closed line carries its own date AND its own count (`FEBRUARY 2013 · 3 plates`), so nothing is discovered by opening it that was not already stated by it — which is the test the NO-HIDDEN-INFORMATION law actually applies. Measured: closed **21px**, open **258px**. First spread and unheaded walls are never stowed, so the VIIIp wall is byte-identical. Siblings named VIDEO ARCHIVE / AUDIO ARCHIVE and **not scaffolded** (C16). |
| **N3** | **DOC CONTROL, and the manual's story out loud.** | **DONE** — a fourth front-desk face: manuals, originals, files, with Mike's canon printed as its own entry (came in pieces, presumably to avoid detection; no complete contents, no index, not every page, therefore not every answer; assembled from preliminary, final, marked-up and one stamped APPROVED). Its hook is a **rubber-stamp card made only of words already on the face**. Nothing on it describes this repository — the incompleteness is stated as a fact about the OBJECT, which is what makes it shippable and, per Mike, the cover. |
| **N4** | **Welcome and Contact, burned down.** | **DONE** — Welcome's proposed reason to exist is **ORIENTATION**, the job nothing was doing: its register block stops being a business card and becomes a contents list of the wing, and a new WHERE TO START row says which door is which. The invented count came out (closes M15). Contact is **the address and three things worth writing about**, one line each: the ranking, the persuasion and the four-line argument for its own slowness are gone, and the card that said ONE / ADDRESS / READ BY ONE PERSON without containing the address now contains the address. |
| **N5** | **The booth's ticket — both directions.** | **DONE, awaiting M23** — the "no ticket required" contradiction is gone and both candidates are LIVE for Mike to compare on the glass: a ticket **made of paper** (guilloche security tint, diagonal hatch, real perforation with torn notches, a punched hole lit from above, ticket stock rather than the ramp's ink) at `/booth`, and an **enamel INFORMATION plate** (keyline inset from the edge, four bolt heads) at `/booth?hook=sign`. Neither adds a claim. |
| **N5b** | **"Are you tracking me?", written against the code.** | **DONE, and it found the old answer wrong.** The previous answer said the visits row was *"the whole of it"* — and `index.html` ships a Google Fonts `<link>` plus two preconnects, so every visitor's browser calls Google before the page paints. That third party is now the loudest clause in the answer. Every other clause is falsifiable from one file: no auth anywhere in `worker.js`, no `Set-Cookie` and no `document.cookie` in the tree, three columns in the visits row, exactly-what-you-typed in the guest book, sessionStorage/localStorage never transmitted. The claim is deliberately scoped to *what this site records*. |
| **N6** | **The scrolling guest book.** | **DONE, awaiting M23** — the obvious reading was already built (`.wb-entries` has been a scrollable seven-row window since the book was made), so the variant is the book scrolling ITSELF. Two copies, a 50% travel, **measured seamless: travel at cycle end 270px against a 270px half**. Pauses on hover and focus-within; `prefers-reduced-motion` falls back to the static list rather than to a slower one; below five signatures it renders the static list because there is nothing to scroll past. |
| **N7** | **The Foundation's DONATED BY column.** | **DONE** — four declared values because a donor column whose empty cells all print one dash cannot tell "nobody has given yet" from "this was never a gift". ANONYMOUS is first-class and **appears nowhere**, which is M27 and is the point rather than the gap. |
| **N8** | **The reveal arc.** | **DONE** — `revealArc` (arrived / understood / partial / online / unset) is the asset table's **fifth judged field**, carried by a scan and never written by one, with its own legend. Populated on 6 rows the wing's own tombstones and captions attest; 245 left unset by instruction (M28). |
| **—** | **The asset table counted a path named in a COMMENT as a reference**, so any orphan was invisible for as long as anybody had written its name down. | **DONE** — caught by committing it: N1 removed The Parts and wrote a comment naming the newly-orphaned file, and the next scan reported that file as still shipped, cited by the very file that had stopped using it. `usedBy` now matches against source with comments stripped by a character scanner that keeps string literals (a regex cannot: `//` and `/*` both live inside strings all over this tree). Shipped 44 → 43; unreferenced 3 → 4. |
| **—** | **`--scan` could never add a new header key**, because it spread the file's own header over the tool's. | **DONE** — `HEADER` now wins for its own keys, so the legend is documentation with a source instead of a hand-edited field in a generated artifact. |
| **—** | **The enamel sign's subtitle was a verbatim echo of the credo 120px below it.** | **DONE** — found by looking at the built page, not the source. It now names the museum, which is the ticket's own kicker and does not repeat the line under it. |

---

## 5h. CLOSED IN v49 (2026-08-04)

| ID | What it was | How it closed |
|---|---|---|
| **A2** | **The tracklist/viewer title bar scrolled off the top.** Reported by Mike and unfixed. Measured on `/robots` at scrollY 500: the band's top was at **−132px**. It had been pinned on `/wal` and only there, because the stickiness was a property of the transport's class rather than of the band. | **DONE** — the pin moves onto `.ex-album-banner` itself, with F2's apron and P1's `pointer-events:none` travelling with it. The flat wing's contents column re-derives its own sticky offset from the band's height so it cannot print underneath it. Verified pinned at `top:52` on `/robots`, `/hr`, `/wal` desktop and at 390px. |
| **A3** | **"THE PLATES" renamed.** | **DONE, pending M6** — both walls read **THE MORGUE** with **IMAGE ARCHIVE** as the plain-language subtitle, so Mike chooses from the glass. The individual photographs are still called plates. |
| **A4** | **Archive structure.** | **DONE** — `ArchiveWall` stacks the collage in headed spreads sorted by record number descending, and the lightbox walks the whole archive in display order. No spread carries a number today because the museum holds none; the order falls to the authored newest-first, and the gap is M19. |
| **A5** | **The asset table.** | **DONE** — `provenance/asset-table.json`, 251 rows across both repos, scanned by `tools/asset-table.mjs`. The scan never overwrites a judgement. |
| **A6** | **The Record Approval Gate.** | **DONE** — doctrine recorded (`OPERATIONS.md` §7 Doctrine 15), wired to the table's `verdict` field via `npm run assets:gate` and `assets:checklist`. |
| **A7** | **Image quality pass.** | **DONE** — every shipped image looked at: 30 usable · 5 weak · 1 placeholder · 5 wrong; 6 audio files honestly marked *not listened to*. Nothing fixed without Mike. |
| **A8** | **The MGK-VIIIp cover becomes just the machine.** | **DONE** — cropped to the unit's measured bounding box, 1536², 8-bit grey (lossless), 2.69 MB → 1.40 MB. It also stopped the deck's `object-fit:cover` decapitating a 3:4 file. The common theme is written into `robots.js`; **the straighten is refused with measurements** — see M8. |
| **A10** | **The RESTATED provenance class stays.** | **DONE** — recorded as an Ops ruling in `OPERATIONS.md` §7 Doctrine 13 and in STATE.md. |
| **—** | **`CLAUDE.md` published a stale lint baseline** (4 errors / 6 warnings) where the live baseline has been **11 / 9** since at least v40. An orientation doc that misstates the tripwire disables the tripwire. | **DONE** — corrected in the same commit. |

---

## 5i. <a id="p-closed"></a>CLOSED IN THE REMOTE-CONTROL ROUND (P1–P11, 2026-08-05)

**Eleven instructions, ten built.** Round log:
`docs/MUSEUM_REMOTE_CONTROL_LOG-20260805.md`.

| ID | What it was | How it closed |
|---|---|---|
| **P1** | Parity between the two machines was a JUDGEMENT with written excuses. | **DONE, AND IT REVERSES THE PREVIOUS ROUND'S OPS RULING IN THE OPEN.** Parity is ABSOLUTE: `tools/menu-parity.mjs` fails on any divergence, the `JUSTIFIED` table and its HOLDINGS · PROPERTY · DESIGN classes are deleted, and so is the ledger read that faulted when a named holding arrived — **the best mechanism in the old file, mourned in its header, with nothing left to guard.** The mainframe gained **The Manual** and **FAQ**; both machines now read *Image Archive · The Manual · Technical Specifications · FAQ*. **THE STUB LAW IS OVERRIDDEN FOR THOSE TWO ROWS AND ONLY THOSE TWO**, with Mike's reason recorded where the rows are: *a row is a promise only when nothing is coming, and these are coming.* Doctrine 12 is untouched — the manual row states what is NOT held and invents no section list. Gate proved by breaking it on a file copy: 2 faults, restored, 0. |
| **P2** | The robots page order, the Portal's placement and THE NAME. | **DONE.** The Record sits above the FAQ on the front desk (same objects, swapped, nothing inside either changed). **THE PORTAL IS ITS OWN ALBUM, second in the deck** — top-shelf and not the landing — with its cover built on the wing's own template; the track is *Feed Control* so the album band and the face heading stop saying the same words. **THE NAME IS DELETED IN TOTAL** — see [M59](#m59) for the three things that went with it. |
| **P3** | The robots FAQ. | **DONE — every answer is Mike's, word for word.** Seven questions ship, one prints nothing ([M57](#m57)), and the tracking warning he required is written to agree with the booth's privacy answer clause for clause. Four real passages of the wing's own copy went with the questions that held them; the one with nowhere else to go is [M58](#m58). |
| **P4** | The Record's navigation. | **DONE — buttons AND cursor keys, which was the proposal.** `RecordJump`: NEWEST · OLDEST · UNREAD (the oldest record not yet opened) · INDEX. `←` `→` walk, `Home`/`End` jump, `Escape` closes — **and the coverflow now YIELDS the arrows while a record is open**, which is a real cost, stated. A record ARRIVES rather than appearing: remounted on every walk, so it fades up and scrolls its own headline into view only when it needs to, and nothing moves under `prefers-reduced-motion`. The read register is `src/lib/record-read.js` — the ONE setting deliberately not session-scoped, because an unread mark that forgets overnight is not one. **Verified against three records on a file copy, then restored: the walk, the register, the disabled states and the arrow yield all behaved. It renders NOTHING on today's one-record volume** and so does the old ‹ NEWER / OLDER › walk, which had been drawing two permanently disabled halves and a count reading "1 of 1" since M5. |
| **P5** | Session defaults, site-wide. | **DONE, AND THE SCROLL HALF WAS ALREADY BUILT WHILE THE SIZE HALF WAS BUILT FOR ONE WING.** F3 gave `usePersist` a session scope and handed it only to wings declaring `fitOnEntry` — /wal — so **/hr, /wb and /robots have been restoring a dragged split and carousel height across months and machines.** Every view setting is `sessionStorage` now: splits, carousel and viewer heights, /hr's deck width, and the `bodyKey` READ that decides whether the visitor has already chosen. `useArrival` added to the two routes that never called it (`/hr/archive`, `/admin`). **Two things stay in `localStorage` and the booth's privacy answer names both**: /hr's preset slots and the Record's read register — saved work, not view state. |
| **P6** | The title block. | **DONE, EVERYWHERE, BECAUSE IT IS ONE RULE.** `.vp-face-title` is the heading of every face in every wing: `--fs-head`→`--fs-lead` (24.4→22.3px), weight 800→600, `--wb-gold`→`--wb-gold-lo` (5.15:1 on paper), tracking .22em→.15em. The padding was two separate rules — `.vp-face-body`'s 30px top (→18px) and a **53px section step under the heading** on any face with no blurb, which is where he was reading. The page name went 1.1→1.2rem; the ≤720px branch is untouched, because its overlap arithmetic was measured at its own size. |
| **P7** | NIAC album art. | **DONE — THE FIRST PHOTOGRAPH OF THIS MACHINE WHOLE.** Cut from the 2021 build video at 00:58.0, cropped to the cabinet's own bounding box, re-encoded so the source's GPS tag does not travel. The badge FITS rather than crops — a square cut of an 858×1438 cabinet throws away two fifths of it — and the disc, its diameter and its ring do not move. **The robot is on the same bench and is out of frame**, so R4's canon is untouched; what IS spent is the withholding on the CABINET, and the wall's tombstone now says the opposite of what it said. |
| **P9** | Weird.Baby Music. | **DONE.** ABOUT THE ARTIST is first in the wing, carrying everything this museum already publishes about this artist and not one clause more: the booth's keeper answer verbatim, the holdings counted off the spine, *PORTRAIT — none on file*, and a biography slot marked in both fields so it prints nothing. **AND IT FORCED A SECOND FIX:** it is the first face /wb has ever declared, and it came up as *"Page 1 of 4"* behind a ‹ BACK / NEXT › transport — the pager Mike's no-hidden-information law killed at M1, still live here because the wing had no content for it to cut up. `faceFlow: "flat"`. **The museum now has no pager anywhere.** |
| **P10** | The Foundation. | **TWO OF THREE DONE.** The invoice is a **LEDGER** and says so — an invoice is a demand and this document demands nothing; the incoming table takes back its own name, **The register**, and the two prose pointers that named the wrong one were repointed. **"held, ever" is deleted** — the three rows under the figure already say it, in a register that reads as an argument being pressed. **THE TONE RULING IS RECORDED AS STANDING** at the head of the wing, with four tests and an audit: the copy already passes, which is written down because a ruling that changed nothing on its first day gets assumed decorative. The albums and tracklist are [M62](#m62). |
| **P11** | M54, and the logo. | **DONE.** weekendatmikeys.com is the homepage door on Mikey Mike's card and @findmikeymike keeps a door of its own — the template already drew both and he was the one artist whose HOMEPAGE door was a video channel. R-a's refusal of findmikeymike.com is untouched. The logo outline is [M63](#m63), ledgered and unbuilt. |
| **P8** | The manual online. | **NOT SHIPPED, AND THE REASON IS HIS OWN RULING** — [M61](#m61). The viewer is built and verified; the only document on file is the one P2 struck yesterday, 61 pages of `[ TEXT REQUIRED ]`. |

---

## 6. HOW THIS FILE WAS BUILT, so the next round can extend it the same way

Harvested, in this order:

1. `STATE.md` (museum) — FULL-LAUNCH GATES, NEXT, KNOWN ISSUES, the gift-shop
   still-open block, BACKUP STATUS, CANNOT-VERIFY, Backlog.
2. `STATE.md` (robots) — Open issues, Parked, Design doctrine.
3. The round logs' own carry-forward sections — v43/E5, v45/R5, v46/C4,
   v47 *"what this round exposes"*, v48/P4.
4. `docs/PROVENANCE_RULINGS-20260804.md` and
   `docs/RECORD_013_QUESTIONS-20260804.md`.
5. `docs/VISUAL_HOOK_AUDIT-20260803.md`.
6. A count of `[PAPA]` markers inside string literals in `src/`.
7. This round's own findings.

**Deliberately not harvested:** the ~130 historical run reports and briefs in
`docs/`. Their open items were either closed by a later round or carried into
`STATE.md`, and sweeping them would fill this register with items nobody can
tell the status of — which is the opposite of what it is for. If something is
missing from this file, add the row; do not add the archive.
