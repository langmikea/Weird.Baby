# THE LANDING — two rough shapes to point at (2026-09-18)

Ops, unattended, two hours. Follows `docs/SLICE-DAY1-20260918.md`: the walk
broke the moment the stranger left the reel, because a reel had no room to
land in. This is that room, rough, on stand-ins, in the development stage.
Nothing was deployed, posted or sent. Museum acc8efd.

## What now exists

- **A drop is a track.** Each released feature in the catalogue is a track at
  the head of its album. The album grows by one a day from the door and shows
  nothing that is not here yet (your no-dead-ends ruling). On 10-31 the
  Everyday has two: "MGK-NIAC, the twenty" and "Answers, the door".
- **A track has an address.** `weird.baby/robots/everyman-answers` opens the
  Everyday on its Answers track. That is what a caption points at.
- **"Today" has an address.** `weird.baby/robots/today` opens the newest drop.
  That is the profile link, the only link Instagram and TikTok let anyone tap.
- **The machine is one press away**, through the Portal's own door.
- **No empty first screen.** From the door on Saturday to Record 001 on Monday
  at five, the wing does not show an empty Record.

## The two shapes

| | lands on | the machine |
|---|---|---|
| **A** `/robots/everyman-answers` | the album, the day's track selected, its pitch and a RUN button | one press away |
| **B** `/robots/everyman-answers/run` | the machine, already coming up, the album behind it | first thing seen |

Pictures: `docs/desk/landing-range/` (phone), and sent to the session.
On your PC: `npm run dev`, then the two addresses above on localhost:5173.

## What the pictures show (Ops looked first)

1. **On a phone, A buries the point.** The track list fills the screen; the
   day's pitch and RUN are below it. A stranger from a reel is on a phone.
2. **B puts the machine first, and it fits a phone.** But the glass is dark:
   the machine takes about 45 seconds to wake. A stranger will not wait.
3. **B shows the wrong unit.** The plate reads "The Informer" inside the
   Everyday's album; the landing does not yet tell the machine who it is.
4. The album's name cuts to "The…" on a phone (it did before today).

## What Ops takes from it (no ruling needed)

- The machine must arrive awake, as the right unit, on the day's feature.
  That is one piece of work on the twin (a wake-on-arrival preset per drop)
  and it is the next thing under this, whichever shape is pointed at.
- RUN is a stand-in word.

## For Mike, one pointing

A or B, or "something between": on a phone, does the stranger meet the album
first, or the machine first?

## Pointed at, 2026-09-18

Mike: **"B"**. The stranger meets the machine first. Logged in `docs/desk/BOARD-DECISIONS.md`.

## Arrive awake, built 2026-09-18 (evening)

- The twin has a recipe `arrive`: powered, past the ceremony, an established
  unit. `?at=<catalogue row id>` stands it on the day's feature; the map is
  `ARRIVE_AT` in twin.html (robots `tools/viiip_twin.html` is master), written
  as menu labels, so `Arrive_Check()` in the twin's console names any drop
  whose path stops walking. All 23 walk today.
- A drop track's RUN and the `/run` landing both send it. Seen on the dev
  server, phone size, `/robots/everyman-answers/run`: lit in about four
  seconds on ASK MGK with MGK-NIAC selected; one CLICK runs Answers; Back
  returns through Answers to the four doors.
- An id the twin does not know lands at the four doors, awake. Day 2
  ("Starting") plays the boot, because the boot is that day's feature. A
  machine the visitor switched off stays off.
- The boot is still one press away for anyone who chooses it (POWER).

**The plate, ruled the same evening.** It read "The Informer" because those
words are in the photograph the twin is drawn on
(`MGK-TWIN_MONITOR_SCREENS_FAMILY_SHOT.png`). Mike: **"One plate for every
album, names no unit."** The twin now wears
`MGK-TWIN_MONITOR_SCREENS_NO_UNIT.png`: the same photograph with the second
line of the nameplate taken back to bare metal, derived by the robots repo's
`tools/twin_plate_no_unit.py` (`--check` fails if it goes stale). The master
is untouched; the museum still shows it as the Informer. Seen on the phone
landing: MGK-VIIIp, the rule, bare plate.

And, unprompted: **"For the twin, let's use unit 0.4, Prototype."** Logged in
`docs/desk/BOARD-DECISIONS.md`; the canon pages that hold .4 "reserved" wait
for his wording.
