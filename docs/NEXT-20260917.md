# NEXT — Mike's list of 2026-09-17, scoped for fresh sessions

Mike: "whatever we start should be a fresh code." Each start below is one
session's brief. Ops answers and assessments are inline.

## Music

**M1. Process ALL existing material into usable reels, or decide explicitly
not to.** Includes the voice-only files (oscilloscope treatment). Catalogue
it all first: every clip and take on OneDrive > WeirdBaby (reels intake and
out, the 09-03 Coconuts reels on the shelf, the Number, the four w2 clips,
the tray movies), C:\AI\MUSIC-20260903, the Desktop > Weird.Baby drops, and
the phone folders he names. Deliverable: a catalogue (one row a file: what,
when, length, lane, usable / not, why) as a served page, then the reels cut
from the usable rows by the existing templates (Q&A, feature, Number), and
a decision line per row he can rule A/B/C. He is right that this refines the
SED structures: every row that does not fit a template names a missing
shape.

**Done 09-17 (night):** the survey, one row a file: `docs/MATERIAL-20260917.json`
(174 rows, nine places), rendered by `tools/material.mjs` to
`docs/desk/MATERIAL.html` (thumbnails embedded, built outside the repo),
served at https://claude.ai/artifact/UXUfm68tfsaxosyyxzEUXi. The
find: the front-porch session of 2026-08-24 from the Audigo, in OneDrive ›
Desktop - Computer Room (a 22-minute portrait movie and its 24-bit wav), on no
ledger; three full takes of Coconuts, one false start, an abandoned E.D.
Yahdah, transcribed and mapped by take. Six pieces cut through the reel line
by `tools/material-cut.py` into OneDrive › WeirdBaby › reels › out › material
(the hook, the bridge, take 1 whole, take 3 whole, the E.D. Yahdah attempt,
and a sketch of the audio-only shape: the June Coconuts demo as a waveform in
the machine's yellow). Four missing shapes named on the page (audio-only
number, session splitter, live single, the fly-by). Not in hand: the
originals of the four YouTube videos, the Tuesday element takes, anything on
the phone. Sunday question 6 asks for the rulings.

**M2. Guitar tabs: create and publish.** Sunday question 3 asks where
(weird.baby, Ultimate Guitar, both). The tabs come from his recordings (chord
detection over the audio, then his ear on the result) and from the chord
captions rule already logged for the music reels (chords in time, like
captions). Fresh session once the venue is ruled.

## Artifacts

**A1. The Gambler's OPA.** Found and handed over: Vegas.jpg in the OPAs
archive (OneDrive MAJEL_04 > Mike's Stuff > _ROBOTS > Weird.Baby > PROJECT
CLOSING > OPAs > OPA - Other), copied to OneDrive > WeirdBaby > artifacts >
OPA > Gambler-OPA-Vegas.jpg (1161 x 1600). It is a Vaseline Hair Tonic ad
from the "What kind of man uses Vaseline Hair Tonic?" campaign
(Chesebrough-Pond's, 1966 to 1968), the blackjack story: "He's watching a
girl lose heavily at Black Jack... You saw the top card reflected in your
ring. Some men don't miss a trick." **The issue, from Mike: LIFE magazine, April 15, 1966.** To replace it,
search the dealers for that issue or for "1966 Vaseline Hair Tonic ad
blackjack" (Attic Paper, Vintage Adventures, eBay).

**A2. The artifact reels: 1960s colour television captured to film.** Grainy,
faded, off-tint, warbly lo-fi music, in the style the story calls for
(business, automobiles, insurance, fun girls, gambling, gaming) or a blend.
Fast-turn samples to review and present. Fresh session: story first (the
look as a template, the artifact as data), three samples from the tray
photographs (Gambler set) at three styles, reviewed on a sheet, then his
letter. Sunday question 4 asks per-story style or a blend.

**Done 09-17 (late):** story `docs/BROADCAST-LOOK-20260917.md` (the look as a
stack of layers: plate, motion, grade, television, film, titles, sound, the
pop; a style is one row of settings); `tools/reels-broadcast.py` builds three
samples on the Gambler set (the late show for gambling and gaming, the
showroom for automobiles and fun girls, the boardroom for business and
insurance) into OneDrive › WeirdBaby › reels › out › broadcast, a frame strip
per sample, and the served sheet with the letter, `docs/desk/BROADCAST.html`
(https://claude.ai/artifact/DPMgazcJHDQK94RN8o5SYk). The beds are his own June demo of Coconuts through the period's
radio; a period library is a licensing question. One law named in the letter:
the 09-06 reading of never-advertised ("never an artifact") against the 09-10
reset that made the artifacts a thing to sell. Ops' read: per story, the
television and film layers shared.

**A3. Tray shots were never meant to be used as-is.** Cropped at least to the
full tray, mostly far beyond, down to one element of one artifact. No
artificial limit. The 09-10 rule ("crop inside the tray") is superseded; the
memory is corrected. Applies to the catalogue, the albums and A2.

**A4. The morning task fails on permission.** Looked. The 09-16 run
diagnosed it exactly: the allowlist approves npm run but every improvised
command (git status, commit, push, the node check, the find over OneDrive,
the MORNING.md write) is new wording each morning and waits for a person.
Its proposal is right and is Ops' call: one script, tools/morning.mjs as
npm run morning, doing the whole run in order and printing a NOTIFY line
only when the message rule applies; one exact approval for that command in
the C:\AI settings; the task file cut to three lines. Zero prompts without
loosening anything else. Fresh session, thirty minutes, then one test run.
**Done 09-17 (evening):** `tools/morning.mjs` as `npm run morning`: git
status, desk, workbook (Excel-open tolerated), days, calendar, the late
tasks, what landed in intake and photos since the last run
(`docs/desk/MORNING.state.json` remembers), the Q&A build and dry queue,
`docs/desk/MORNING.md`, then a commit of only the run's own paths and a
push; a `NOTIFY:` line only when the message rule applies. The task file is
three lines and runs one command, `npm run morning --prefix
C:/AI/Projects/weird-baby-museum`, which already matches the standing
`Bash(npm run *)` approval; the exact-approval line could not be written by
Ops (the harness refuses to let a session edit its own permissions) and is
handed to Mike as two lines to paste. Two test runs passed end to end
(0be84d5, b0b7fdc). `__pycache__` untracked and ignored: it dirtied every
morning.

**A5. Is the calendar still valid, and up to date?** Valid in shape, stale in
content. The plan of 09-10 holds: the door on 10-30, four albums, the Q&A
line from 10-31, the Records as promotion copy. The task rows do not: Mike's
31 rows are the practice reels and element takes of the story-era plan, 0 of
32 done and only one late by the calendar's own rule; the feature reel line
(09-16, 09-17) and the music catalogue are not on it; the Records 006 to 010
are "not written"; the week-3 wing build from the mocks has not started. Ops'
rows read 10 of 32 done and are ahead of the plan. Recommendation: re-cut the
calendar at the Sunday sitting, one sheet: what Mike actually shoots next
(the tray re-shoot for the albums, the ask on camera), what Ops builds (the
ask reel, the music catalogue, the broadcast look, the morning script), and
the parked lines (the game features). Sunday question 5 asks for the re-cut.

**Done 09-17 (late), in session:** Mike brought it forward ("so much has
changed"). Four rulings, one at a time, in `docs/desk/CALENDAR-RECUT-20260917.md`;
applied to TASKS.json and BLOCKS.json; `npm run desk` rewrote the workbook,
the Days page and the feeds. His rows: 32 to 14 (the fifteen Q&As, four
tray re-shoots, three pieces of writing, three sittings, Buffer, one Number
sitting, the bench). The artifact reels are shelved (A2 §8).

## Fresh sessions, in the order Ops would run them

1. A4 the morning script (unblocks every day after).
2. M1 the catalogue of everything in hand (unblocks the music reels and the
   SED refinement).
3. A2 the broadcast look, three samples (fast, reviewable).
4. The ask feature reel on MGK-NIAC (a finished feature; the mashup with
   the Q&A line).
5. M2 tabs, after the venue is ruled.
