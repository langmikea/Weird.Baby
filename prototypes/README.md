# Prototypes

Standalone HTML thinking instruments. None of these are target UI.

- `prototype_a_v28_3.html` — reference implementation for
  `UX_CONTROLS_SPEC_v0.4 §§4.8/5.5/6.5/9` (current; earlier docs say
  v17/v27 — superseded).
- `prototype_presets_v3.html` — **not yet committed** (file pending from
  the 2026-06-05 presets design session). Throwaway thinking instrument
  for `docs/UX_PRESETS_SPEC.md`; concept-validated but explicitly NOT
  the target UI (spec §0.1 — its boxed two-column layout must not be
  built). When the file resurfaces, drop it in here unchanged.
